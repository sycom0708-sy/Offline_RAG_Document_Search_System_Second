# CLAUDE.md — 오프라인 RAG 문서 검색 시스템

기획 문서: `PRD_오프라인RAG시스템.md`(제품 요구사항) / `TECH_오프라인RAG시스템.md`(기술 설계) / `TASK_오프라인RAG시스템.md`(작업 분해·진행 상황) / `DESIGN_오프라인RAG시스템.md`(UI 디자인 명세, 목업 v3 기준 — Phase 4~5 구현 기준 문서) / `PLAN_오프라인RAG시스템.md`(Phase별 실행 계획·계획 대비 결과 누적 기록) / `PROMPTS_오프라인RAG시스템.md`(Phase별 실행 프롬프트 + 모델 추천) / `PHASE_오프라인RAG시스템_단계별_구현계획서.md`(Claude Code plan 모드 계획 원문 아카이브 — 승인 시점 스냅샷, 보고서 작성용)

**Phase 착수 시 `PLAN` 문서를 먼저 확인한다.** 각 Phase의 모듈 구조·선결 결정 사항·이전 Phase에서 넘어온 과제가 정리되어 있다. Phase 완료 시 해당 Phase의 "실행 결과" 절을 채우고 다음 Phase 계획을 갱신한다.

**Phase 완료 시마다 plan 모드 계획 파일(`~/.claude/plans/*.md`)의 해당 Phase 원문을 `PHASE_오프라인RAG시스템_단계별_구현계획서.md` 맨 위에 이어붙이고 git으로 커밋한다.** plan 파일 자체는 프로젝트 저장소 밖(로컬 PC 전용)에 있어 다른 PC로 옮기거나 버전관리가 안 되므로, 이 저장소 안에 사본을 남겨야 나중에 보고서를 쓸 때 착수 시점 계획 원문을 참조할 수 있다. TASK/PLAN/CLAUDE.md 갱신과 함께 Phase 완료 체크리스트의 일부로 취급한다.

```bash
python -m scripts.archive_plan --list   # 후보 + 출처 판정
python -m scripts.archive_plan --all    # 판정을 통과한 것만 아카이브
```

**🔴 손으로 복사하지 말고 위 스크립트를 쓴다.** `~/.claude/plans/`는 **전 프로젝트 공용**이고 파일명이 무작위(`linked-hugging-clover.md` 같은 식)다. 이 PC에는 **같은 주제(오프라인 RAG)의 다른 프로젝트**가 있어 계획서가 섞여 쌓이는데, 제목이나 주제로는 구분되지 않는다 — 둘 다 "표 청크", "임베딩 128토큰" 같은 말을 쓴다. 실제로 갈리는 것은 **구조**다(`indexing/` vs `indexer/`, `content_original` vs `content`). 스크립트가 계획서에 나온 파일 경로가 이 저장소에 실제로 있는지 대조해 남의 것이면 막는다. 판정이 틀렸다고 확신하면 `--force`.

**Phase 착수 직전에는 `PROMPTS` 문서의 "모델 추천" 표를 확인해 사용자에게 알려준다.** 해당 Phase에 Opus 5 전환이 권장되는 결정 지점이 있으면 먼저 짚어주고, 그 결정 이후 구현은 Sonnet 5로 이어가도록 안내한다.

## 개발 환경

**Python 3.14.6 기준.** venv 재생성이 필요하면 `py -3.14 -m venv .venv` 후 `requirements.txt`를 설치한다.

```bash
./.venv/Scripts/python.exe -m pytest -q
```

전체 통과 시 **376 passed**. LibreOffice·`.hwp` 샘플·임베딩 모델이 없는 환경에서는 일부가 스킵되며, 이는 실패가 아니다 (환경별 예상 결과는 `parser/README.md` 참고).

임베딩 모델은 용량 때문에 저장소에 없다. 인터넷이 되는 PC에서 한 번 받아둔다:

```bash
./.venv/Scripts/python.exe -m indexer.vector.download
```

## 진행 상황

- **Phase 1 (문서 파서 모듈) — 완료 (DoD 충족)**: 9종 형식 파서(`parser/`)를 TECH 4.2절 공통 청크 스키마로 통일해 구현, 표는 행·열 구조 보존 + 이미지는 삽입/렌더링 캡처 구분해 텍스트와 분리 저장. 테스트 126 passed / 0 skipped, 9종 전부 실검증(hwp는 실문서, doc·xls·ppt는 LibreOffice 실변환). 실환경 검증에서 버그 3건(HWP 이미지 전량 누락, 단일행 표 데이터 소실, soffice CP949 출력 유실)을 잡았다.
- **알려진 성능 이슈**: 구버전 포맷은 건당 2.47초(순정 0.01초) — Phase 9 패키징 시 배치 변환 검토 필요. 상세는 `parser/README.md`.
- **Phase 2 (FTS5 키워드 인덱싱) — 완료 (DoD 충족)**: `indexer/` 모듈로 폴더 스캔→청킹(kss+정규식 폴백)→FTS5 저장(external content 트리거 동기화)→BM25 검색까지 구현. "대/소문자 구분"은 원문 보존 후처리 필터링으로, "일치되는 단어"는 접두(`"어절"*`)/완전 토큰 매치 전환으로 해결(둘 다 DESIGN §4.2 토글과 실제 연동 확인). 표 청크는 캡션·헤더를 별도 컬럼에 담아 BM25 가중치 5배로 상위 노출. `IndexingThread`로 백그라운드 인덱싱 + 진행 콜백. 임시 CLI(`python -m indexer.cli index|search`)로 실검증. 테스트 51건 추가(누적 177 passed / 0 skipped). 상세는 `PLAN_오프라인RAG시스템.md` Phase 2 절 참고.
- **Phase 3 (임베딩 연동 + 벡터 재순위) — 완료 (DoD 충족)**: `ko-sroberta-multitask` int8 ONNX(111MB)로 torch 없이 추론(`indexer/vector/`), FTS5 후보 → 코사인 재순위(`search/hybrid_search.py`). **벡터 저장소는 ChromaDB 대신 SQLite BLOB** — ANN을 안 써서 ID 조회만 필요한데 ChromaDB는 79개 패키지를 끌고 오기 때문(TECH 5.1 정정 완료). 검색 지연 7~14ms. 테스트 233 passed / 0 skipped.
- **Phase 3에서 Phase 2 결함 2건 발견·수정**: ① `kss`가 53자/초로 실사용 불가(8,267자에 157초) → 정규식 분리로 전환 ② 자연어 질의가 0건 반환(AND 조건 + 한국어 조사 미처리) → 조사 제거 + OR 폴백. 청킹도 문자 수 → **토큰 수** 기준으로 변경(자/토큰 비율이 0.50~2.70으로 흔들려 문자 수로는 모델 한계를 지킬 수 없음).
- **Phase 4 (추출형 검색 UI) — 완료 (DoD 충족, MVP 완료 지점)**: `ui/` 모듈로 PySide6 기반 검색 앱 완성. 검색바(debounce) → 사이드바(형식 필터/대소문자·일치단어·AI요약 토글/PC 성능 콤보) → 결과 카드(하이라이트+발췌 윈도잉, 관련성 낮음 흐림) → 상태바(인덱싱 진행률/폴더 관리) 전체 구현. `search.hybrid_search()`를 `SearchWorker(QThread)`로 블로킹 없이 호출, 요청 일련번호로 늦게 도착한 결과 폐기. 모델 관리 ↔ PC 성능 선택은 Option A로 연동(콤보는 선택 트리거만, 전환은 모델 관리 팝업에서만), `KURE-v1`은 ONNX 미제공이라 "준비 중"으로만 노출(TECH 9.3 "임베딩 항상 설치됨" 문구 정정 완료). 동시 DB 접근(백그라운드 인덱싱+검색) 대비 **SQLite WAL 모드** 도입. 테스트 125건 추가(누적 358 passed / 0 skipped). 실제 데모 폴더(실 hwp 문서 포함)로 검색→필터/토글 교차→원문 열기까지 `QWidget.grab()` 스크린샷으로 시각 검증 완료. 상세는 `PLAN` 문서 §4-D 참고.
- **Phase 4에서 잡은 버그**: 토글 스위치 라벨·스위치 순서가 목업과 반대(합성 위젯으로 재작성), PC 성능 콤보 텍스트 잘림(레이블 축약+툴팁), `ResultList` 잔상 위젯(`setParent(None)` 누락), 상태바 UTC/로컬 타임존 혼선, `_ModelRow` 포커스 무반응(`FocusPolicy` 누락). 전부 실제 창을 띄워본 시각 검증에서만 드러났다 — 자동화 테스트는 통과한 상태였다.
- **T4.11b(고성능 모드 재인덱싱 트리거)는 코드만 연결, 종단 검증 불가**: KURE-v1이 실제로 설치될 수 없어 이번 Phase에서 확인하지 못함 — KURE-v1 변환 파이프라인이 생기는 이후 Phase에서 재검토.
- **Phase 5 (표 카드 / 이미지 카드 렌더러) — 완료 (DoD 충족)**: TECH §6.3 청크 타입 기반 동적 라우팅 완성. `ui/widgets/card_common.py`가 세 카드(텍스트·표·이미지)의 헤더(파일명·위치·원문 열기)를 상속 없이 빌더 함수로 공유. 표 카드는 `QTableWidget`(헤더 없는 1행 표도 처리, "표 복사"로 TSV 클립보드 복사), xlsx는 `page_or_slide`(시트 인덱스) 대신 `TableData.caption`(시트명)을 위치로 표시. 이미지 카드는 `ui/thumbnail_cache.py`(Pillow 없이 `QImage`만으로 300px 캐시)로 썸네일 표시 + "확대"(`QDialog`, 화면 80% 이내로 스케일 — 목업에 스펙이 없어 자체 판단[제안]) + 미인식 안내 문구. 테스트 18건 추가(누적 376 passed / 0 skipped). 상세는 `PLAN` 문서 §5-B 참고.
- **Phase 5에서 잡은 버그**: `QTableWidget`이 부모 창에 실제로 붙어 화면에 표시되기 전 계산한 헤더 높이가 표시 후보다 작게 나와(24px→34px) 표 카드 마지막 행이 잘렸다 — `showEvent()`에서 실제 표시 직전에 높이를 다시 계산하도록 수정. Phase 4의 `StyledCheckbox`와 같은 종류로, QSS 폴리시 타이밍 관련 함정이 반복됨. 이번에도 자동화 테스트는 통과했고 `QWidget.grab()` 시각 검증에서만 드러났다.
- **Phase 6 (sLM 후보군 실측 검증) — 완료 (DoD 충족)**: `slm/` + `scripts/{setup_llamacpp,build_slm_testset,benchmark_slm}.py`로 측정 하네스 완성. 추론은 llama-cpp-python이 아니라 **llama.cpp 사전 빌드 바이너리(b10306) + `llama-server` 서브프로세스**(py3.14 휠 없음 → 컴파일 필요 → "관리자 권한 불필요"와 충돌). 실문서 25문항으로 3종 측정 — **기권 정확도 EXAONE-4.0-1.2B 100% / Phi-4-mini 27.3% / Qwen3.5-4B 100%**. **최소 사양 결론: sLM 요약 기본 OFF 유지**(준수율이 쓸 만한 Qwen3.5-4B는 지연 34초·메모리 4.2GB, 빠른 EXAONE은 과잉 기권 50%). **Phi-4-mini는 전 사양 제외** — 지어낸 답에 근거 번호 `[1]`까지 붙인다. 테스트 74건 추가(누적 435 passed). 상세는 `PLAN` §6-B, 하네스 사용법은 `slm/README.md`.
- **Phase 6에서 잡은 함정**: ① **system 메시지가 모델에 도달하지 않았다** — EXAONE-4.0의 chat template이 system을 렌더링에서 버린다(`/apply-template`로 확인). 그대로 4종을 쟀다면 "규칙을 안 지키는 모델"이라는 정반대 결론이 나왔을 것이다. 규칙을 user 메시지로 옮기고, 모델을 올릴 때마다 프롬프트 도달을 사전 점검하도록 했다 ② Qwen3.5는 thinking 모드가 기본 켜짐 — 300토큰을 사고에 쓰고 빈 응답을 준다(`--reasoning off`로 해결, `--reasoning-budget 0`은 무효) ③ 표본 육안 검증에서 채점기 오판 1건("어느 시점"↔"언제든지") → 동의어 채점 + `--rescore`(재추론 없이 재채점) 도입.
- **T6.8 권장 사양 측정 완료 [2026-08-10, Core Ultra 5 125U/16GB]**: 저사양 PC의 인덱스·테스트셋을 옮기는 대신(doc_id가 **파일 절대경로**의 해시 기반이라 — 내용이 아니다, `parser/utils/ids.py`의 `uuid5(ns, 정규화된 절대경로)` — PC나 세션이 달라 경로가 조금만 달라도 `chunk_ids`가 깨짐 — Phase 3의 "인덱스는 이식할 수 없다"와 동일 원인 [정정, Phase 11-D 착수 조사 중 확인]) **이 PC의 exdoc 폴더 기준으로 26문항 테스트셋을 새로 작성**해 측정했다. **Qwen3.5-4B**(지연 34초→18.3초로 절반 가까이 개선) / **EXAONE-3.5-7.8B**(11.4초로 오히려 더 빠름, 메모리는 8.3GB로 2배) 둘 다 기권정확도·응답정확도·과잉기권이 완전히 동일(81.8%/80.0%/6.7%) — 우연이 아니라 **두 모델 다 객관식 "정답이 뭐야" 질문에 발췌에 없는 답을 자기 지식으로 채우는 동일한 실패**를 보였다(근거 번호까지 붙여서). **최종 채택: Qwen3.5-4B** [사용자 확정, 2026-08-10] — 속도는 EXAONE-3.5-7.8B가 근소하게 빠르지만(11.4s vs 18.3s), 이 PC에서 안드로이드 스튜디오로 앱 개발과 동시 작업할 계획이라 메모리 여유(4.8GB vs 8.3GB, 16GB 중)를 우선했다. 문서 수(~1천 개)는 프롬프트에 들어가는 게 검색으로 추린 상위 발췌 몇 개뿐이라 이 결정에 직접 영향은 없음(문서 수 증가는 백그라운드 인덱싱 부담이라 별개 이슈). 객관식류 무근거 답변은 사양·모델과 무관한 별도 과제(Phase 7 대상)로 남는다. 테스트셋이 저사양 25문항과 달라 `--compare` 병합은 보류. 상세는 `PLAN` §6-B T6.8.
- **Phase 7 (sLM 답변 생성 옵션 모드) — 완료 (DoD 충족) [2026-08-10]**: TECH 5.3의 4단계 안전장치를 전부 구현하고 채택 모델(Qwen3.5-4B)을 적용했다. 착수 전 코드에 근거가 없던 판단 3건을 사용자와 확정: ① sLM 서버는 **유휴 5분 자동 종료**(안드로이드 스튜디오 동시 작업 전제 — 4.8GB를 놀 때도 물고 있으면 안 됨) ② 4단계는 "권장 사양 전용" 대신 **항상 켬**(계산 비용 ≈0, 애초에 사양 판정 근거가 코드에 없음) ③ 요약 출력 위치는 DESIGN에 명세가 없어 **결과 목록 맨 위 요약 카드**로 신규 정의(DESIGN §5.8). 3단계 출처 표기는 모델에게 파일명을 직접 쓰게 하지 않고 `[N]` 번호만 받아 코드가 결정론적으로 치환(4B급이 파일명을 지어낼 여지 차단), 4단계는 문자 bigram 겹침도로 근거 없는 문장을 잡는다. **T7.11 회귀 측정(Phase 6 테스트셋 26문항을 앱과 같은 경로로)에서 기권정확도·응답정확도·과잉기권이 Phase 6 순수 추론과 완전히 동일**(81.8%/80.0%/6.7%)했고, **Phase 6에서 놓쳤던 지어낸 답 2건을 4단계가 전부 잡았다**(놓친 것 0건). 모델 매니저는 측정 후보 4종 중 채택된 2종만 노출(`SLM_OFFERED`) — 탈락한 EXAONE-3.5-7.8B(8.3GB)를 보여주면 "메모리 때문에 Qwen을 골랐다"는 오늘 결정과 어긋난다. 테스트 70건 추가(누적 558 passed / 5 skipped). 실물 llama-server로 검색→요약→창 닫기까지 종단 실행해 고아 프로세스 없음 확인. 상세는 `PLAN` §7-A.
- **Phase 7에서 잡은 함정**: ① 모델 매니저 "새로고침"이 누를 때마다 GB 단위 GGUF를 통째로 재해시해 수십 초 멎었다 — 크기·mtime 캐시(`data/slm_verified.json`)로 해결 ② `QLabel.isVisible()`은 부모 창이 화면에 안 올라온 상태면 `setVisible(True)`를 해도 False다(Phase 4·5에서 반복된 "화면 부착 전/후" 함정과 같은 종류) — `isVisibleTo(self)`로 수정, 테스트가 잡았다 ③ 배선 테스트용 4문서 픽스처의 실제 유사도가 1단계 임계값(0.5) 밑이라 그대로 쓰면 요약 배선 자체를 테스트할 수 없었다 — 유사도를 직접 지정해 배선만 분리 검증.
- **Phase 7.5 (KURE-v1 임베딩 변환 파이프라인) — 완료 (DoD 충족) [2026-08-10, 우선순위 상향 신설]**: Phase 3부터 "레포에 ONNX 없음"으로 미뤄온 고성능 임베딩을 `scripts/convert_kure.py`(safetensors→fp32 ONNX→int8, `.venv-convert`에서만 실행해 프로젝트 venv는 torch-free 유지)로 실제 변환했다. 🔴 **KURE-v1은 mean이 아니라 CLS 풀링**이었다 — 착수 전 `1_Pooling/config.json` 확인 안 했으면 검색 품질이 조용히 나빠질 뻔했다(실측: CLS 코사인 0.9826 vs mean 0.7377). 변환 정확성 최소 0.9758, 양자화 재현성은 경량 모델과 동등(중앙 0.984대). 사내망 TLS 검사 프록시로 다운로드가 막혀 `truststore` 추가(원인: requests가 certifi 번들을 써 사내 CA가 없음 — 기존 다운로더들은 urllib라 안 겪던 문제). **T4.11b 종단 검증 중 `chunk_vectors` PK 버그 발견**(`chunk_id` 단독 PK라 모델 전환 시 이전 모델 벡터가 통째로 삭제됨 — Phase 3의 "모델별 공존" 설계가 스키마 단에서 애초에 성립 안 함) → `(chunk_id, model)` 복합키로 수정 + 기존 DB 자동 마이그레이션. **결론[사용자 확정]: 처리량 5.4배 느림(28.5→5.3청크/초), 품질은 10문서로는 결론 미확정 → 고성능을 기본값으로 밀 근거 부족, 경량 기본값 유지.** 테스트 16건 추가(누적 574 passed / 5 skipped). 상세는 `PLAN` Phase 7.5.
- **Phase 7.5에서 잡은 함정**: ① 표 청크는 chunker.py가 구조 보존을 위해 의도적으로 분할 안 함(Phase 1 결정) — 239행 표 하나가 13,625자 청크로 통째로 들어가 있었는데, LIGHT(max_seq_length=128)에선 자동으로 잘려 안 드러나다가 KURE-v1의 실제 한계(8192)를 쓰자 배치 패딩이 전체를 8192로 끌어올려 어텐션 행렬이 **64GB**를 요구하며 실패(batch16×head16×8192²×4bytes) — 인코딩 길이에 512토큰 안전 상한(`_MAX_SAFE_ENCODE_TOKENS`)을 둬 해결, 이 PC 실측 메모리 피크 2.75GB로 확인 ② 착수 전 조사에서 "원본 2.27GB는 문서 오기, 실제 568MB"라고 판단했는데 **틀렸다** — 허깅페이스 API의 `usedStorage`(Xet 중복제거 기준)를 파일 크기로 오독한 것이었고, 실제 다운로드로 재확인하니 2.27GB가 맞았다.
- **Phase 6 작업 중 발견한 Phase 4 크래시 (수정 완료)**: 검색이 **겹치면** 앱이 통째로 죽었다(`0xC0000409`). `_run_search()`가 워커를 `self._active_worker` 한 자리에만 붙들어, 다음 검색이 참조를 덮어쓰면 실행 중인 `SearchWorker(QThread)`가 GC에 수거되며 Qt가 죽는다 — "검색 후 결과 대기 중 필터 조작"이면 밟는 실사용 경로다. 실행 중인 워커를 `set`으로 전부 붙들고 `finished`에서 빠지도록 수정, 같은 종류인 창 닫기 크래시를 막는 `closeEvent`도 추가(인덱싱은 `stop_event`로 중단 요청 후 최대 5초 대기). **Phase 4의 시각 검증에서는 검색을 한 번씩만 해서 못 잡았고, 자동화 테스트 125건은 전부 통과 상태였다.** 상세는 `PLAN` §4-D ⑦.
- **최소 사양 기준기 변경 [사용자 확정, 2026-08-07]**: PRD 4장의 "i5 9세대 / 8GB"를 **i5-8265U(4C/8T) / 8GB**로 바꿨다. 실측 가능한 실기를 기준으로 삼기 위해서다(9세대 데스크톱보다 낮은 저전력 U형이라 기준 완화가 아님). Phase 3이 Phase 9로 미뤄둔 "8GB 실기 재측정"은 기준기가 확보돼 실행만 남았다.
- **구버전 포맷(doc/xls/ppt) 변환 = LibreOffice 포터블 동봉으로 확정 [사용자 확정, 2026-08-08]**: Office COM(Word/Excel 자동화)을 대안으로 실측 검토했다 — 변환 결과가 기존 파서로 정상 파싱되고 MOTW(다운로드 출처 표시) 파일도 문제없이 열리지만, 파일마다 새 프로세스를 띄우면 건당 7.0초로 LibreOffice(2.47초)보다 느리고 인스턴스를 재사용해 0.5초로 줄이려면 상주 프로세스·좀비 프로세스 처리를 새로 구현해야 한다. **지금은 채택하지 않고 이미 있는 LibreOffice 포터블 동봉(`vendor/LibreOfficePortable/`)으로 확정** — 실제로 배치해 `.doc`·`.xls` 재인덱싱 성공 확인(0청크 → 13청크·1청크). LibreOffice 미배치 시 자동 다운로드는 하지 않고 **직접 설치 안내만 표시**하기로 했다. 상세는 `TECH` 9.1절, `PLAN` Phase 3·Phase 10 절.
- **T10.2(구버전 포맷 실패 안내) 완료 [2026-08-09]**: 근본 원인은 `LegacyOfficeParser`가 예외를 던지지 않고 `document.status=FAILED`만 남겨 `IndexReport.failures`가 애초에 채워지지 않던 것 — `.doc`/`.xls`가 오류 없이 0청크로 조용히 빠지는 버그의 정체였다. `indexer/pipeline.py` 한 줄 수정으로 고쳤더니 **CLI(`indexer.cli`)도 덩달아 고쳐졌다**(원래 실패 목록을 출력하는 코드가 있었지만 지금까지 채워진 적이 없어 무용지물이었다). 상태바에 "구버전 문서를 변환하지 못했습니다(파일명). LibreOffice 포터블을 …에 넣으세요" 정적 안내를 추가했다(자동 다운로드 없음). 테스트 9건 추가, 전체 469 passed. 상세는 `PLAN` Phase 10 T10.2.
- **T10.3(원문 열기 실패 무반응) 완료 [2026-08-09]**: 앱을 실제로 띄워 테스트하다 "파일 열기 안 되는데"로 발견. `ResultCard`/`TableCard`/`ImageCard`는 실패 시 `open_failed`를 emit하지만 **받는 곳이 `MainWindow` 어디에도 없었다**(Phase 5에서 상위 연결을 빠뜨림) — 버튼을 눌러도 신호만 나가고 화면엔 아무 반응이 없었다. `ResultList`가 카드들의 `open_failed`를 한 자리로 relay하고, `MainWindow`가 T10.2의 `set_warning()`으로 사유를 보여주도록 연결했다. 기존 카드 단위 테스트는 신호가 emit되는지만 봤지 어딘가에 **도달**하는지는 검증한 적이 없어 버그가 테스트를 다 통과한 채 남아 있었다 — 이번엔 종단 테스트를 추가했다. 겉보기 증상이 같았던 별건 하나도 함께 확인: `data/index.sqlite3`(권장 사양 PC에서 옮겨온 인덱스)는 문서 20건 중 17건이 이 PC에 물리적으로 없다(다른 PC 계정의 Temp 경로, 없는 `D:` 드라이브) — Phase 3의 "인덱스는 이식할 수 없다"가 파일 경로에도 그대로 적용된다. 이쪽은 코드 결함이 아니라 손대지 않았다. 테스트 2건 추가, 전체 471 passed. 상세는 `PLAN` Phase 10 T10.3.
- **T10.4(인덱싱 진행률 팝업 + 취소) 완료 [2026-08-09, 사용자 요청]**: "인덱싱 중 다른 동작을 못 하게 팝업+취소 버튼"을 요청받았으나, TECH 4.6("메인 UI가 멈추지 않도록")·Phase 4의 WAL 도입 목적과 정면 충돌 — 세 선택지를 제시해 **비모달로 확정**(완전 모달 전환 여지는 코드에 남김). `ui/widgets/indexing_progress_dialog.py` 신설, `exec()` 대신 `show()`. 취소 시 `IndexingThread.stop_event.set()`. 이후 "처리 중인 파일 경로도 보여달라"는 추가 요청으로 `_IndexingBridge.progress` 시그널을 `(done,total)`→`(done,total,path)`로 확장, 긴 경로는 `QFontMetrics.elidedText()`로 가운데 생략+툴팁. **부수 발견(별건)**: 인덱싱 완료 후 창을 닫으면 `closeEvent`가 `IndexingThread`(threading.Thread)에 `QThread` 전용 API(`isRunning`/`wait`)를 불러 `AttributeError`가 100% 재현됐다 — Phase 4·6에서 각각 만든 크래시 수정 이후 "인덱싱 후 닫기" 경로를 실행한 테스트가 한 번도 없어 안 드러났던 것. `is_alive()`/`join()`으로 수정. 테스트 10건 추가, 전체 481 passed. 상세는 `PLAN` Phase 10 T10.4.
- **T10.5(재인덱싱 문서 누적 버그) 완료 [2026-08-09]**: "인덱싱했는데 문서 33개로 나온다"는 사용자 보고로 발견. `index_folder()`가 이전 인덱스를 정리하는 로직이 없어 대상 폴더를 바꿔도 옛 폴더(다른 PC 세션 흔적 포함) 문서가 영구히 쌓였다. "새 폴더로 완전히 교체"(사용자 확정)로 `_prune_stale_documents()` 추가 — 처음엔 "폴더 바깥이면 지운다"(경로 접두사)로 짰다가, **같은 폴더 안에서 파일이 서브폴더로 이동한 경우**를 못 잡는 걸 재검증 중 실제로 겪어(파일 10개인데 문서 17개) 이번 스캔 결과와 직접 대조하는 방식으로 다시 고쳤다. **별건, 더 심각한 것**: `AppState.save()`가 인자 없이 불리면 항상 진짜 `data/app_state.json`에 써서, 테스트가 `state=AppState()`로 인덱싱을 트리거할 때마다 **실제 앱 상태 파일이 pytest 임시 경로로 오염**되고 있었다(Phase 4부터 존재, 이번에 처음 발견) — `AppState`가 `load()`한 경로를 기억하도록 고치고 오염 지점 9곳을 격리했다. 오염된 실제 DB·상태 파일을 정리하고 재인덱싱(10개 문서·522청크). 부수 영향으로 Phase 6 sLM 테스트셋의 `chunk_id`가 무효화됨(권장 사양 PC에서 재생성 필요). 테스트 7건 추가, 전체 488 passed. 상세는 `PLAN` Phase 10 T10.5.
- **T10.6(파일명 매치 vs 본문 매치) 백로그 등록 [2026-08-09]**: "'김성용'으로 검색되는데 본문엔 없다"는 사용자 지적으로 발견. `chunks_fts`가 `file_name`도 함께 색인하고(`_BM25_WEIGHTS`로 본문의 2배 가중치) FTS5가 컬럼이 아니라 행(청크) 단위로 매치해, 파일명이 걸리면 그 파일의 모든 청크가 결과에 낀다(실측: 청크 22개 전부, 본문엔 0건). 파일명 검색 자체는 의도된 설계라 없앨 문제는 아니고 "기준을 세워야겠다"며 백로그로 보류 — 착수 시 재검토. 상세는 `PLAN` Phase 10 T10.6.
- **T10.7(인덱싱 팝업 경과/남은 시간) 백로그 등록 [2026-08-09, 사용자 요청]**: T10.4 진행률 팝업에 경과·예상 남은 시간을 추가해달라는 요청. 구버전 포맷 변환이 순정 파싱보다 약 250배 느려(Phase 1 실측) 단순 평균 속도 기반 추정은 부정확할 수 있어 표시 방식 결정이 착수 시 필요 — 지금은 구현 없이 백로그만. 상세는 `PLAN` Phase 10 T10.7.
- **T10.8(검색어 전체 포함 결과 상단 노출) 완료 [2026-08-11, 사용자 요청]**: 재순위가 벡터 유사도만 보고 정렬해 **입력한 단어를 전부 담은 청크가 한 단어만 담은 청크에 밀리고 있었다**(실측: `rpm 패키지 삭제 옵션`에서 4/4가 1/4에 밀려 2위). **FTS5 후보 수집은 정상**이었고(전체 일치 청크가 BM25 상위에 이미 들어옴) 문제는 그다음 `_rerank()`라, 정렬 키만 **(관련성 낮음 여부, 일치 개수 ↓, 유사도 ↓)**로 바꿨다[사용자 확정] — 일치 개수를 유사도보다 우선하되 흐림 카드가 1위에 오지 않도록 "관련성 낮음"을 더 앞선 키로 뒀다(DESIGN §5.6 충돌 방지). 🔴 **일치 개수는 본문+캡션으로만 세고 파일명은 뺀다** — 파일명까지 세면 같은 파일 청크가 전부 동점이 돼 순위가 하나도 안 바뀐다(실측: "코치 윤리규정 준수" 상위 10건 전부 3/3). 조사 처리는 FTS5와 같은 함수(`query_term_variants()`)를 재사용 — 따로 구현하면 "전체 일치인데 상단에 안 온다"가 된다. T10.6(파일명 매치)은 순위 면에서만 완화됐을 뿐 여전히 열려 있다. 테스트 9건 추가(누적 585 passed). 상세는 `TASK` T10.8.
- **T10.9(권장 모드에서 AI 요약이 항상 실패하는 버그) 완료 [2026-08-11, 사용자 보고]**: 🔴 `ui/search_worker.py`가 `hybrid_search()`에 `embedder=`만 넘기고 **`profile=`은 안 넘겨서**, 권장 모드(HEAVY)에서도 벡터 조회는 내부 기본값(경량)으로 이뤄지고 있었다 — 벡터는 있는데 차원(768 vs 1024)이 안 맞는 것으로 처리돼 **모든 결과의 유사도가 조용히 None**이 되고, AI 요약 1단계가 이를 "관련성 판단 불가"로 보고 전부 차단해 "관련 문서를 찾을 수 없습니다"만 나왔다. `profile=self._embedder.profile`을 같이 넘기도록 수정. **Phase 7.5 전까지는 권장 모드가 실제로 설치될 수 없어 이 경로가 한 번도 실사용된 적이 없었다** — `SearchWorker` 단독 테스트가 그동안 아예 없었고 `MainWindow` 통합 테스트는 전부 경량(기본값)으로만 돌아 가려져 있었다. 신설한 회귀 테스트가 수정 전에는 실제로 실패하는 것까지 확인(되돌렸다 복원). 부수 발견: 실제 인덱스에 권장 모델 벡터가 아예 없던 것도 같이 드러나 재인덱싱으로 채웠다(PC 성능 전환은 자동 재인덱싱을 안 한다는 Phase 7.5 기존 발견과 동일 원인). 테스트 3건 추가(누적 588 passed). 실제 앱에서 권장 모드로 검색→AI 요약까지 실제 답변이 나오는 것 종단 확인. 상세는 `TASK` T10.9.
- **T10.10(표·이미지 카드 관련성 낮음 흐림 누락) 완료 [2026-08-11, 사용자 보고]**: DESIGN §5.6의 흐림 처리가 Phase 4에서 텍스트 카드에만 구현됐고, Phase 5에서 표·이미지 카드를 만들 때 `card_common.py`가 "관련성 낮음" **라벨**은 세 카드 공통으로 붙였지만 실제로 흐려 보이게 하는 `QGraphicsOpacityEffect`는 옮겨 붙이는 걸 빠뜨렸다 — 라벨만 뜨고 안 흐려지는 카드가 나온 이유. `apply_low_relevance_style()`로 공유 함수화해 세 카드가 전부 쓰도록 고쳤다. 표·이미지 카드에는 이 흐림을 검증하는 테스트가 그동안 하나도 없었다 — 회귀 테스트 4건 추가(수정 전으로 되돌려 실패 확인 후 복원). 실제 앱에서 표·이미지 카드가 `relevance=low`·흐림 적용 상태로 뜨는 것 확인. 테스트 누적 592 passed. 상세는 `TASK` T10.10.
- **최소 사양 실측 완료 [2026-08-08]**: Phase 9로 넘겼던 과제를 닫았다. 실문서 10건을 이 기준기에서 새로 인덱싱해(청크 508개) 측정 — **검색 지연은 체감 차이 없고**(키워드 0.7~0.8ms, 하이브리드 11.5~26.6ms) **임베딩 처리량만 4.8배 느리다**(8 vs 38 청크/초). 인덱싱은 백그라운드라 최소 사양에서 추출형 검색은 문제없이 쓸 수 있다. 측정 중 **`_memory_mb()`가 Phase 3부터 계속 실패하고 있던 것**을 발견해 고쳤다(ctypes가 `GetCurrentProcess()` 의사 핸들을 32비트로 잘라 넘겨 조용히 실패 — T3.8의 메모리 항목이 그동안 측정된 적이 없었다). 벡터 재순위는 자연어 질의 4건에서 **2건 개선 · 1건 동일 · 1건 악화**로 일관되게 좋지는 않다. 이 기준기에는 LibreOffice가 없어 `.doc`·`.xls`가 **오류 없이 0청크로 조용히 누락**된다(Phase 9 번들 전까지 사용자에게 보이지 않음). 상세는 `PLAN` Phase 3 절.
- **🔴 인덱스는 이식할 수 없다 [Phase 3 추가 조사, 2026-08-08]**: 최소 사양 기준기의 배치 편차 실패를 파다가 더 큰 문제를 찾았다. 편차를 분해하니 **PC 간 차이(중앙 0.707 / 최소 0.556)가 배치 효과(0.940)보다 훨씬 컸다.** 다른 PC에서 만든 인덱스로 검색하면 top10 겹침이 4~8/10으로 무너지지만, **같은 PC 안에서는 top1 전부 동일 · top10 겹침 8~10/10**으로 순위가 유지된다. → **인덱스는 그 인덱스를 쓸 PC에서 생성해야 한다.** 실사용(각 PC에서 인덱싱)에는 문제가 없으나, **개발 중 인덱스를 PC 간 복사하면 검색 품질 측정이 왜곡된다.** 배치 편차의 원인은 패딩이 아니라 int8 동적 양자화의 배치 단위 활성값 스케일이며(패딩 0인 문장도 갈리고, 패딩을 늘리면 오히려 편차가 줄었다), 크기는 CPU 의존적이다(이 기준기는 AVX-512/VNNI 없음). 부수 결과로 **최소 사양에서는 `batch_size=1`이 더 빠르다**(8.9 vs 7.0 청크/초) — 편차도 사라지지만 기본값 변경은 권장 사양 PC 재측정 후 결정. 상세는 `PLAN` Phase 3 절.
- **Phase 7.6 (AI 요약을 AI 챗봇으로 교체, 2단 응답 구조) — 완료 (DoD 충족) [2026-08-11]**: Phase 7의 "AI 요약 보기"(검색마다 자동 1회 요약)를 실사용해보니 필요성이 불분명해, 검색 결과를 근거로 후속 질문을 이어가는 챗봇으로 바꿨다. 설계 중 **속도 실측이 발목을 잡았다** — LLM 생성 답변은 11~79초가 걸려 "은행 앱 챗봇처럼 빠르게"라는 요구와 정면 충돌했고, 두 차례 설계안(문서별 팝업 챗봇 → 자동 전체 생성 챗봇)이 이 이유로 반려됐다. **최종 채택: 응답 2단 분리**[사용자가 목업 2장으로 직접 제시, 확정] — ① 메시지를 보내면 `hybrid_search()`의 top-1 발췌를 LLM 없이 즉시 원문 그대로 보여주고(검색 지연 7~14ms), ② 그 아래 "AI 요약 보기"를 **눌렀을 때만** LLM이 돈다. 메시지마다 독립 처리(stateless) — 매번 전체 문서 범위로 새로 검색한다. 새 백엔드 코드가 거의 없다 — **`SearchWorker`·`SummaryWorker`·`SummaryCard`·4단계 안전장치를 전부 무수정으로 재사용**했고, 새 프롬프트(`build_chat_messages`)는 계획에서 뺐다(②가 원문을 이미 보여준 뒤라 기존 3문장 요약 프롬프트가 그대로 맞는 역할). 실제 인덱스(문서 19건)로 "코치인증자격시험 응시방법 찾아줘" → 즉시 발췌 → "AI 요약 보기" → 근거 딸린 정답 생성까지 스크린샷으로 종단 확인. 테스트 11건 추가(누적 603 passed / 5 skipped). 상세는 `TASK`/`PLAN` Phase 7.6.
- **Phase 7.6에서 잡은 함정**: ① 옛 `ResultList.summary_card()`류 API를 챗봇 패널과 나란히 남겨두려다, 자동 요약 카드가 **대체**되는 것이지 **추가**되는 게 아니라는 걸 재확인하고 통째로 제거 — 남겨뒀으면 죽은 코드가 됐을 것 ② `_AnswerBubble`이 요약 상태 분기(정상/기권/근거없음/실패 + "확인 필요" 배지)를 처음엔 새로 구현하려다, `SummaryCard`를 그대로 끼워 넣는 쪽으로 방향을 바꿔 Phase 7에서 이미 검증된 로직을 재사용했다 ③ 챗봇의 "AI 요약 보기" 클릭이 그 턴의 질문 원문을 알아야 하는데 `ChatPanel.summarize_requested` 신호는 `(request_id, results)`만 실어 보낸다 — 신호 자체를 바꾸는 대신 `MainWindow`가 `message_sent` 시점에 `request_id → question` 매핑을 따로 들고 있는 쪽으로 해결(패널 내부 구조를 안 건드림) ④ 표·이미지 청크가 top-1일 때 렌더링이 다듬어지지 않은 원시 텍스트로 나온다는 걸 실제 질의로 확인 — 크래시는 없지만 가독성은 이번 범위 밖으로 남겨뒀다.
- **Phase 8 (증분 인덱싱 / 폴더 감시) — 완료 (DoD 충족) [2026-08-13]**: 재인덱싱이 매번 대상 폴더 전체를 처음부터 다시 파싱하던 것을, "바뀐 파일만 다시 파싱"하도록 바꿨다. Phase 1부터 이미 깔려 있던 `documents`/`chunks`의 `source_mtime`/`source_hash` 컬럼과 `parser/utils/hashing.py`를 그대로 재사용해 스키마 작업 없이 착수했다(코드 docstring이 이미 "Phase 8에서 재사용"이라고 못박아뒀다). `indexer/incremental/needs_reindex()`가 mtime을 먼저 보고 다를 때만 SHA-256 해시로 확인하는 2단계 비교로 판별하고, `index_folder()`가 변경 없는 파일은 파싱·저장을 건너뛴다(`report.skipped`). 실사용 검증(`../exdoc` 19개 문서, CLI): 전체 최초 인덱싱 117초(구버전 doc/xls LibreOffice 변환 포함) → 무변경 재실행 0.87초(19/19 스킵) → 1개 파일만 수정 후 1.0초(18/19 스킵). **T8.5(watchdog 실시간 폴더 감시)**도 핵심 완료 직후 이어서 구현했다 — 재인덱싱 자체가 이미 값싸져 있어 "파일 하나만 재파싱"하는 새 경로 없이, 변경 감지 시 기존 `_start_reindex()`를 무음 모드(`silent=True`, 진행률 팝업 없이 상태바로만)로 재사용했다. 토글은 사이드바(Phase 7.7에서 막 확정돼 건드리지 않음) 대신 폴더 관리 다이얼로그에 두었고 기본 OFF. 테스트 49건 추가(누적 675 passed / 5 skipped). 상세는 `PLAN` §8-B·§8-C.
- **Phase 8에서 잡은 함정**: ① `chunk_id`가 `doc_id+type+ordinal` 기반이라 이미지 내용이 바뀌어도 위치(ordinal)가 같으면 chunk_id가 그대로다 — 즉 재파싱만으로는 이미지 썸네일 캐시가 자연 무효화되지 않는 잠재 버그가 있었다(Phase 5 당시 "재인덱싱하면 chunk_id가 바뀌어 자연 무효화된다"고 적어둔 전제가 부분적으로만 맞았다). `store_document()`가 교체 직전 옛 이미지 청크 id를 반환하도록 하고 `IndexReport.stale_image_chunk_ids`를 거쳐 `MainWindow`가 `evict_thumbnails()`를 호출하도록 연결했다(T8.4) — 이 버그는 실사용에서 아직 보고된 적 없지만 설계 중 코드를 들여다보다 발견했다 ② 실사용 검증 중 `cp` 백업 명령이 잘못된 경로(`exdoc/README.txt`, 실제로는 `exdoc/sub1/README.txt`)를 대상으로 실패했는데, 뒤이은 `echo ... >> "잘못된 경로"`가 **존재하지 않던 파일을 실제 사용자 문서 폴더에 새로 만들어버렸다**(리다이렉트는 대상이 없으면 만든다) — 바로 발견해 사용자 확인 후 삭제했고, 이후 검증은 `/tmp` 사본에서 진행하도록 바꿨다. 코드 버그는 아니지만 실제 폴더를 대상으로 한 셸 검증은 명령 하나하나가 흔적을 남길 수 있다는 걸 재확인했다 ③ T8.5 테스트(`MainWindow`를 짧은 시간에 여러 개 연달아 생성) 작성 중, 이 PC에서 수정 전 `main`에도 이미 있던 ONNX 워밍업 스레드 크래시(access violation, `TestPerformanceComboStartupSync`에서 처음 확인)를 훨씬 자주 재현하는 조건을 발견했다 — `MainWindow._start_embedder_warmup`을 해당 테스트 클래스에서만 no-op 처리해 우회했다(근본적인 ONNX 스레드 안전성 문제 자체는 이번 범위 밖).
- **Phase 7.7 (UI 전면 재구성, 목업 컨셉 적용) — 완료 (DoD 충족) [2026-08-13]**: 사용자가 HTML 목업 2장(`rag_ui_concept_chatbot.html`/`rag_ui_concept_searchresult.html`)을 제시하며 "이대로 적용 가능한지" 물었고, 검토 결과 **기능은 이미 100% 구현돼 있고 입력창 위치만 다르다**는 게 확인됐다 — 기존 앱은 상단 고정 검색바(debounce 자동검색)와 챗봇 모드 전용 하단 입력창이 공존해 입력 지점이 두 곳이었는데, 목업 두 화면 모두 **하단 입력창 하나**로 통일돼 있었다. **"컨셉대로 전면 재구성"으로 확정**[사용자 선택] — 사이드바가 창 왼쪽 세로 전체를 차지하도록 바꾸고, 하단에 최근 검색·모델/폴더 관리 버튼을 새로 두고, 상단 검색바를 없애 검색·챗봇 두 모드가 공용 입력창(`InputBar`) 하나를 쓰도록 했다. **강조색은 목업의 `#1e90ff` 대신 기존 `#2563EB` 유지, 슬라이드 애니메이션은 이번 범위 제외**[사용자 확정] — 색상은 QSS 전반(검색바 포커스·버튼·요약 카드 테두리)이 공유하는 토큰이라 바꾸면 전 화면을 다시 시각 검증해야 하고, Qt는 QSS `transition`을 지원하지 않아 `QPropertyAnimation`을 새로 붙여야 해 이번 범위에서 뺐다. **핵심 설계 결정: 입력창은 `MainWindow`가 소유하고 두 모드가 공유한다**(챗봇 패널이 자체 입력창을 갖지 않음) — `ChatPanel.send_message(text)`가 이미 "외부에서 대신 보내는" 공개 API로 존재해(Phase 7.6에서 자동 전송용으로 만든 것), 입력창을 떼어내도 `tests/test_ui_chat.py`가 **한 줄도 안 바뀌었다**. 말풍선 좌/우 정렬·최대 폭 65%는 Qt QSS가 `max-width`·`margin-left:auto`를 지원하지 않아 `QWidget`+`QHBoxLayout` 래퍼와 `setMaximumWidth()`로 직접 구현했다. 테스트 다수 추가·정리(누적 626 passed / 5 skipped). 실제 인덱스(문서 19건)로 초기 화면→검색→헤더 ✕→챗봇 전환→말풍선 정렬→리사이즈→모델/폴더 관리 다이얼로그까지 스크린샷으로 종단 확인. 상세는 `TASK`/`DESIGN` §2~4·§5.8 Phase 7.7 절.
- **Phase 7.7에서 잡은 함정**: ① 말풍선 최대 폭을 생성 시점에 `_transcript.width()`로 계산하면 창에 아직 부착되지 않아 0이다 — Phase 4·5·7에서 반복된 "화면 부착 전/후" QSS·기하 타이밍 함정과 같은 종류. 그대로 두면 첫 프레임에 말풍선이 폭 전체로 그려졌다가 다음 이벤트 루프에 확 줄어드는 게 보인다 — 자신의 폭, 그마저 없으면 고정 임시값(640)으로 미리 상한을 걸고 `resizeEvent()`가 실제 크기로 바로잡게 했다 ② 헤더 ✕가 `_last_query`를 안 비우면 두 가지 회귀가 난다 — 옵션 토글이 지운 검색을 되살리거나(`_on_filters_changed`), 이후 챗봇 모드 진입 시 지운 질문이 유령 첫 메시지로 자동 전송된다(`_activate_chat_mode`의 기존 자동 전송 로직과 충돌) — 둘 다 회귀 테스트로 재현·확인 후 고쳤다 ③ 사이드바 하단에 "모델 관리" 버튼을 그대로 추가하면 `PerformanceCombo`가 이미 갖고 있던 같은 기능의 링크 버튼과 중복된다는 걸 구현 중 발견 — 콤보의 링크를 제거하고 사이드바 버튼으로 일원화 ④ 시각 검증 스크립트를 `QTimer.singleShot` 체인으로 짰다가 실제 앱 기동 지연(임베더 워밍업 등) 때문에 `processEvents()`가 다른 대기 중인 타이머를 먼저 처리해 스크린샷 순서가 꼬였다(콘솔 로그로 실제로 확인: 2000ms 타이머가 300ms 타이머보다 먼저 실행됨) — qtbot.waitUntil과 같은 폴링 방식으로 바꿔 해결. 같은 스크립트에서 실제 `data/app_state.json`을 `AppState.load()`로 그대로 읽어 썼더니 이미 `ai_chat_enabled: true`(과거 Phase 7.6 검증 세션의 흔적)가 저장돼 있어 스크립트가 검색 모드를 가정한 지점에서 챗봇 모드로 진입, 위젯이 `deleteLater()`로 예약 삭제된 뒤 접근돼 크래시가 났다 — 실제 버그가 아니라 테스트 스크립트가 실제 상태를 격리하지 않은 문제(T10.5와 같은 종류의 함정)였고, 이후 스크립트는 `state._path`만 격리 경로로 바꿔 재사용했다.
- **T10.6(파일명 매치가 본문 매치처럼 보이는 문제) 완료 [2026-08-13]**: FTS5가 컬럼이 아니라 행 단위로 매치해 파일명에만 있는 단어를 검색해도 그 파일의 모든 청크가 결과에 끼던 문제(백로그 등록 시점 2026-08-09)를 해결했다. **새 계산 로직 없이 T10.8이 이미 만들어둔 신호를 재사용**했다 — 재순위용으로 계산해두던 "본문+캡션에 실제로 포함된 검색어 수"(`matched_terms`, 파일명은 의도적으로 제외하고 셈)가 0이면 정확히 "파일명에만 걸린 결과"다. `HybridResult.is_filename_only_match` property 하나만 추가하고, `card_common.build_card_header()`(세 카드 타입 공유)에 "파일명 매치" 배지를 붙였다 — DESIGN §5.6("관련성 낮음")과 같은 자리·스타일(회색 라벨, 흐림 없음, 독립적으로 동시 노출 가능)로 신설(DESIGN §5.6b `[제안]`). 백로그가 제시한 3방향(표시/묶기/가중치 재검토) 중 표시만 채택 — 나머지 둘은 범위·위험이 커서 보류. 실사용 검증(실제 인덱스, "김성용" 검색): 원래 버그 리포트와 동일하게 한 파일의 청크 20개 전부에 배지가 정확히 붙는 것을 위젯 조회로 확인. 테스트 8건 추가(누적 683 passed / 5 skipped). 상세는 `PLAN` Phase 10 절, `DESIGN` §5.6b.
- **T10.7(인덱싱 진행률 팝업 경과/남은 시간 표시) 완료 [2026-08-13, 사용자 요청]**: `IndexingProgressDialog`에 경과 시간과 예상 남은 시간을 추가했다. 남은 시간은 전체 평균이 아니라 **최근 5개 표본의 이동평균**으로 추정한다 — 구버전 포맷 변환이 순정 파싱보다 약 250배 느려서(Phase 1 실측), 전체 평균이면 그 구간이 지나간 뒤에도 계속 느리게 잡힌다. 표본 2개 모이기 전엔 남은 시간을 아예 안 보여준다(억지 추정 방지). **범위가 착수 전 메모의 예상보다 작아졌다** — 다이얼로그가 이미 파일마다 `set_progress(done,total,path)`를 받고 있어, `_IndexingBridge`/`IndexingThread`/`main_window.py`를 전혀 안 건드리고 다이얼로그 내부에서만 시각을 쟀다. `time_source` 매개변수로 시계를 주입 가능하게 만들어(Phase 8과 같은 이유로 시간 대신 결정론적 산술 검증) 가짜 시계로 "이동평균이 최근 구간에 실제로 반응하는지"까지 테스트했다. 실사용 검증(실제 exdoc 사본, 19개 문서): 경과 시간 단조 증가, 느린 doc/xls 구간에서 남은 시간 추정치가 커졌다가 빠른 구간에서 줄어드는 것을 로그로 확인, 완료 시 "남음" 문구 정상 소멸. 테스트 8건 추가(누적 691 passed / 5 skipped). 상세는 `PLAN` Phase 10 절.
- **T10.11(일치 개수 같으면 본문 긴 결과 우선) 완료 [2026-08-14, 사용자 요청]**: 실제 검색 화면을 보다가 "내용이 있는 항목을 최상단에" 요청. T10.8이 확정한 정렬 키(관련성 낮음 → 일치 개수 ↓ → 유사도 ↓)에 **본문 길이 ↓**를 일치 개수와 유사도 사이에 추가했다[사용자 확정 — 유사도보다 본문 길이 우선]. `_rerank()`의 벡터 있음/없음 두 갈래와 임베딩 불가 시 폴백 정렬까지 세 곳 모두 맞췄다 — 새 계산 없이 기존 `HybridResult.content` 길이만 쓴다. 테스트 723 passed. 상세는 `TASK` T10.11.
- **Phase 11-A(UI 3페이지 셸 재구성) 완료 [2026-08-18]**: 사용자가 다른 프로젝트(`l3_2_codex/offline_document_search`) 화면 3장을 제시해 적용 가능성을 물었고, 대조 결과 **본문(검색/대화)은 이미 동일하고 사이드바 구조만 달랐다.** 🔴 그대로 옮기면 이 프로젝트에만 있는 기능 5개(형식 필터·대소문자·일치단어·PC 성능·최근 검색)가 갈 곳을 잃는 게 문제였다 — 배치를 사용자가 항목별로 확정했다(최근 검색 위치 유지 / PC 성능·모델 관리는 설정 / 폴더 관리는 문서 관리의 "폴더 선택"으로 흡수 / 문서 형식·AI 챗봇은 확장 영역 / 대소문자·일치단어는 **UI만 제거하고 기능 유지** / 네트워크 항목 제거 — 이 프로젝트엔 그런 검사가 실제로 없어 문구만 남을 뻔했다). 11-A는 **기존 기능 이동만** 했다: `Sidebar` 전면 재작성(다크 네이비 네비 + 확장 영역), `QStackedWidget` 3페이지, `DocumentPage`/`SettingsPage` 뼈대 신설, 본문 배경 `#F3F6FA`. **확장 버튼은 네비게이션과 독립**이다(항목 본체는 페이지 이동, 버튼은 펼침만) — 묶으면 "필터 보려다 페이지가 바뀐다"가 된다. 확장 영역을 흰 카드로 둔 건 `StyledCheckbox`·`ToggleSwitch`가 밝은 배경 기준으로 직접 페인팅하는 위젯이라(Phase 4) 네이비 위에선 안 보이기 때문. 대소문자·일치단어는 `hybrid_search()` 인자와 FTS5 매칭 경로를 그대로 두고 `AppState`에서 읽게 했다 — **UI 테스트 2건을 삭제하지 않고 `AppState` 기반으로 전환**해 "기능 유지" 약속이 앱 경로로 계속 검증된다(기능 회귀는 `tests/test_indexer_search.py` 3건이 UI 없이 지킨다). 테스트 10건 + 리사이즈 회귀 3건 추가, 전체 843 passed / 5 skipped. `QWidget.grab()`으로 4화면 시각 확인. **11-A 중 기존 결함 1건도 잡았다** — "리사이즈할 때마다 팝업이 쏟아진다"는 보고로, `RecentSearches`가 ① 리사이즈마다 목록을 통째로 다시 그리고 ② 🔴 항목 버튼을 **부모 없이** 만들고 있었다(부모 없는 QWidget은 Qt에서 최상위 창이라 순간적으로 별도 창처럼 보인다). 개수가 실제로 달라질 때만 다시 그리도록 하고 생성 시점에 부모를 줬다 — 실측 240회 리사이즈에서 `_render()` 호출 240→0회. Phase 7.7부터 있던 결함이고 11-A에서 높이 계산식이 바뀌며 드러났다. 설계는 `DESIGN` §14, 계획·결과는 `PLAN` Phase 11. **11-A 마감 다듬기 [2026-08-18~19]**: 확장 영역을 사이드바와 같은 다크 네이비로 바꾸고(`StyledCheckbox`를 다크용으로 재도색 — 꺼짐은 투명+밝은 테두리, 켜짐은 강조색 채움+흰 체크마크, 글자 12px·박스 16px. `ToggleSwitch` 라벨은 **확장 영역 안쪽으로만 QSS 범위를 한정** — 같은 위젯을 폴더 관리 다이얼로그가 밝은 배경에서 쓴다), 최근 검색을 하단 고정 + 10건 자리 확보로 바꿨다. 🔴 이 과정에서 **`setFixedHeight`가 창 전체의 최소 높이를 706px로 묶는 함정**을 찾았다 — Qt에서 `setFixedHeight`는 최소·최대를 동시에 묶어서, 최근 검색 270px가 사이드바를 거쳐 창의 최소 높이가 됐고, 창이 안 줄어드니 `resizeEvent`도 안 와서 재계산이 영영 돌지 않는 고리에 갇혔다. 간격 축소(16→10) + 압축 가능하게(최소 0 + 최대 예약높이, `sizeHint()`로 선호 크기 제공, 안쪽 `SetNoConstraint`) + 가용 공간을 `설정` 버튼 실제 좌표 기준으로 계산 + `MainWindow.resizeEvent`에서 직접 갱신, 네 가지로 해소. 실측: 접힘·펼침 같은 위치 유지, 창 759~455px 어디서도 창 밖으로 안 나감, 창 최소 높이 706→672px. 부수로 T10.28 때 만든 테스트 경합(3회 중 1회 실패)도 `waitUntil`로 수정. 전체 844 passed / 5 skipped. **후속**: 확장 영역이 문서 관리·설정 페이지에서도 계속 보인다(배치 판단 필요 — 11-B에서 해소), 11-B(문서 관리 지표)·11-C(설정 적용 배선) 미착수.
- **Phase 11-B(문서 관리 페이지) 완료 [2026-08-19]**: 11-A가 뼈대만 두고 간 자리에 카드 5장(헤더·선택 폴더·인덱스 작업·INDEX OVERVIEW·FILE DIAGNOSTICS)과 그 값을 만들 파이프라인 지표를 채웠다. 착수 전 2건 확정 — `재시도`는 **실패 파일만 강제 재파싱**[사용자 확정], 확장 영역은 **검색/대화에서만 표시**[사용자 확정, 11-A 후속 해소]. 🔴 **착수 조사에서 기존 결함을 찾았고 그게 `재시도`의 의미를 정했다** — `parser/base.py`가 파싱 **시작 시점에** mtime·해시를 채우고 `store_document()`가 `status=FAILED` 문서에도 그대로 저장해서, LibreOffice 없이 실패한 `.doc`은 다음 실행에서 `UNCHANGED`로 판정돼 **영원히 건너뛰어진다**(나중에 설치해도 안 고쳐진다). Phase 8(증분 스킵)과 T10.2(FAILED 저장)가 각각은 옳은데 만나서 생긴 구멍이다. **`needs_reindex` 쪽은 안 고쳤다** — 상태를 보고 되살리면 Phase 8이 측정한 "무변경 재실행 0.87초" 전제가 바뀐다. 대신 `reindex_files()`가 증분 판정을 무시하고 실패 파일만 다시 파싱한다. 🔴 **`reindex_files()`는 `_prune_stale_documents()`를 부르지 않는다** — 대상이 폴더 일부라 그대로 불렀다면 나머지 문서가 전부 "이번 스캔에 없음"에 걸려 인덱스가 통째로 지워진다(T10.5의 정반대 사고). 조건문이 아니라 폴더 정리를 `index_folder()` 쪽에만 남기는 **구조**로 막았다. 지표는 `needs_reindex()`(bool) → `classify_file()`(`FileChange` 열거형)로 넓히고 기존 함수를 얇은 래퍼로 남겨 Phase 8 호출부가 한 줄도 안 바뀌게 했고, `IndexReport`에 `scanned`/`created`/`updated`를 더했다(신규/변경은 **파싱 전** 판정을 쓴다 — 저장 후엔 전부 "이미 있는 문서"가 돼 구분 불가). **`총`은 DESIGN 초안의 `indexed + skipped` 파생 대신 `scanned`로 바꿨다** — 실패한 파일은 둘 중 어디에도 안 잡혀 파생값이면 진단에는 실패가 떠 있는데 총계에서 사라진다. `on_stage(단계, done, total)` 콜백을 신설해 현재 단계(파싱/임베딩)를 **파일 진행률과 따로** 보낸다 — 임베딩은 청크 단위라 607청크 136초 동안 파일 진행률이 마지막 값에 멈춰 있어, 섞으면 "19/19에서 2분 멈춘 화면"이 된다. `embed_missing()`이 이미 `on_progress`를 받고 있었는데 파이프라인이 안 쓰고 있었다 — 팝업에도 `단계:` 줄을 붙여 기존 구멍이 공짜로 메워졌다. 마지막 실행·실패 목록은 **기동 시점에 DB에서 읽는다**(`failed_document_paths()`) — 안 그러면 앱을 켠 직후 화면이 깨끗해 보이고 재시도 버튼도 꺼져 있어 인덱싱을 한 번 돌리기 전엔 재시도할 방법이 없다. 진행률 팝업은 **문서 관리 페이지가 열려 있으면 안 띄운다**(DESIGN §14.4.3). 테스트 23건 추가(누적 867 passed / 5 skipped), 팝업 규칙 테스트는 수정 전으로 되돌려 실제 실패 확인 후 복원. `QWidget.grab()`으로 6화면 시각 확인. 설계는 `DESIGN` §14.4·§14.10, 상세는 `PLAN` Phase 11.
- **Phase 11-B에서 잡은 함정**: ⓪ **diff 검토에서 11-B가 새로 만든 위험을 찾았다** — `IndexingThread.run()`이 `index_folder()` 예외 시 `on_done`을 아예 안 불렀다. 지금까지 대가는 "안 닫히는 진행률 팝업" 정도였는데, 11-B부터는 문서 관리 페이지가 `인덱싱 중`에 갇히고 `인덱스 업데이트`가 계속 비활성이라 **앱을 껐다 켜야 회복된다**(대상 폴더가 사라지면 `scan_folder()`가 실제로 던진다). 어떤 경우에도 통지하도록 고쳤다 ① **실사용 검증에서 Office 잠금 파일 결함 발견** — 실제 인덱스에 `~$인증자격시험 세부사항_인증_1_28_v3.doc`이 실패로 남아 있었다. Word가 문서를 열어 둔 동안 만드는 파일인데 확장자가 원본과 같아 `is_supported()`를 통과하고 내용은 스텁이라 **항상 실패**한다. 지금까지 조용히 쌓이기만 하다 11-B가 실패를 화면에 보여주기 시작하자 "재시도해도 영원히 실패하는 항목"으로 드러나, `scan_folder()`가 `~$` 접두사를 건너뛰게 했다(숨김 폴더 제외와 같은 자리) ② **진행 바가 흰 카드 위에서 테두리 없는 막대로 보였다** — 지금까지 팝업 안에만 있어 플랫폼 기본 모양으로 충분했고, 캡처를 보고서야 드러났다(`QWidget.grab()` 시각 검증이 또 잡았다) ③ **새 테스트 1건이 전체 실행에서만 실패했다** — 파일을 복사하고 곧바로 덮어쓰면 Windows 시스템 클럭 틱(약 15.6ms) 안에 들어가 mtime이 같은 값으로 찍혀, 내용이 달라도 `UNCHANGED`가 된다(단독 실행은 통과했다). 제품 결함이 아니라 mtime 우선 비교라는 Phase 8의 의도된 성질이라, 테스트가 시계에 기대지 않도록 `os.utime()`으로 밀어 결정론적으로 고쳤다.
- **T10.30(벡터 없는 청크가 확실히 관련성 낮은 결과보다도 아래로 밀리던 문제) 완료 [2026-08-19, 사용자 지적]**: "관련성 낮은 항목은 하위에 모아야 한다"는 지적으로 조사하다 원인이 정반대라는 걸 발견 — `_rerank()`가 벡터 있음/없음 두 그룹을 **따로** 정렬해 이어 붙이고 있어서, 검색어 3개를 전부 담은 청크가 **권장 모드 벡터를 아직 못 받았다는 이유만으로** 흐림 처리된(확실히 관련성 낮은) 다른 문서보다도 아래로 밀려 있었다("AICA 취득 절차" 검색에서 실측). 두 그룹을 **하나의 정렬**로 합쳐 — 벡터 없으면 `is_low_relevance=False`(판단 근거 없으니 불리하게 안 봄)로 두고 같은 키로 섞는다. 🔴 기존 테스트 1건이 옛 동작("벡터 없으면 항상 맨 뒤")을 명시적으로 검증하고 있어 고쳐야 했다(수정 전 실패 확인 후 복원). 상세는 `TASK` T10.30.
- **T10.29(상태바 "벡터 없는 문서" 문구 오류) 완료 [2026-08-19, 사용자 질문]**: "1891개 문서"라는 상태바 경고를 보고 "이 1891개가 무슨 문서냐"는 질문 — 실제 인덱스(문서 72건)로 대조하니 `missing_vector_count()`는 **청크** 단위로 세는 함수(docstring에도 명시)인데 `ui/main_window.py`의 경고 문구만 "문서가 {missing}개"로 잘못 표기하고 있었다. 실측으로 확인(전체 청크 2,498 − 경량 벡터 2,498/권장 벡터 607 = 정확히 1,891) — 문서는 72개뿐이라 "1891개 문서"는 애초에 성립할 수 없는 숫자였다. "청크"로 문구만 수정, 테스트 1건 갱신.
- **T10.31(검색 결과 카드에 절 제목 표시) 완료 [2026-08-19, 사용자 요청]**: "검색 결과에 '1-1. AICA 취득 절차' 같은 타이틀도 나오면 좋겠다"는 요청. 🔴 **처음 제안이 틀렸고 사용자 확정까지 받은 뒤 전제가 깨졌다** — "짧은 제목 줄을 찾자"고 제안했는데 실측하니 **text 청크 972개 중 952개(98%)가 줄바꿈 없는 한 줄**이라(PDF 추출이 페이지 전체를 한 덩어리로 준다) 그대로 만들었으면 대부분 아무것도 안 나왔다. 인라인 정규식도 문서마다 갈렸다(TIMS는 날짜 `2021. 04`를 제목으로 오인). **제목이 텍스트가 아니라 글꼴 크기에 남아 있다는 것을 찾아 다시 확정받았다**[사용자 확정] — AICA 안내서 3쪽 제목 20pt / 본문 14pt. PDF는 페이지 최대 글꼴, docx는 Heading 스타일, pptx는 제목 플레이스홀더. `chunks.heading` 컬럼 신설(🔴 `chunk_vectors`와 달리 테이블을 안 지운다 — `chunks`는 원문이라 복구 불가, `ALTER TABLE`로 컬럼만 추가). 🔴 **제목은 검색 색인에 안 넣는다** — 넣으면 T10.6(파일명 매치)과 똑같이 그 문서의 모든 청크가 결과에 낀다(일부러 넣어 회귀 테스트 실패 확인 후 복원). 오탐 2건도 잡았다 — 목차 페이지가 통째로 붙는 것(→ 최대 글꼴 **첫 줄만**), 본문 문단이 제목으로 둔갑하는 것(→ 40자 초과는 자르지 않고 **버린다**; 정상 제목 최대 28자 vs 오탐 54자로 실측). 카드는 파일명·위치 줄 **바로 아래 별도 줄**, 세 카드 타입이 `build_heading_label()`을 공유(T10.10 함정 회피). 실제 코퍼스 강제 재인덱싱(11-B `reindex_files()` 재사용) 73문서 243.6초 실패 0, heading 615/2589 — 🔴 **낮은 비율은 코드 결함이 아니라 문서에 구조가 없는 것**(pptx 0%인 파일은 제목 플레이스홀더가 0개, 제대로 쓴 파일은 23/23; docx 0%는 Heading 스타일 부재 — 🔴 **이 진단은 절반만 맞았다. T10.32에서 docx 쪽은 코드 결함**(`_is_heading()` 미호출)**으로 밝혀졌다**). AICA 안내서는 129/129. 테스트 11건 추가.
- **Phase 9(exe 패키징) T9.1~T9.6 완료, T9.7~T9.10 배포 시점으로 보류 [2026-08-21]**: LibreOffice 포함 여부를 먼저 실측·확정했다 — TECH 문서가 LibreOffice 포터블을 300~600MB로 추정했는데 **실제로는 1.5GB**였다(사용자 확정으로 그대로 인스톨러에 포함, "다른 PC 문서에 `.doc`가 많다"는 이유). PyInstaller `--onedir` 빌드(`deploy/app.spec`) + models/vendor/font를 exe 옆으로 복사하는 스크립트(`deploy/build.py`) + Inno Setup 인스톨러 스크립트(`deploy/installer.iss`, 관리자 권한 없이 `%LOCALAPPDATA%` 설치)를 만들었다. **인스톨러(설치 마법사)와 포터블(zip)을 각각 구현하지 않았다** — PyInstaller onedir 결과물 자체가 이미 포터블이라(상대 경로 구조 덕분에 폴더째 zip하면 끝) Inno Setup은 그 위에 설치 마법사 UX만 얹는 선택적 한 단계다. 🔴 **착수 조사에서 구조적 결함을 찾았다** — `config/settings.py`를 비롯한 4개 파일이 전부 `Path(__file__).resolve().parents[N]`으로 프로젝트 루트를 계산했는데, PyInstaller onedir 빌드는 이 파일들을 exe와 다른 위치(`_internal/`)에 번들한다. 그대로 뒀다면 얼린 exe에서 `models/`·`vendor/`·`font/`를 `_internal/` 안에서 찾아 지금까지 설계한 "exe와 같은 레벨" 배포 구조가 깨졌을 것 — `sys.frozen`이면 `sys.executable` 위치를 쓰도록 `config.settings._project_root()`에 분기를 추가하고, 나머지 세 파일은 각자 계산 대신 `config.settings.PROJECT_ROOT`를 가져다 쓰게 정리했다. **실제 빌드로 검증**: 얼린 `OfflineRAGSearch.exe`를 직접 실행해 창이 정상 기동(제목 "오프라인 문서 검색")하고 `data/index.sqlite3`가 정확히 exe 옆에 생기는 것까지 확인 — 수정 전이었다면 `_internal/data/`에 조용히 생겨 배포 구조와 어긋났을 것이다. 🔴 **폴더 이름을 `packaging`이 아니라 `deploy`로 지었다** — `packaging`은 pip 생태계의 실제 라이브러리(PyInstaller 자신도 내부적으로 씀) 이름과 겹쳐서, 그대로 썼으면 `python -m packaging.build`가 cwd 우선순위 때문에 그 라이브러리를 가리고 PyInstaller 실행 자체를 깨뜨릴 뻔했다. 실측 크기: PyInstaller 런타임(`_internal/`) 240MB, LibreOffice 포함 전체 배포 폴더 약 2.4GB(경량 모델 107MB + KURE-v1 559MB + LibreOffice 1.5GB + llamacpp 45MB) — TECH 문서의 "최소 인스톨러 800MB~1.2GB" 목표는 LibreOffice 실측치 때문에 더는 성립하지 않는다. T9.7~T9.10(실제 배포·실기 테스트)은 실물 배포 시점으로 미뤘다. 테스트 2건 추가. 전체 967 passed / 5 skipped 유지(수정 후 재확인). 상세는 `TASK` Phase 9.
- **Phase 9 후속 [2026-08-22]: Inno Setup 설치해 실제 인스톨러 컴파일·설치·제거까지 종단 검증**: winget으로 Inno Setup 설치(이미 있었음) 후 `deploy/installer.iss`를 실제로 컴파일 — 압축 전 2.4GB가 **836MB**로 압축(약 65% 절감). 🔴 **컴파일과 설치 양쪽에서 Windows MAX_PATH(260자) 문제를 실제로 겪었다** — 리포 경로가 길어 iscc가 "지정된 경로를 찾을 수 없습니다"로 죽었고(짧은 경로로 옮겨 우회), 더 심각하게는 **`%LOCALAPPDATA%`에 실제로 설치할 때도 에러 메시지 하나 없이 자동 롤백**됐다(로그에 "Installation succeeded"가 안 뜨고 곧장 "Uninstallation process succeeded"만 남아, 처음엔 원인을 로그만 봐선 알 수 없었다 — `LongPathsEnabled=1`인 이 PC에서도 재현돼 OS 설정이 아니라 Inno Setup 자체의 긴 경로 미지원으로 결론). **근본 원인을 찾아 고쳤다**: 문제의 깊은 경로(실측 최대 223자)는 LibreOffice 배포판 구조가 아니라 `vendor/LibreOfficePortable/App/DefaultData/settings/user/`·`Data/`(PortableApps 런처로 직접 실행하면 생기는 사용자 프로필 캐시)였고, 이 앱은 매 변환마다 자체 임시 프로필을 쓰므로(T1.9) 둘 다 코드가 절대 안 읽는다 — `deploy/build.py`가 조립 시 이 두 폴더를 제거하도록 고쳐(최대 경로 223→169자) 재컴파일하니 설치가 실제로 성공했다. 무인 설치(`/VERYSILENT`) → 로그에 "Installation process succeeded" 확인 → 설치된 exe 실행해 창 기동 확인 → 생성된 제거 프로그램으로 무인 삭제 → 설치 폴더·레지스트리 키 깨끗이 삭제, `data/`만 설계대로 남는 것까지 종단 확인. 상세는 `TASK` Phase 9 T9.4~T9.6.
- **T10.44(상태바 우측에 ATEC 모빌리티 CI 워터마크 추가 + 문서 관리·설정 페이지로 확대) 완료 [2026-08-22~23, 사용자 요청]**: 회사 CI를 앱 아이콘으로 바꾸는 안을 먼저 검토했으나, 제공된 로고(`atecmobility_ci.png`, 3481×315 투명 배경)가 가로로 긴 워드마크라 정사각형 아이콘엔 크게 레터박싱된다는 점을 짚었고, 사용자가 **상태바 우측 끝에 원본 비율 그대로 작게 넣는 안**으로 방향을 바꿨다. 로고를 `ui/icons/atecmobility_ci.png`에 두어(`ui/icons/app.ico`·`ui/qss`와 같은 자리 — `deploy/app.spec`의 `datas`에 이미 `ui/icons` 통째로 등록돼 있어 빌드 설정 변경 불필요) `ui/widgets/status_bar.py`가 `QPixmap.scaledToHeight()`로 상태바 한 줄 높이에 맞춰 배치했다. **후속 요청 두 건**: ① "사이즈를 조금 줄여서 문서관리/설정 화면에서도 나오게 해줘" — 두 페이지엔 상태바 자체가 없어(11-A부터 "검색/대화 페이지만 입력창·상태바를 갖는다"로 명시) `build_ci_watermark_row()`로 분리해 `document_page.py`·`settings_page.py`가 공유하게 했다(로고 높이 16→12px). ② "설정만 위치가 달라 다른 곳과 동일한 위치로 수정해" — 문서 관리 페이지는 워터마크를 여백 0인 `outer`에 붙였는데 설정 페이지는 카드 여백(24px)이 있는 `root`에 그대로 붙여 우측 정렬 기준선이 24px 어긋나 있었다. 설정 페이지도 문서 관리와 같은 `outer`(여백 0)+`QScrollArea`+`content`(여백 24) 3단 구조로 맞췄다. 🔴 **이 과정에서 설정 페이지에 스크롤 영역이 아예 없던 것도 함께 발견** — 기본 창 크기(1100×720)에서 옵션+실행 정보 카드가 다 안 들어가 워터마크는 물론 "실행 정보" 카드 아래쪽까지 창 밖으로 잘려 있었다(문서 관리 페이지는 Phase 11-B부터 스크롤 영역이 있어 같은 문제가 없었다). 스크롤 영역을 추가해 문서 관리와 동일한 동작(창이 작으면 스크롤해야 워터마크가 보임)으로 통일했다. PowerShell `PrintWindow`(포커스 없이 창 내용 캡처)·`PostMessage`(포커스 없이 좌표 클릭 전달)로 세 페이지를 실제 창에서 시각 확인. `tests/test_ui_status_bar.py`+`tests/test_ui_main_window.py`(153건) 재실행만으로 확인, 순수 표시 요소라 신규 테스트는 추가하지 않았다.
- **T10.45(경량 모드에서는 AI 챗봇을 기본으로 꺼둔다) 완료 [2026-08-23, 사용자 확정]**: "리눅스 마스터 2급 명령어 종류" 질문에서 권장 모드(Qwen3.5-4B)는 답했는데 경량 모드(EXAONE-4.0-1.2B)만 "문서에서 찾을 수 없습니다"로 기권한 사용자 보고를, 같은 발췌 5건을 두 모델에 그대로 재현해 원인을 확인했다 — 검색 결과는 두 모드가 동일했고 차이는 순수하게 **어느 sLM이 답하느냐**(T10.41: 경량↔EXAONE-4.0-1.2B, 권장↔Qwen3.5-4B)였다. Phase 6 실측에서 이미 EXAONE-4.0-1.2B의 과잉 기권률이 약 50%로 나와 있던 것과 일치 — 여러 페이지에 흩어진 내용을 종합해야 하는 질문일수록 경량 모델은 20초 가까이 기다리고도 기권으로 끝나는 경우가 잦다. 사용자가 "경량 모드일 때는 AI 챗봇 토글을 끄는 것으로 하자"고 정책을 확정했다 — 경량 모드로 전환·기동할 때마다 토글을 꺼둔다(강제로 막지는 않는다). `MainWindow._enforce_light_mode_ai_chat_policy()`를 `_refresh_ai_chat_availability()` 끝에 한 곳만 추가해 기동·모드 전환·모델 관리 팝업 닫기 등 기존 세 호출 지점 전부에 자동 적용했다 — `SearchOptions.set_ai_summary(False)`가 `toggled(False)`를 실제로 내보내는 성질(T10.40에서 이미 확인된 동작)을 그대로 타고 `_on_ai_chat_toggled()`까지 이어지므로 새 로직 없이 기존 신호 경로 재사용만으로 챗봇 패널 해제·상태 저장까지 끝났다. 테스트 3건 추가(수정 전 실패 확인 후 복원). 상세는 `TASK` T10.45.
- **T10.43(sLM 카드 순서 변경 + 확정된 앱 아이콘을 메인 창·모든 팝업에 적용) 완료 [2026-08-22, 사용자 요청]**: ① 모델 관리 팝업의 sLM 카드 순서를 `SLM_OFFERED_ORDER` 상수 하나로 뒤집었다(EXAONE 먼저) — `ModelManagerDialog`가 이미 이 목록을 그대로 순회해 그리는 구조라 새 로직 불필요. ② 확정한 문서+돋보기 아이콘을 `ui/app.py`의 `create_app()`에서 `QApplication.setWindowIcon()` 한 번으로 메인 창·모든 팝업(`QDialog`)에 적용 — Qt가 자기 아이콘 없는 최상위 창에 앱 아이콘을 자동으로 물려준다. 🔴 **아이콘 파일을 `deploy/icon.ico`(빌드 전용, `dist/`에 안 복사됨)에서 `ui/icons/app.ico`로 옮겼다** — 런타임에 `QIcon`으로 읽으려면 얼린 exe 안에도 번들돼야 하는데 `deploy/`는 그 대상이 아니라, 그대로 뒀으면 개발 모드에서만 되고 배포 exe에서는 아이콘이 안 뜰 뻔했다(T9.2와 같은 "빌드 시점 자원 vs 런타임 자원" 구분 실수). `ui/qss/app.qss`와 같은 방식(`__file__` 상대 경로 + `app.spec` datas 등록)으로 통일, `app.spec`·`installer.iss`도 새 경로로 맞췄다. 개발 앱 재기동해 정상 확인, 배포 exe는 다음 재빌드 때 확인 필요.
- **T10.42(PC 성능 "설치됨" 배지를 임베딩+sLM 둘 다 확인하도록 확대, 미설치는 선택 불가) 완료 [2026-08-22, 사용자 요청]**: T10.41로 PC 성능이 sLM도 고르게 됐는데 배지는 여전히 임베딩만 봐서, Qwen 없이 KURE-v1만 있어도 "권장·설치됨"으로 잘못 떴다. **[사용자 확정 기준]**: "설치됨" = 임베딩+대응 sLM 둘 다 있을 때만(경량은 임베딩이 항상 번들이라 사실상 EXAONE 여부로만 갈림). `performance_combo.py`에 `_is_fully_installed()` 신설. **미설치 항목은 `QStandardItem.setEnabled(False)`로 드롭다운에서 아예 회색 처리해 선택 자체를 막는다**[사용자 확정, 기존엔 클릭→모델 관리 팝업→선택 되돌림이었음, 그 경로는 방어적으로 남김]. 배지 문구도 "준비 중"→"설치 안 됨". 🔴 테스트 중 `LIGHT`·`HEAVY`가 같은 `ModelProfile` 클래스라 `type(HEAVY)` 패치가 `LIGHT`에도 번지는 기존 함정을 다시 확인(HEAVY 전용 검증으로 좁혀 해결). 테스트 4건 추가 + 2건 갱신(수정 전 실패 확인 후 복원), 관련 파일만 실행(28개 통과).
- **T10.41(PC 성능 선택이 sLM도 함께 정하도록 배선) 완료 [2026-08-22, 사용자 제안]**: T10.37 조사 중 미뤄뒀던 결함 마무리 — `AppState.slm_profile`이 항상 `DEFAULT_SLM_PROFILE`(Qwen3.5-4B)로 고정돼 아무도 안 바꾸던 것을 고쳤다. **[사용자 확정]**: 새 UI 없이 기존 PC 성능 선택(경량/권장)에 얹는다 — 경량↔EXAONE-4.0-1.2B(최소 사양), 권장↔Qwen3.5-4B(권장 사양), T6.8 실측 결론 그대로. `config/settings.py`에 `slm_for_model_profile()` 신설, `MainWindow.__init__`이 기동 시 이 매핑으로 `state.slm_profile`을 재계산하고, `_on_profile_activated()`가 모드 전환마다 같은 매핑 + `SlmService.set_profile()`을 호출. 🔴 **`set_profile()`은 Phase 7.5부터 있었는데 실사용 경로에서 부른 적이 한 번도 없었다** — 테스트 1건에서만 쓰이던 죽은 배선(T10.37·T10.39와 같은 "절반만 끝난 기능" 패턴). 새 로직 없이 기존 두 조각(매핑 결론 + 전환 함수)을 잇기만 했다. 테스트 4건 추가(수정 전 `ImportError`로 실패 확인 후 복원), 관련 파일만 실행(T10.40에서 정한 방침 유지).
- **T10.40(AI 챗봇 토글 회색 비활성화 → "누르면 안내 팝업"으로 변경) 완료 [2026-08-22, 사용자가 배포 exe 스크린샷 2장으로 지적]**: 새로 설치한 exe에서 sLM이 없는 게 당연한데 토글이 회색으로 죽어 있어 "고장난 것처럼 보인다"는 지적. **[사용자 확정]**: 토글은 항상 클릭 가능하게 두고, 켜려는 시도를 가로채 모델이 없으면 되돌리고 "설정 → 모델 관리에서 다운로드하라"는 팝업만 띄운다. `SearchOptions.set_ai_summary_available()`에서 `setEnabled()` 호출을 없애고 `_available` 플래그로만 추적, `ToggleSwitch.toggled`를 가로채는 `_on_toggled()`가 `checked=True and not available`일 때만 되돌리고 팝업(꺼질 때는 그대로 통과). 🔴 되돌리는 `setChecked(False)` 자체가 `toggled(False)`를 다시 내보내는 재귀 호출이라 "신호가 안 나간다"고 잘못 기대했던 테스트를 바로잡았다(최종 상태를 한 번 더 알리는 건 정상 동작). 테스트 3건 수정 + 1건 추가. 전체 982 passed / 5 skipped. **후속 [같은 날]**: 그 안내 팝업이 `QMessageBox.information()`이라 OS 네이티브 스타일로 떠서 폴더 관리 등 카드형으로 재도색된 다른 팝업과 안 맞는다는 지적 — `ui/widgets/info_dialog.py`(`show_info()`) 신설, `FolderDialog`와 같은 토큰(흰 배경·`PrimaryButton`)으로 재사용 가능한 안내 팝업으로 교체. 테스트 1건 추가(관련 파일만 실행, 전체 스위트는 매번 안 돌리기로 함[사용자 확정] — LibreOffice·ONNX가 섞여 13~14분 걸려 작은 UI 수정엔 과함).
- **T10.39(사이드바 검색 옵션을 열 방법이 없던 문제 — 1차 오진단을 사용자가 정정) 완료 [2026-08-22]**: "검색/대화를 눌러도 하위 메뉴가 안 나온다"는 보고. 🔴 **1차 수정이 틀렸다** — DESIGN §14.2.2의 옛 `[확정]` 문구("확장 버튼이 네비게이션과 독립")를 그대로 믿고 2026-08-20 커밋(`3fa4e4f`)이 지운 별도 확장 버튼을 되살렸는데(테스트까지 통과, 배포 exe 재빌드까지 완료), **사용자가 "확장 버튼을 없애는 것으로 이미 결정했었다"고 정정** — 그 커밋의 진짜 의도는 별도 버튼 대신 `검색/대화` 클릭 시 펼침·`문서 관리`/`설정` 클릭 시 접힘이었는데, 클릭 핸들러를 새로 잇는 걸 빠뜨려 반쯤 끝난 채로 남아 있었을 뿐이었다(대화로만 정해지고 문서·커밋 어디에도 문장으로 안 남은 결정). **2차로 정정**: `expand_button`과 관련 QSS·상수를 완전히 제거하고, 네비게이션 클릭 핸들러에 펼침/접힘을 직접 실었다. DESIGN §14.2.2도 새 동작으로 갱신. 테스트 6건을 새 동작에 맞게 다시 작성. 전체 981 passed / 5 skipped. **교훈**: 문서와 커밋 메시지가 모순될 때 문서 쪽을 무조건 권위로 믿지 말 것 — 애매하면 사용자에게 먼저 확인하는 게 재작업보다 빠르다.
- **T10.38(sLM 모델 관리 팝업에 실제 다운로드 버튼) 완료 [2026-08-22, 사용자 요청]**: 기존 "다운로드 안내" 버튼은 링크·명령줄을 팝업 텍스트로만 보여줘 사용자가 직접 복사해 받아야 했다 — `ui/widgets/slm_download_dialog.py`(`SlmDownloadDialog`) 신설로 "다운로드 시작" 버튼이 앱 안에서 직접 받는다. **자동 다운로드 금지 원칙(TECH 9.1)과는 안 부딪힌다**[사용자 확정 방향] — 그 원칙은 앱이 조용히 인터넷에 나가는 것을 막는 것이지, 사용자가 명시적으로 누른 다운로드까지 막는 게 아니다. UI 시안을 먼저 보여주고 사용자 피드백 2회 반영(닫기 버튼 추가, 닫기를 항상 오른쪽에 + 실패 시 팝업 안에서 실패 문구와 재시도) 후 확정. 상태는 idle→progress→(done|failed). **새 다운로더를 만들지 않았다** — `slm/download.py`의 기존 이어받기 지원(`.part` 파일)에 `on_progress`·`cancel_event`만 얹었다. 🔴 **"닫으면 이어받기가 끊기지 않나"를 설계 단계에서 먼저 확인** — QThread를 다이얼로그가 소유해 창을 닫을 때 살려두면 `ModelManagerDialog`가 이미 겪은 것과 같은 소유권 크래시 위험(Phase 6 패턴)이라, 닫으면 취소하되 `.part` 기반 이어받기로 다음에 다시 누르면 이어지게 해 "닫아도 이어받을 수 있다"는 안내를 실제로 참이 되게 했다. `_DownloadWorker`는 `ModelManagerDialog`의 기존 `_VerifyWorker`(T7.9)와 같은 패턴. 테스트 9건 추가(다운로더 4건은 가짜 `urlopen`으로 네트워크 없이 검증, 다이얼로그 5건은 `qtbot.waitUntil`). 🔴 **`QWidget.isVisible()` 함정을 또 밟았다**(Phase 7.6·11-C와 같은 종류) — `isVisibleTo(dialog)`로 해결. 전체 978 passed.
- **T10.37(인덱싱이 권장 모드를 무시하고 항상 경량 모델로만 벡터를 만들던 버그) 완료 [2026-08-22]**: "qwen3.5는 언제 쓰이는지" 질문으로 sLM 선택 로직을 조사하다 별개로 발견 — `indexer/pipeline.py`의 `_prepare_embedder()`가 프로필 인자를 아예 안 받아 PC 성능 선택을 권장 모드로 바꿔도 인덱싱은 항상 경량 모델(ko-sroberta-multitask)로만 벡터를 계산했다. 🔴 **T10.26("모드 전환 시 벡터 자동 보완")도 실은 이 경로를 다시 부르는 것이라 권장 모드 벡터를 끝내 못 채우고 있었다** — CLAUDE.md 기록과 실제 코드가 어긋나 있었다(실제 `data/index.sqlite3`는 과거 세션의 수동 스크립트로 KURE-v1 벡터가 채워진 상태라 실사용에서 가려져 있었음). **착수 전 "고칠 가치가 있는지" 실측부터 했다**[사용자 요청] — 실제 인덱스(2,577청크)로 자연어 질의 6건을 경량·권장 양쪽으로 비교: 4건에서 권장이 뚜렷이 나음(경량은 "리눅스마스터 자격증" 같은 무관 문서가 반복적으로 섞임), 2건 동일, 권장이 나쁜 경우 없음 — Phase 7.5가 "10문서로는 결론 미확정"이라 미뤘던 검증을 실제 규모로 재확인 후 착수했다. 수정은 `_prepare_embedder()`~`IndexingThread`까지 `profile` 인자를 순서대로 실어 나르는 배선뿐 — `embed_missing()`이 이미 "빠진 벡터만" 채우는 구조라 전체 재파싱 불필요, T10.36(대량 인덱싱 PC 멎음)과도 무관. 테스트 2건 추가(수정 전 `TypeError`로 실패 확인 후 복원), 기존 `_FakeIndexingThread` 테스트 대역도 `profile` 인자를 받도록 수정. 전체 971 passed / 5 skipped. 상세는 `TASK` T10.37.
- **T10.36(대량 인덱싱 중 PC 멎음 — 진단 로그 신설) [2026-08-21, 사용자 보고, 원인 미확정]**: 권장 사양 PC에서 `.doc`가 많이 섞인 문서 1만 개 이상을 인덱싱하다 화면이 반복해서 까맣게 변했다 돌아오길 몇 차례 겪은 뒤 PC가 완전히 멎어 재부팅, 재부팅 후 재인덱싱은 몇 분간 무반응 뒤 평소보다 느리게 진행됨. **다른 PC의 문서라 이 PC에서 재현 불가** — 코드 조사로 세운 가설: ① LibreOffice 변환(`.doc` 많음, 파일마다 새 soffice 프로세스)이 유력 용의선상, 자식 프로세스가 완전히 안 죽는 것은 알려진 LibreOffice CLI 고질 이슈 ② 대상 폴더가 OneDrive 등 클라우드 동기화면 `Path.resolve()` 반복 호출이 온디맨드 다운로드를 유발해 "재부팅 후 몇 분 무반응"을 설명할 수 있음 ③ 이 프로젝트가 실측 검증한 최대 규모는 ~1천 개뿐이라 1만 개 규모는 프로파일링된 적이 없음. **이번엔 원인을 고치지 않고 관측 지점만 심었다** — `indexer/index_log.py` 신설(`data/logs/`), 파일마다 파싱 시작/완료/실패 로그(크래시 시 마지막 줄 = 범인 파일) + 50개마다 `soffice.bin` 잔존 수 체크포인트 + LibreOffice 변환 10초 초과/timeout 시 소요시간·잔존 프로세스 수 기록. `ASSETS_DIR`과 같은 테스트 격리 함정(로거 전역 싱글턴이 첫 테스트의 경로를 고정)을 미리 피해 `_isolate_indexing_log` fixture 추가. 전체 967 passed / 5 skipped 유지. 실제 원인 확정과 수정은 다음번 재현 시 이 로그를 보고 진행하기로 함 — 확신 없는 처방(프로세스 강제 종료 등)을 지금 코드에 넣지 않았다. 상세는 `TASK` T10.36.
- **T10.34(hwp에서 머리말·배너가 절 제목으로 잡히는 오탐) 완료 [2026-08-21]**: 백로그 등록 시(2026-08-19) 표본이 hwp 1개뿐이라 보류했으나, exdoc에 hwp·hwpx가 8개로 늘어 착수했다. 문제의 배너(`최강 자격증 기출문제 전자문제집 CBT : www.comcbt.com`)를 만드는 원인은 진짜 절 제목(`1과목 : 리눅스 운영 및 관리`)이 **칸이 정확히 하나뿐인 표**로 들어가 있는 것이었고, 새로 늘어난 KSEL 서식 중 하나(`시험 환경 및 시험 구성도`)도 같은 패턴이라 우연이 아님을 확인했다. 백로그가 우려한 "표 셀 전체를 제목 후보로 승격"(범위가 큼) 대신, **칸이 정확히 하나뿐인 표만** 좁혀 잡는 `single_cell_table_heading()`을 신설(`parser/utils/headings.py`) — xlsx의 "1행에 한 칸만 차면 제목"(T10.32)과 같은 원리라 새 개념이 아니다. 실측으로 위험을 먼저 쟀다: exdoc 8개 문서의 표를 전수 조사해 칸 하나짜리 표가 딱 2건(전부 절 제목 배너)뿐이고 나머지 20여 개 정상 데이터 표는 전부 칸이 여럿이라 오탐 위험이 없음을 확인 후 구현했다. 테스트 15건 추가(누적 967 passed / 5 skipped). T10.33(pptx 폴백)·T10.35(txt)는 여전히 백로그. 상세는 `TASK` T10.34.
- **T10.32(제목 표시를 나머지 포맷으로 확대) 완료 [2026-08-19]**: "이 수정의 대상 포맷이 어떻게 되지?"라는 질문에 답하려다 🔴 **T10.31의 docx 지원이 한 번도 동작한 적이 없다**는 것을 발견했다 — `_is_heading()`이 정의만 되고 파싱 루프가 부르지 않아, Heading 문단이 15개인 문서도 제목이 0개였다(`.doc`·`.rtf`도 위임 경로라 함께 막혀 있었다). T10.31의 테스트 11건에 docx·pptx 경로 검증이 하나도 없어 가려져 있었고, 낮은 채움률이 "문서에 구조가 없어서"로 설명돼 있어 코드를 의심할 이유가 없어 보였다. 판별 로직을 `parser/utils/headings.py`로 공용화하고(PDF는 순수 이동, 동작 무변경) **xlsx**(1행에 한 칸만 찬 제목 행, 🔴 시트명과 같으면 비움[사용자 확정] — 표 카드가 이미 시트명을 위치로 보여준다) · **hwp/hwpx**(글꼴 크기, 크기가 본문이 아니라 `header.xml`·`CharShape` 정의에 있어 두 곳을 맞물려 읽는다) · **`.doc` 폴백**(Heading 스타일이 없는 문서만)을 추가했다. 🔴 **구현 전에 세 대상을 먼저 쟀다** — 순정 docx는 크기 정보가 아예 없어 폴백 무효, pptx는 제목 없는 슬라이드 154개 중 크기 차이가 28개(18%)뿐이라 **제외**, `.doc`만 값이 있었다. 문턱(1.2배)만 쓰면 12pt 노이즈가 16pt 진짜 제목을 덮어써서(이어달리기 방식) **문서 최대 크기만** 쓴다. 함정: 문단 판별을 lxml 요소 `id()`로 했더니 항상 불일치(lxml은 같은 노드에 매번 새 프록시를 준다) — 미리 써둔 테스트가 잡았고 문단 순번으로 바꿨다. **실측: 전체 채움률 21.9% → 34.3%, `.doc` 0% → 94.7%, `.xls` 0% → 100%, `.xlsx` 0% → 60.8%(27/30 문서).** 테스트 15건 추가(누적 937 passed / 5 skipped). 남은 과제는 T10.33(pptx 폴백)·T10.34(hwp 배너 오탐)·T10.35(txt)로 백로그 등록.
- **Phase 11-E(팝업 3종 재도색) 완료 [2026-08-19, 사용자 요청]**: `폴더 관리`·`모델 관리`·`인덱싱 진행률` 팝업이 Phase 4·7 시절 "최소 구현"에서 재도색을 못 받아, Phase 11의 카드형 디자인 시스템 옆에서 기본 Qt 버튼·여백 없는 레이아웃으로 튀고 있었다(사용자가 스크린샷을 보고 지적). **새 색·새 클래스 없이 기존 토큰만 재사용**했다 — `PageEyebrow`/`PrimaryButton`/`SidebarFooterButton`, 여백 20px, 얇은 구분선. 폴더 관리는 문서 관리 카드처럼 라벨 중복을 뺐고, 모델 관리는 행마다 카드 배경을 둘러 표처럼 붙어 보이던 목록을 분리하고 배지를 알약 모양으로 바꿨다. 🔴 **`_ModelRow`/`_SlmRow`가 순정 `QWidget`이라 `WA_StyledBackground` 없이는 QSS `background`가 조용히 무시된다** — `QFrame`과 다른 이 함정을 놓치지 않고 속성을 명시로 추가했다. 🔴 **재도색 검증 중 실제 버그를 잡았다** — 버튼에 QSS `background`를 직접 지정하면 Qt의 기본 "비활성화" 회색 처리가 더는 자동으로 안 먹어서, 비활성 버튼과 활성 버튼이 스크린샷에서 구분이 안 됐다. `#SidebarFooterButton:disabled` 규칙을 추가했는데, 이 토큰을 문서 관리 페이지의 "취소"/"재시도" 버튼과 공유해 그쪽 비활성 표시도 같이 고쳐졌다(같은 토큰을 여러 화면이 공유하는 구조가 아니었다면 세 번 따로 겪었을 함정). 인덱싱 진행률은 Phase 11-B에서 objectName만 붙이고 QSS를 빠뜨렸던 `IndexingProgressStage` 라벨에도 스타일을 채웠다. `QWidget.grab()`으로 3개 팝업 시각 확인, 전체 906 passed / 5 skipped 유지(기존 테스트는 objectName 문자열이 아니라 속성을 참조해 재도색에 안 흔들림).
- **Phase 11-D(이미지 자산 중앙화) 완료 [2026-08-19, 사용자 제안]**: "문서 폴더마다 `.assets`가 생겨 지우기 어렵다"는 지적. 실측(`../exdoc`)으로 `.assets` 루트 11개·하위 90개·파일 1,173개·104.1MB가 문서 트리 곳곳에 흩어진 것을 확인했고, 더 심각한 건 **사용자 원본 문서 폴더에 쓰기를 한다**는 점(OneDrive·백업 도구 동기화 부작용)이었다. `data/assets/<doc_id>/`로 중앙화 + 옛 잔재는 상시 버튼 대신 **일회성 스크립트**(`scripts/clean_assets.py`)로 정리[사용자 승인]. 🔴 **착수 조사에서 CLAUDE.md 서술 오류를 발견했다** — 아래 T6.8 항목의 "doc_id가 파일 내용 해시 기반"이 틀렸다(실제로는 절대**경로** 해시) — 이번 작업이 doc_id를 중앙 폴더 키로 쓰는 것이라 재확인 중 잡혔다. **구현이 파서를 한 줄도 안 건드렸다** — `parse_file(path, asset_dir=...)`가 Phase 1부터 있던 배선이라 `indexer/pipeline.py`의 호출부 한 줄만 바뀌었다. `_prune_stale_documents()`(T10.5)가 문서를 지울 때 그 doc_id 폴더도 함께 지우도록 얹었다(안 그러면 중앙화한 곳이 계속 불어난다). `clean_assets.py`는 **DB가 아직 옛 경로를 참조하면(재인덱싱 전) 삭제를 거부한다**(`--force`로 무시 가능) — 순서를 지키지 않으면 이미지 카드가 깨진다. 테스트 16건 추가(파이프라인 3건은 수정 전 실패 확인 후 복원). 실사용 검증: `../exdoc` 이미지 포함 문서 8건 사본 인덱싱 — 작업 폴더 `.assets` 0개, 이미지 청크 288개 전부 중앙 위치 확인. 🔴 **전체 스위트 실행에서 진짜 오염 버그를 잡았다** — `index_folder()`를 부르는 테스트 전부가 이 PC의 실제 `data/assets/`에 doc_id 폴더를 만들고 있었다(실측: 한 번에 31개, 159KB). `indexer/pipeline.py`가 `ASSETS_DIR`을 임포트 시점에 지역 이름으로 복사해 와서 `config.settings.ASSETS_DIR`만 패치해서는 안 먹는다 — Phase 7.7의 `app_state.json` 오염, T10.5의 `index.sqlite3` 오염과 같은 함정이 이미지 자산에서 재발한 것. `tests/conftest.py`에 전역 `autouse` fixture(`_isolate_assets_dir`)를 추가해 막았다. **실제 프로젝트 데이터 마이그레이션은 아직 안 함** — 강제 재인덱싱 + 실제 파일 삭제라는 되돌리기 어려운 작업이라 사용자 확인 후 별도 진행. 상세는 `PLAN` Phase 11(11-D).
- **Phase 11-C(설정 페이지) 완료 [2026-08-19]**: 11-A가 옮겨 놓은 `PC 성능 선택`·`모델 관리` 옆에 sLM 실행 옵션 3개(모델 상주 · 유휴 종료 시간 · AI CPU 사용)와 실행 정보 4줄을 채웠다. 🔴 **착수 조사에서 DESIGN §14.5.2의 전제가 틀렸다는 것을 확인했다** — 셋 다 생성자 인자라 재기동이 필요하다고 봤지만, `유휴 종료 시간`(과 그것을 끄는 `모델 상주`)은 `touch()`가 **요청마다 값을 다시 읽어** 재기동 없이 바로 먹는다. 진짜 기동 인자는 `AI CPU 사용`(`n_threads`)뿐이라 실제 결정 대상은 하나였고 **즉시 내리고 다음 요청에 새 값으로**로 확정[사용자 확정] — 같은 방식이 이미 `set_profile()`에 있고(모델 교체 시 "떠 있으면 내린다"), 4.8GB를 그 자리에서 돌려주는 것이 Phase 7의 유휴 종료 결정과 결이 맞는다. 🔴 **세 setter 모두 `_lock`을 기다리지 않는다** — 그 락은 `chat()`이 추론 내내(중앙 18.3초) 쥐고 있어 기다리면 **UI가 18초 얼어붙는다**(T10.23에서 취소 함수로 겪은 그 함정). 락을 즉시 얻을 수 있을 때만 타이머 재장전·서버 종료를 하되, 못 얻어도 누락되지 않게 두 경로를 뒀다 — 유휴는 `chat()` 끝의 `touch()`가, CPU는 `ensure_ready()`가 기동 당시 값(`_active_n_threads`)과 비교해 재기동한다. 🔴 **CPU는 숫자가 아니라 모드(`auto`/`half`/`max`)를 저장한다** — 폴더째 다른 PC로 옮기는 것이 전제라(TECH 9.1) 저장된 "8스레드"가 4코어 PC에선 전혀 다른 의미가 된다. `auto`는 계산 없이 `None`을 넘겨 llama.cpp 판정에 맡긴다(Phase 6~7 실측치가 전부 인자 없이 나온 값). 상주 중에는 유휴 콤보를 닫고, 실행 정보는 없으면 `(설치되지 않음)`을 붙이며, 기동 시 저장값을 화면과 서비스 **양쪽에** 건다(화면만 맞추면 11-A의 PC 성능 콤보와 같은 어긋남이 난다). **테스트 함정**: 복원이 신호를 쏘면 안 되는데, 처음 쓴 테스트는 복원값이 현재값과 같아 Qt가 애초에 신호를 안 쏴서 `blockSignals`를 빼도 통과했다 — 세 값이 전부 달라지는 복원으로 바꾸니 세 신호가 모두 잡혔다. 테스트 23건 추가(sLM 서비스 6 · 설정 4 · UI 13), `QWidget.grab()` 3화면 확인(상주 ON 시 유휴값 300→0, CPU 절반에서 `n_threads=7` 실측). 상세는 `PLAN` Phase 11, 설계는 `DESIGN` §14.5.2·§14.5.3.
- **T10.28(답변 카드 폭을 대화 영역의 80%까지) 완료 [2026-08-18, 사용자 요청]**: `MAX_BUBBLE_WIDTH_RATIO` 0.70 → 0.80. 🔴 **비율 상수만 올려서는 화면이 안 바뀐다** — `setMaximumWidth()`는 상한일 뿐이라 위젯은 자기 `sizeHint`만큼만 차지하고 남는 공간은 `addStretch()`가 다 먹는다(실측: 회색 영역 877px에 표 카드가 475px). 답변 말풍선에 늘어날 권한을 줘야 한다 — 크기 정책 `Expanding` + `addWidget(widget, 1)`/`addStretch(0)`. **stretch 인자가 핵심**: 둘 다 1이면 공간을 반씩 나눠 상한에 못 미친 채 멈춘다. 질문 말풍선은 그대로(짧은 질문이 폭 전체를 먹으면 안 된다). 실측: 대화 영역 868px / 답변 카드 **704px(81%)** / 질문 243px(28%). 테스트 2건 추가 + `expand`를 꺼서 실제 실패 확인 후 복원, 비율을 하드코딩하던 기존 테스트도 상수 참조로 수정.
- **T10.27(AI 요약 답변이 중간에 끊기는 문제 + 표에서 질문 관련 행만 남기기) 완료 [2026-08-18, 사용자 보고]**: "답이 `… 다릅니다. [ ` 로 끝난다"는 보고. 🔴 **권장 모드 특유의 문제가 아니었다** — 두 모드의 프롬프트 크기가 6,594자로 동일했고, 실측하니 `prompt_tokens=3,922`로 `n_ctx=4096`에 **답변용 여유가 174토큰뿐**이었다. 하필 `[` 직후 끊기면 반쪽 대괄호가 남는다(`expand_citations`는 완전한 `[N]`만 치환). **같은 측정이 지연도 설명했다 — 150초 중 140초가 프롬프트 처리**(35.6ms/토큰 × 3,922토큰), 생성은 11초뿐. 잘림과 지연이 같은 원인이다. ① **표에서 질문 관련 행만 남기기**(조사 처리는 FTS5·재순위와 같은 `query_term_variants()` 재사용 — T10.8의 교훈. 한 행도 안 걸리면 표를 비우지 않고 원래대로 채운다) ② **`n_ctx` 4096→6144**(줄이는 것과 달리 늘리는 건 발췌를 안 자르므로 Phase 6·7 수치 유효). 실측: 프롬프트 3,922→**2,151토큰**, 여유 174→**3,885토큰**, 프롬프트 처리 140→**71초**, 첫 응답 150→**85초**. 답변도 **세 등급 × 두 열이 전부 정확**(KAC 20/35, KPC 30/45, KSC 40/60). 테스트 832 passed / 5 skipped. 남은 과제: `needs_review` 오판, 인용 번호가 엉뚱한 파일을 가리킴, 85초 지연.
- **T10.26(모드 전환 시 벡터가 없으면 자동 보완 — 권장 모드가 조용히 죽던 문제) 완료 [2026-08-18, 사용자 보고]**: "권장 모드로 바꿨더니 무엇을 물어도 '관련 문서를 찾을 수 없습니다'". 인덱스에 경량 벡터만 있고 KURE-v1 벡터가 0개라 모든 유사도가 `None`→1단계가 전부 차단. 벡터는 `(chunk_id, model)` 복합키(Phase 7.5)라 **모드 전환만으로는 절대 생기지 않는다.** ① 현재 인덱스에 KURE-v1 벡터를 채우고(재파싱 없이 임베딩만, 607청크 136초) ② 모드 전환 시 자동 보완 — 새 스레드 없이 기존 재인덱싱 경로를 쓴다(`index_folder()`가 파일을 전부 건너뛰어도 `embed_missing()`은 무조건 부른다). **시작 시점엔 자동 실행 대신 상태바 안내**(켜자마자 몇 분짜리 작업이 튀면 곤란). 테스트 826 passed.
- **T10.25(AI 요약 발췌에서 표를 행·열 구조 그대로) 완료 [2026-08-18, 사용자 지적]**: "응시료가 단계별로 다른데 2단계 값만 나온다". 조사해보니 **검색은 세 단계를 다 물어왔고** 프롬프트에 세 금액이 전부 있었는데 모델이 하나만 골랐다 — 🔴 원인은 **평문화로 열 머리글이 셀에서 멀어진 것**(등급×지원유형 2축을 뭉개 "KPC 응시자는 40만원"처럼 다른 등급의 열 값을 붙였다). `table_json.header_row`가 이미 보존돼 있고 등급명은 **바로 앞 청크**에 있다는 걸 확인해, `render_table_text()`가 머리글+행을 렌더링하고 `heading_before()`(T10.21 이웃 조회의 역방향)가 제목을 얹는다. **형식을 SYSTEM_PROMPT 기존 예시에 맞춰 프롬프트 문구를 한 글자도 안 바꿨다** — Phase 6·7 실측치 유효. 절단은 행 단위(글자 수로 자르면 "40만" 같은 반쪽 셀이 남는다). 발췌 10,498→5,693자, 지연 206.5→142.6초. 테스트 820 passed.
- **T10.24(AI 요약 발췌 중복 제거) 완료 [2026-08-18]**: 발췌 5칸 중 **2칸을 완전히 같은 청크**가 차지하고 있었다 — 근거가 5칸뿐이라 중복 하나가 곧 근거 하나의 손실. 🔴 **순서는 안 바꿨다** — 처음엔 "유사도 순"을 추천했다가 실제 계산에서 **역효과**를 확인(유사도 상위는 47~536자 헤딩 조각들이고 금액 표가 전부 밀려나 프롬프트에 20/35만원만 남았다). T10.8·T10.11의 순위가 내용 있는 표를 위로 올려주고 있어 그대로 따른다. 기존 테스트 2건이 우연히 같은 내용 픽스처를 쓰고 있어 함께 수정.
- **T10.23(챗봇 모드에서 AI 요약 자동 생성) 완료 [2026-08-18, 사용자 제안]**: 검색 모드와 챗봇 모드가 **첫 턴에 사실상 같은 화면**이던 문제(실제로 다른 건 `fallback_query`·이력 누적·이력 주입 셋뿐이고 셋 다 2번째 턴부터 켜진다)를, 챗봇에서 ② AI 요약을 버튼 없이 자동 시작하도록 해 갈랐다 — 검색은 목록, 챗봇은 답변. **Phase 7.6이 자동 생성을 두 번 반려한 사유("11~79초 화면이 빈다")는 이제 성립하지 않는다** — ①이 7~14ms에 화면을 채우고 ②가 뒤이어 채워진다. 동시 요청은 **"직전 생성 취소"로 확정**[사용자 선택]. 🔴 결과만 버리는 소프트 취소로는 부족해 `LlamaClient`에 진짜 중단 경로를 넣었다(`stream=False`라 블로킹 구간엔 응답 객체가 없어 `urllib`으론 못 끊는다 → `http.client`로 연결을 쥐고 다른 스레드에서 닫는다). 🔴 **`abort_active_request()`는 `_lock`을 잡지 않는다** — 그 락은 `chat()`이 18.3초 쥐고 있어 취소 함수가 기다리면 **UI가 그만큼 얼어붙는다**(취소가 멈춤을 만든다). 부수로 T10.19 지연 스크롤이 패널 파괴 뒤 발사되는 기존 결함도 수정(`QTimer.singleShot`에 컨텍스트 객체 전달). 실물 검증: 버튼 없이 요약이 뜨고, 취소 시 llama-server CPU 증가가 6초당 59초분 → 10초당 0초분으로 **실제로 멈춘다**. 테스트 801 passed.
- **T10.22(AI 요약 진행 표시 개선 + 최근 검색 최신순) 완료 [2026-08-15, 사용자 요청]**: ① 진행 문구를 사용자 지정안으로 교체하고 **부드러운 깜박임**(`QGraphicsOpacityEffect`+`QPropertyAnimation`, 1.2초 주기 1.0↔0.35, `InOutSine`) 추가 — "생성형 AI처럼 동작 중임을 알려주는 효과" 요청. 본문 라벨에만 걸어 제목·배지는 안정적으로 두고, 효과 객체는 계속 붙여둔 채 애니메이션만 시작/정지(붙였다 뗐다 하면 레이아웃 재계산으로 흔들림). `state=="pending"`에서만 돌고 완료 시 즉시 정지 + 불투명도 1.0 복원(안 하면 흐린 값이 굳어 완성 답변이 흐려 보임). ② `RecentSearches._render()`가 최신순 목록을 **뒤집어** 최신을 맨 아래 두던 것(Phase 7.7 해석)을 그대로 쓰도록 바꿈 — 최신이 맨 위, 공간 부족 시 아래(오래된 것)부터 빠진다. 기존 정렬 테스트 3건 반대 기대값으로 갱신, 깜박임 테스트 4건 추가(누적 788 passed / 5 skipped).
- **T10.21(결과 카드 "근처 내용 더보기" 추가) 완료 [2026-08-15, 사용자 요청]**: 헤딩 한 줄짜리 청크가 1위로 나오는데 같은 섹션의 실제 내용(표)이 안 보이는 문제 — **파서 청킹 구조가 원인**(헤딩 문단과 뒤따르는 표가 별개 청크, Phase 1의 의도된 분리). 세 방향(더보기 버튼/자동 병합/파서 수정) 중 **버튼**으로 확정[사용자 선택] — 자동 병합은 "짧은 헤딩" 판정 오판 위험, 파서 수정은 전체 재인덱싱 + Phase 1 원칙 변경이라 범위가 큼. `search/chunk_neighbors.py`(신규) `fetch_next_chunk()`가 같은 문서 안 삽입 순서(내부 PK = 원본 순서) 기준 다음 청크를 찾고, `card_common.NearbySection`(T10.14 `SummarySection`과 같은 공유 패턴)을 세 카드가 쓴다. **LLM·임베딩을 안 써서 모델 설치와 무관하게 항상 보이고** 워커 없이 메인 스레드에서 처리. 테스트 10건 추가(누적 784 passed / 5 skipped). 실제 인덱스로 사용자가 보고한 그 헤딩 청크 → 그 표 청크가 정확히 나오는 것 확인.
- **T10.20(결과 카드 텍스트 드래그·복사 안 되던 문제 수정) 완료 [2026-08-15, 사용자 보고]**: AI 요약 카드는 이미 선택 가능했지만 정작 검색 결과 카드 본문·파일명·위치 라벨엔 `TextSelectableByMouse`가 빠져 있었다. `result_card.py` 본문 + `card_common.py`의 공유 헤더 함수(세 카드 타입이 다 씀)에 추가해 한 번에 해결(T10.10과 같은 패턴). 테스트 1건 추가, 전체 774 passed / 5 skipped.
- **T10.19(챗봇 검색·AI 요약 진행 중 스크롤이 안 따라가는 문제 수정) 완료 [2026-08-15, 사용자 보고]**: "AI 요약 누르면 이전 메시지로 올라감", "다음 검색하면 직전 검색 끝에 멈춰 방금 질문이 안 보임" 두 보고. `_scroll_to_bottom()`이 `_send()`에서만 불렸고 검색 결과 도착·AI 요약 진행 전환(총 6개 지점)에서는 재호출이 없었다. 이 6곳 모두에 스크롤 재조정을 추가하면서 실측으로 기존 `QTimer.singleShot(0,...)` 패턴 자체의 결함 2건도 발견 — ① 0ms 뒤에도 레이아웃이 안 끝나 있어 `maximum()`이 갱신 전 값일 때가 있음(`rangeChanged` 신호 기반으로 전환) ② 레이아웃이 여러 번(348→418→482)에 걸쳐 재계산돼 한 번만 받고 끊으면 중간값에서 멈춤(120ms 유휴 디바운스로 해결). 테스트 4건 추가. 실제 앱 스크린샷으로 AI 요약 완료·다음 메시지 전송 모두 최하단 도달 확인.
- **T10.18(대명사만 있는 챗봇 후속 질문이 검색 0건으로 막히는 문제 수정) 완료 [2026-08-15, 사용자 보고+실사용 디버깅]**: T10.17 검증 중 "코치 자격시험 응시 방법이 뭐야?" → "그건 얼마야?" 흐름에서 후속 질문이 검색(①) 단계부터 0건이 나와 "AI 요약 보기" 버튼이 비활성화(Phase 7.6부터 있던 로직)돼 대화 이어가기를 시도조차 못했다. "이력 텍스트만으로 LLM이 답하게 하자"는 안은 TECH 5.3 근거 강제 원칙(인용 근거가 없어짐)과 충돌해 기각하고, 대신 검색 단계에서 고쳤다 — `SearchWorker`에 `fallback_query` 추가, 이번 메시지 단독 0건일 때만 직전 턴 질문을 붙여 재검색(LLM 호출 없음, `hybrid_search()` 무수정). 결과가 생기면 버튼도 기존 로직 그대로 자연히 켜져 버튼 문제가 부수적으로 해결됨 — 별도 UI 수정 불필요. 🔴 **조사 중 실시간 로그로 두 가지 겉보기 동일한 문구를 구분해냈다** — "관련 문서를 찾을 수 없습니다"(NO_EVIDENCE, 모델 미호출)와 "문서에서 찾을 수 없습니다"(ABSTAINED, 모델이 실제로 기권) — 키워드 스타일 질문은 유사도가 임계값 근처라 통과해도 모델이 기권할 수 있고, 완결된 질문으로 바꾸면 유사도·응답 둘 다 개선됨(별개 현상, 버그 아님). 테스트 8건 추가, 전체 769 passed / 5 skipped.
- **T10.17(챗봇 AI 요약이 이전 대화를 참고 — 대화 맥락 유지) 완료 [2026-08-15, 사용자 요청]**: "이어나가는 대화"를 검색(①)/생성(②) 두 단계로 나눠 접근 — ①은 그대로 매 턴 독립(Phase 7.6이 검색에 LLM 끼우는 설계를 이미 두 번 반려한 전례 때문), **②("AI 요약 보기")만 대화형으로** 만들어 이미 LLM 지연을 감수하는 지점에만 손을 댔다. `slm/prompt.py`에 `HistoryTurn`/`format_history()` 신설, `build_messages(history=...)` 추가 — `history=()` 기본값은 이전과 **완전히 동일한 문자열**을 만들도록 회귀 테스트로 고정(Phase 6/7 실측 기권정확도가 이 경로 기준). 이력은 "근거로 쓰지 말 것"을 프롬프트에 명시. 새 저장소 없이 T10.16이 이미 들고 있는 `ChatPanel._bubbles`에서 성공(OK) 턴만 읽어 씀(`history_before()`). 테스트 다수 추가, 전체 764 passed / 5 skipped. 실제 sLM 종단 검증은 이 세션 응답 지연으로 결론 못 냈으나, 스텁 LLM + 실제 QThread 경로 통합 테스트로 배선은 이미 증명됨.
- **T10.16(챗봇 껐다 켜도 이전 대화 유지) 완료 [2026-08-15, 사용자 요청]**: T10.15 직후 "껐다 켜도 이전 대화가 남아 있으면 좋겠다"는 요청. `_activate_chat_mode()`가 토글마다 `ChatPanel()`을 새로 만들던 것을, `_chat_panel_cache`에 인스턴스를 계속 들고 있다가 재사용하도록 변경. 🔴 **`ResultList._clear()`가 모드 전환마다 챗봇 패널까지 `deleteLater()`해 재사용이 막혀 있었다** — 패널이 이미 갖고 있던 `objectName == "ChatPanel"` 마커(T7.6, `card_count()`에 안 잡히려는 용도)로 예외 처리했다. 테스트 2건 추가, 전체 751 passed / 5 skipped. 실제 앱에서 대화 후 끄기→켜기 시 같은 대화가 유지되는 것을 스크린샷으로 확인.
- **T10.15(검색↔챗봇 모드 질문/결과 완전 분리) 완료 [2026-08-15, 사용자 보고]**: "AI 챗봇 미사용으로 검색 후 챗봇으로 이동하면 검색 결과가 그대로 나온다"는 보고 — 버그가 아니라 Phase 7.7이 의도적으로 넣은 동작이었다(`_activate_chat_mode()`가 직전 검색어를 챗봇 첫 메시지로 자동 전송, "패널이 빈 채로 뜨면 토글이 안 먹는 것처럼 보인다"는 이유였다). 세 선택지 중 **완전 분리**로 사용자 확정 — 자동 전송 코드를 제거해 챗봇은 항상 빈 챗창으로 시작한다. 반대 방향(챗봇→검색 복귀)은 `_on_chat_message_sent`가 애초에 `_last_query`/`_last_results`를 안 건드려 이미 분리돼 있었다(재확인만, 코드 변경 없음). 전체 57 passed(알려진 ONNX 크래시 클래스 제외).
- **T10.14(검색 결과 카드마다 개별 "AI 요약 보기" 버튼) 완료 [2026-08-15, 사용자 지적]**: "AI 요약은 검색 결과 각 항목마다 있어야 하지 않나" 질문으로 확인해보니, Phase 7.6 이후 AI 요약은 챗봇 모드에만 있고 그마저도 턴 전체 대상 버튼 하나뿐이었다(일반 검색 화면엔 요약 기능 자체가 없었다). "카드마다 개별 버튼"으로 사용자 확정. `card_common.py`에 `SummarySection`(버튼+`SummaryCard`)을 신설해 세 카드 타입이 공유, `SummaryWorker` 신호를 이 위젯 메서드에 직접 연결해(중간 람다 없음) Qt의 QObject 파괴 시 자동 연결 해제에 기댔다 — 새 검색으로 카드가 사라진 뒤 응답이 늦게 와도 죽은 위젯을 안 건드린다. 모델 미설치면 자동으로 버튼을 숨긴다. 챗봇 말풍선은 카드를 재사용하되(T10.13) `show_summary=False`로 꺼서 턴 단위 버튼과 중복 안 되게 했다. 🔴 **테스트 중 이 PC의 실제 임베딩 유사도가 실행마다 흔들리는 것을 재확인**(Phase 3·7과 동일한 int8 양자화 배치 스케일 문제) — 작은 픽스처가 요약 임계값(0.5)을 못 넘겨 Phase 7과 같은 해법(유사도 직접 지정)으로 우회했다. 테스트 8건 추가(누적 749 passed / 5 skipped). 실제 앱에서 카드별 버튼 클릭 시 그 카드에만 "AI 요약"이 펼쳐지고 다른 카드는 무영향인 것을 스크린샷으로 확인.
- **T10.13(검색·챗봇 결과 5개로 통일 + 더보기 버튼) 완료 [2026-08-14, 사용자 요청]**: 검색·챗봇 둘 다 5개+"더보기" 버튼으로 개수 기준을 맞췄다 — 처음엔 챗봇만 텍스트 안내였다가, 같은 날 사용자가 "챗봇도 검색과 같은 버튼으로" 요청해 `ResultListMoreButton`을 그대로 재사용하도록 다시 고쳤다. **"챗봇 2~5순위도 각자 원문 열기 버튼"이라는 사용자 확정이 설계를 크게 단순화**했다 — 손으로 새로 만들지 않고 검색 카드(`ResultCard`/`TableCard`/`ImageCard`)를 말풍선에 그대로 재사용, T10.12가 챗봇 전용으로 짰던 렌더링을 전부 걷어내 코드가 더 단순해졌다. 카드 분기 함수를 `ui/widgets/card_dispatch.py`(신규)로 옮겨 `result_list.py`↔`chat_panel.py` 순환 임포트를 피했다. 실제 인덱스로 검색·챗봇 양쪽에서 5개 표시+"8개 더" 일치 확인. 테스트 다수 갱신(누적 735 passed / 5 skipped).
- **T10.12(챗봇 즉시 발췌 표/이미지 렌더링) 완료 [2026-08-14, 사용자 요청]**: Phase 7.6에서 "가독성은 범위 밖"으로 남겨뒀던 것을 해결 — 챗봇 top-1이 표/이미지 청크여도 이제 검색 화면과 같은 `QTableWidget` 그리드/썸네일로 나온다. `TableCard`/`ImageCard`에만 있던 렌더링 로직을 `card_common.py`로 끌어올려 공유(T10.10과 같은 함정을 처음부터 피함), `_AnswerBubble`의 본문을 "지우고 다시 그리기"로 바꿔 타입에 맞춰 텍스트/표/이미지를 갈아 끼운다. 표 복사·확대 버튼도 검색 화면과 동일하게 얹었다[사용자 확정]. 실제 인덱스로 175행×12열 표가 실제로 그리드 렌더링되는 것 확인. 테스트 7건 추가(누적 730 passed / 5 skipped).
- **T10.1("원문 열기" Office COM 딥링크) 완료 [docx/pptx/xlsx만, 2026-08-13]**: `QDesktopServices.openUrl()`로 파일만 열던 것을, 검색이 찾아낸 정확한 위치(문단/슬라이드/셀)로 이동하도록 PowerShell 서브프로세스 기반 Office COM 자동화를 얹었다. **hwp/hwpx는 이 PC에 한글이 없어 이번 범위에서 제외**(사용자 확정, 후속 과제). Word/Excel은 읽기 전용으로 연다(사용자 확정). `search/office_link.py`의 순수 함수 `plan_open()`이 청크 타입·확장자별로 검색어(사용자 질의어가 아니라 **그 청크 자신의 내용**)를 결정한다 — pptx는 `GotoSlide()`만, docx는 needle ladder로 `Range.Find`, xlsx는 시트 활성화 후 `Cells.Find`. COM이 없거나 실패하면 예외를 삼키고 조용히 기존 방식으로 폴백 — "파일이 열리기는 한다"는 보장을 절대 안 깬다. ~1~3초 걸려 `SearchWorker`와 같은 `QThread`(`OpenFileWorker`)로 돌리되 "파일 없음" 판정만 동기로 해 기존 테스트를 한 줄도 안 건드렸다. 🔴 **실제 Office COM 종단 검증에서 진짜 버그를 잡았다** — 청커가 여러 Word 문단을 `"\n"`으로 이어붙이는데 needle ladder가 그 문자열을 글자 수로만 잘라 문단 경계를 넘는 needle을 만들면 Word 문단 구분자(`\r`)와 안 맞아 실패했다(20자짜리도). 문단 단위로 먼저 쪼갠 뒤 문단 안에서만 사다리를 만들도록 고쳐 해결 — pptx·xlsx는 첫 실측부터 성공, docx만 이 수정이 필요했다. 자동화 테스트는 전부 통과한 상태에서 실제 Office로 열어보고 나서야 드러났다(Phase 4·5·7·7.7과 같은 패턴). 테스트 31건 추가(누적 722 passed / 5 skipped). 상세는 `PLAN` Phase 10 T10.1.
- **T10.50("원문 열기" 딥링크가 PDF는 아예 동작 안 하고 docx TEXT 청크는 사실상 항상 실패하던 문제) 완료 [2026-08-25, 사용자 질문 계기]**: "딥링크가 안 되는 것 같다"는 질문으로 형식별 실측 조사부터 했다. ① **PDF는 처음부터 시도조차 안 됐다** — `search/office_link.py`가 docx/pptx/xlsx만 지원하고 PDF는 `_PROGID_BY_EXT`에 없어 `is_office_available(".pdf")`가 항상 `False`(이 인덱스에서 청크 수 압도적 1위인 형식인데도). Office COM 없이 **Edge를 `--single-argument`로 직접 실행 + `file://...#page=N` URL 프래그먼트**로 페이지 이동을 붙였다(pptx의 `GotoSlide`처럼 검색 없이 페이지 번호만으로 충분). 🔴 **로컬 file:// URL에 `#page=`를 붙여 `ShellExecute`(Qt `QDesktopServices.openUrl`, PowerShell `Start-Process` 포함)로 열면 "지정된 파일을 찾을 수 없습니다" 오류로 아예 안 열린다**(실측 확인 — 셸이 프래그먼트까지 파일 경로의 일부로 봄) — msedge.exe를 직접 호출해 ShellExecute의 파일 조회 단계를 건너뛰어야 한다. 기본 PDF 뷰어가 Edge(`MSEdgePDF`)일 때만 지원, 그 외(Adobe Reader 등)는 조용히 기존 폴백. ② **docx TEXT 청크는 실제 인덱스 두 건으로 재현하니 Word `Find.Execute()`가 항상 실패했다** — 근본 원인은 `indexer/chunker.py`: `docx_parser.py`는 문단을 `"\n"`으로 이어붙여 경계를 보존해 넘기는데, 문장 재그룹 단계(`_group()`)가 `" ".join()`으로 다시 이으면서 그 `\n`을 전부 공백으로 뭉갰다. `office_link.py`의 `_build_needle_ladder()`는 T10.1 당시 이미 "문단 경계를 지켜야 Word Find가 성공한다"는 걸 알고 `\n` 기준으로 needle을 쪼개려 했지만, 이 시점엔 `\n`이 이미 사라진 뒤라 그 방어 로직이 무력화돼 있었다 — **두 기능이 문서에는 각자 맞게 짜여 있었는데 그 사이 파이프라인 한 단계가 전제를 깨고 있었던 경우**. `_regex_split_with_seps()`를 신설해 문장마다 원래 구분자(줄바꿈 vs 공백)를 그대로 붙여 반환하고 `_group()`이 `"".join()`으로만 잇도록 고쳤다 — `split_sentences()`(공개 API, 기존 테스트 계약)는 손대지 않고 청킹 내부 경로만 바꿔 kss·FTS5·sLM 프롬프트 등 다른 소비처는 영향 없다. 표/이미지 청크는 chunker.py를 안 거쳐(Phase 1 설계) 원래도 무관했다. **실제 인덱스 사본으로 재인덱싱 후 재검증**: 문단이 여럿 섞인 실제 docx 청크가 `\n`을 보존한 채 저장되고, `open_and_locate()`가 실제로 성공하는 것까지 확인(수정 전엔 같은 청크로 실패 재현). PDF도 실제 문서로 Edge가 정확한 페이지로 이동하는 것을 스크린샷으로 확인. 테스트 12건 추가(office_link 8건, chunker 2건 신규 + 기존 유지, 관련 파일 212건 통과).
- **T10.51(xlsx "원문 열기"가 두 번 실행돼 창이 두 개 뜨는 문제) 완료 [2026-08-25, 사용자 보고]**: T10.50 직후 이어서 발견. 원인은 세 COM 스크립트(docx/pptx/xlsx) 전부 "파일 열기"(`Documents.Open`/`Presentations.Open`/`Workbooks.Open`)와 "위치로 이동"(Find/GotoSlide/`Worksheets.Item`)이 **하나의 try/catch**로 묶여 있던 것 — 시트명이 조금 어긋나거나 검색어가 안 걸리는 등 위치 이동만 실패해도(파일은 이미 정상적으로 열려 창까지 뜬 뒤인데도) 스크립트 전체가 실패로 처리돼 `open_source_file()`이 "COM 자동화 실패"로 보고 **기존에 항상 있던 폴백**(OS 기본 프로그램으로 다시 열기)을 실행했고, 이 폴백이 Excel의 새 프로세스를 하나 더 띄웠다. 존재하지 않는 시트명으로 강제 재현해 실제로 PID 두 개(별도 창 두 개)가 뜨는 것을 확인 후, `.Open()` 호출을 try 블록 **밖으로** 빼고 위치 이동 로직만 내부 try/catch로 감싸 위치 이동 실패를 조용히 무시하도록(창은 이미 열려 있으니 그대로 둠) 고쳤다 — "파일이 열리기는 한다"는 T10.1의 원래 보장은 `.Open()` 시점에 이미 충족되므로 위치 이동 실패와는 무관해야 한다는 원칙을 그대로 코드에 반영. 같은 재현 시나리오로 수정 후 창이 하나만 뜨는 것 확인. 실제 COM을 띄우지 않고 지키는 구조적 회귀 테스트(`.Open()`이 `try {`보다 앞에 오는지) 3건 추가(관련 파일 38건 통과).
- **pptx "원문 열기"가 항상 실패하던 버그 수정 [2026-08-25, T10.1 후속]**: 사용자 요청으로 docx·pptx 딥링크를 실측 검증하다 발견 — 이 인덱스에 pptx 문서가 없어 T10.1 이후 pptx 경로가 한 번도 실 GUI로 종단 검증된 적이 없었다(합성 pptx로 처음 실측). `$ppt.Visible = $true`에서 매번 캐스팅 예외가 났다 — Word/Excel의 `Application.Visible`은 순수 Boolean이지만 PowerPoint만 `MsoTriState` 열거형이라, 이 서브프로세스 실행 컨텍스트에서는 `$true`가 자동으로 안 바뀐다. `-1`(MsoTriState.msoTrue의 실제 값)로 바꿔 해결 — 수정 전엔 실패, 수정 후 합성 pptx 3슬라이드로 슬라이드 3까지 정상 이동 확인. 문자열 검사로 지키는 구조적 회귀 테스트 1건 추가(관련 파일 39건 통과).
