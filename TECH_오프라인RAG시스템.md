# TECH: RAG와 sLM을 활용한 오프라인 문서 검색 시스템 — 기술 설계 문서

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 기준 문서 | 오프라인_RAG_시스템_설계문서.md |
| 문서 목적 | 아키텍처/구현 상세를 정의해 개발 참조 문서로 사용 (PRD의 기능 요구사항을 기술적으로 구체화) |

---

## 1. 시스템 아키텍처 개요

```
[대상 폴더 지정]
       │
       ▼
┌─────────────────────────────────────────┐
│  인덱싱 파이프라인 (최초 1회 + 증분 갱신)   │
│  폴더 스캔 → 형식별 파싱 → 제목/키워드 추출  │
│  → 문장 분리(한국어) → 청킹 → 임베딩 계산   │
│  → FTS5(키워드) + 벡터DB 동시 저장          │
└─────────────────────────────────────────┘
       │
       ▼ (인덱스 참조)
┌─────────────────────────────────────────┐
│  검색 파이프라인 (질문마다 실행)            │
│  질문 입력 → 1단계 키워드 필터(BM25, 즉시)  │
│  → 2단계 벡터 재순위(후보 내 정밀 검색)      │
│  → [기본] 원문 발췌 결과 표시                │
│  → [선택] 유사도 임계값 통과 시에만 sLM 답변 │
└─────────────────────────────────────────┘
```

핵심 설계 결정: 검색 시점에 전체 문서를 재스캔하지 않고 **사전 구축된 인덱스만 조회**한다. 이 결정이 8GB RAM 환경에서의 체감 속도를 좌우하는 가장 큰 요인이다.

## 2. 기술 스택

| 영역 | 선택 기술 | 비고 |
|---|---|---|
| 문서 파싱 | python-docx, openpyxl, python-pptx, PyMuPDF, pyhwp | 형식별 상세는 3장 참고 |
| 구버전 포맷 변환 | LibreOffice 포터블 헤드리스 | doc/xls/ppt → 순정 라이브러리보다 안정적 — Phase 3 Office COM 실측 검토 후 확정 (9.1절 참고) |
| OCR | — (미구현) | PRD·TASK 어느 문서에도 포함된 적 없는 항목이었다 — 초기 구상 표에만 있었고 실행 계획에 오른 적이 없다. 스캔 PDF는 텍스트를 추출하지 못한다. 필요해지면 별도 Phase로 새로 계획할 것 [2026-08-20 정정] |
| 인코딩 감지 | chardet | 한글 txt는 CP949/EUC-KR 대응 필수 |
| 한국어 문장 분리 | **정규식** (kss는 옵션) | kss는 품질이 낫지만 53자/초로 실사용 불가 — Phase 3 실측 후 변경 (7장 참고) |
| 키워드 검색 | SQLite FTS5 | BM25 기반 즉시 필터링 |
| 벡터 저장/검색 | **SQLite BLOB** (기존 인덱스 DB에 통합) | ANN 미사용·ID 기반 조회 방식이라 별도 벡터DB가 불필요 — Phase 3 실측 후 ChromaDB에서 변경 (5.1절 참고) |
| 임베딩 모델 | ko-sroberta-multitask(경량, 최소 사양) / KURE-v1(권장, 권장 사양) | int8 ONNX, 사양별 차등 적용 (8장 참고). ko-sbert-nli는 초기 구상 단계에서 빠지고 채택되지 않았다 |
| 임베딩 추론 최적화 | ONNX + int8 양자화 | PyTorch 대비 체감 2~4배 향상. CPU별 양자화 편차 실측됨 — 인덱스는 만든 PC에서만 써야 한다(Phase 3) |
| sLM (옵션) | Qwen3.5-4B(권장 사양 채택) / EXAONE-4.0-1.2B(최소 사양 채택) — GGUF Q4_K_M | llama.cpp 기반 추론. Phase 6~7 실측 후 채택 — 초기 구상이던 Qwen2.5/EXAONE-3.5/Phi-3.5-mini는 2024년 기준이라 전부 교체됐고, 후속 측정에서 Phi-4-mini(준수율 문제)·EXAONE-3.5-7.8B(메모리 2배)는 제외됐다 [2026-08-20 정정] |
| 패키징 | PyInstaller + Inno Setup | exe 인스톨러, 압축 옵션 적용 — Phase 9 미착수(계획만 존재) |
| 폴더 감시(옵션) | watchdog | 실시간 증분 갱신용, 리소스 부담 고려해 옵션 처리 — Phase 8 T8.5에서 구현 완료 |

## 3. 문서 형식별 파싱 전략

| 형식 | 처리 방식 | 비고 |
|---|---|---|
| docx, xlsx, pptx | `python-docx`, `openpyxl`, `python-pptx` | 순정 라이브러리 |
| pdf | `PyMuPDF` | 스캔본(이미지만 있는 PDF)은 텍스트 추출이 안 된다 — OCR 미구현 (2장 참고) |
| txt | `chardet`로 인코딩 자동 감지 후 파싱 | CP949/EUC-KR 대응 |
| hwp | `pyhwp`(hwp5txt) | 한/글 프로그램 미설치 환경에서도 파싱 가능 |
| hwpx | 자체 XML 파싱 | zip+XML 구조, docx와 유사하게 처리 |
| doc, xls, ppt (구버전) | LibreOffice 포터블 헤드리스 변환 후 파싱 | 인스톨러에 내장, +300~600MB |

### 3.1 표/이미지 보존 규칙
- **표**: 행·열 구조를 유지한 채 별도 저장. 문단 텍스트와 합쳐 하나의 청크로 만들면 구조가 소실되므로 반드시 분리
- **이미지·도면**: 삽입된 실제 이미지는 그대로 추출. 벡터 도형으로 구성된 다이어그램은 이미지 파일로 추출되지 않으므로 해당 페이지를 렌더링해 캡처 (PDF: PyMuPDF 페이지 렌더링 / docx·pptx: LibreOffice 헤드리스 변환을 페이지·슬라이드 캡처 용도로 확장)

## 4. 인덱싱 설계

### 4.1 처리 흐름
1. 지정 폴더 전체 스캔
2. 파일별 제목(파일명/문서 내 제목 태그/최초 헤딩), 핵심 키워드, 본문 청크 추출
3. FTS5(키워드/제목)와 벡터DB(Chroma)에 동시 저장

### 4.2 청크 스키마
모든 청크에 `type: text | table | image` 필드를 태깅한다. 검색 로직은 타입과 무관하게 동일하게 동작하며, **화면 렌더링 단계에서만** 이 필드로 분기한다 (7장 참고).

권장 청크 메타데이터:

```
chunk_id, doc_id, file_path, file_name, type(text|table|image),
page_or_slide, content, keywords, embedding_vector,
created_at, source_mtime, source_hash
```

### 4.3 표 청크 특이사항
표 자체는 텍스트 밀도가 낮아 벡터 유사도만으로는 검색이 누락되기 쉽다. 캡션/헤더 텍스트를 키워드 인덱스(FTS5)에 강하게 반영해 보완한다.

### 4.4 이미지 썸네일 캐시
- 벡터/키워드 인덱스와 별도로, 캡처된 페이지 이미지를 축소판(너비 300px 내외)으로 생성해 로컬 캐시
- 검색 시점에 매번 캡처하지 않고 캐시만 조회 → 속도 확보
- 원본 변경 시 증분 갱신 로직에 따라 캐시도 함께 갱신

### 4.5 증분 갱신
- 파일별 `mtime`, 해시값을 DB에 기록
- 재실행 시 변경/신규 파일만 재인덱싱 (썸네일 캐시 포함)
- `watchdog` 기반 실시간 폴더 감시는 옵션으로 고려 (최소 사양 리소스 부담 고려, 로드맵 8단계에서 검토)

### 4.6 백그라운드 처리
인덱싱은 별도 스레드에서 진행 바와 함께 수행해 메인 UI가 멈추지 않도록 한다.

### 4.7 성능 최적화
임베딩 모델을 ONNX + int8 양자화로 변환하면 순수 PyTorch 대비 체감 2~4배 속도 향상을 기대할 수 있다.

## 5. 검색 파이프라인

### 5.1 하이브리드 2단계 검색
1. **1단계 — 키워드 필터(BM25, FTS5)**: 즉시 후보군 축소
2. **2단계 — 벡터 재순위**: 1단계 후보 내에서 코사인 유사도 기반 정밀 재순위

> **설계 근거**: ANN(근사 최근접 탐색)이 아닌 **후보 내 직접 코사인 유사도 계산** 방식을 사용한다. 즉 벡터 저장소에 필요한 기능은 `chunk_id → 벡터` 조회 하나뿐이며, ANN 인덱스·컬렉션 관리·서버 모드는 쓰지 않는다.
>
> **결정 변경 (Phase 3)**: 원안은 ChromaDB였으나 **SQLite BLOB**으로 변경했다. 실측 결과 ChromaDB는 `kubernetes`·`grpcio`·`bcrypt`·onnxruntime 중복본을 포함해 **79개 패키지(75MB)** 를 끌고 오는데, 그중 실제로 쓰는 기능은 ID 조회뿐이었다. FTS5 인덱스와 같은 DB 파일에 `chunk_vectors` 테이블을 두면 ① 추가 의존성이 0개이고 ② 후보 조회가 같은 커넥션에서 끝나며 ③ 문서 삭제 시 CASCADE로 벡터까지 함께 정리된다. TECH 9.2의 인스톨러 예산(800MB~1.2GB)에도 유리하다.
>
> 벡터는 저장 시점에 L2 정규화해두므로 검색 시 코사인 유사도는 **내적 한 번**으로 계산된다.

### 5.2 할루시네이션 방지 — 추출형 검색을 기본값으로
sLM이 문장을 새로 생성하는 순간 할루시네이션 위험이 발생하므로, **기본 모드는 원문 구절을 그대로 발췌·하이라이트하는 방식**으로 설계한다. sLM 기반 요약/답변 생성은 사용자가 명시적으로 켜는 "AI 요약 보기" 옵션으로 분리한다.

### 5.3 sLM 답변 생성 4단계 안전장치 (옵션 모드)

| 단계 | 내용 | 효과 |
|---|---|---|
| 1. 유사도 임계값 필터링 | 검색 점수가 기준(예: 코사인 0.5) 미달이면 sLM 호출 자체를 생략, "관련 문서를 찾을 수 없습니다" 응답 | 근거 없는 답변 생성을 원천 차단 |
| 2. 근거 강제 프롬프트 | "주어진 문서 내용에서만 답하고, 없으면 반드시 모른다고 답하라" 시스템 프롬프트 + low temperature(0~0.2) | 문서 밖 지식 사용 억제 |
| 3. 문장 단위 출처 표기 | 각 문장마다 `[파일명, 페이지/슬라이드 번호]` 형태로 근거 위치 표시 | 사용자가 즉시 원문 대조 가능 |
| 4. 답변-근거 교차 검증 (권장 사양 전용) | 생성 답변 문장과 실제 근거 청크 간 겹침도를 사후 계산, 낮으면 "확인 필요" 표시 | 자동 이중 검증 |

**설계 철학**: 완전 자동으로 할루시네이션을 100% 막을 수는 없다는 전제 하에, "답변을 사용자가 즉시 검증할 수 있는 구조"를 최우선으로 한다.

> **주의**: sLM은 대형 LLM 대비 instruction following 능력 편차가 크다. 2번 안전장치 준수율은 모델마다 달라 설계만으로 보장할 수 없으며, 후보 모델별 실측 검증이 필요하다 (TASK 문서 Phase 6 참고).

## 6. UI 컴포넌트 구조

### 6.1 메인 검색 화면
- 상단 검색바 + **"AI 요약 보기" 토글** (기본 OFF)
- 좌측 필터 패널: FTS5 인덱스 메타데이터(문서 형식, 폴더, 기간) 기반 후보군 축소
- 하단: 인덱싱 상태(문서 수, 마지막 갱신 시각) + 폴더 관리 진입점

### 6.2 결과 카드 컴포넌트

| 유형 | 표시 내용 | 비고 |
|---|---|---|
| 텍스트 카드 | 발췌문 + 검색어 하이라이트 | 기본 결과 유형 |
| 표 카드 | 원본 표 구조 렌더링 + "표 복사" 버튼 | 캡션/헤더 매치로 상위 노출된 경우 |
| 이미지 카드 | 캡처 썸네일 + "확대" 버튼 | 텍스트 미인식 가능성 안내 문구 포함 |

공통 요소: 파일명 + 위치(페이지/시트/슬라이드) + "원문 열기" 버튼. 유사도 임계값 미만 결과는 카드를 흐리게 표시.

### 6.3 결과 렌더러 동적 라우팅

```
검색 결과 청크 → 타입 판별(text/table/image)
   ├─ text  → 텍스트 카드
   ├─ table → 표 카드
   └─ image → 이미지 카드
                 └─ 하나의 결과 리스트에 함께 표시
```

검색 로직 자체는 타입 무관 동일 동작, 렌더링 단계에서만 분기. 신규 결과 유형(수식, 코드 블록 등) 추가 시 렌더러만 확장하면 되는 구조.

## 7. 한국어 처리 최적화

- **임베딩 모델**: `nlpai-lab/KURE-v1`(검색 특화) 또는 `jhgan/ko-sroberta-multitask` 우선 고려. 최소 사양은 경량 모델(`ko-sbert-nli` 등)
- **문장 분리**: **정규식 기반 청킹** (마침표류 + 닫는 인용/괄호 + 줄바꿈). 원안은 `kss`였으나 Phase 3에서 실측한 결과 **8,267자에 157초(53자/초)** 로, 10만 자 문서 하나에 31분이 걸려 PRD 5.1의 "최초 실행 시 전체 인덱싱"이 성립하지 않는다. kss가 권장하는 C++ 백엔드(mecab)를 쓰면 빨라지지만 Windows 설치가 까다로워 PRD 4장의 "관리자 권한 불필요"·9장 포터블 배포와 충돌한다. `use_kss=True`로 여전히 선택 가능하다
- **청크 크기**: 임베딩 모델의 입력 한계(128토큰)를 **토큰 수 기준**으로 지킨다. 문자 수는 대리 지표가 될 수 없다 — 실측한 자/토큰 비율이 0.50~2.70으로 5배 넘게 흔들려(한국어 산문은 느슨하고, 번호·기호·영문이 섞인 표는 조밀하다) 같은 300자가 107토큰이 되기도 146토큰이 되기도 한다. 초과분은 토크나이저가 **경고 없이 잘라내** 키워드 검색과 벡터 검색이 서로 다른 범위를 보게 된다
- **sLM 선택(옵션 모드)**: EXAONE(한국어 특화) 또는 Qwen2.5 계열 우선 검토. `Phi-3.5-mini`(3.8B)는 크기 대비 지시 준수·추론 능력이 좋아 최소~권장 중간 지점 후보로 함께 검토
- **인코딩**: 파일 내용뿐 아니라 파일/폴더 경로의 한글 처리도 별도 테스트 필요

## 8. 사양별 모델 구성

기준기: **최소 사양 = i5-8265U(4C/8T) / 8GB / GPU 없음** (PRD 4장 확정, 실측용 실기). 권장 사양 = Ultra 5 125U(12C/14T) / 16GB / GPU 없음.

| 구성 요소 | 최소 사양 (8GB, GPU 없음) | 권장 사양 (16GB, GPU 없음) |
|---|---|---|
| 임베딩 모델 | 경량 한국어/다국어 모델 (~100M) | KURE-v1 등 한국어 특화 중형 모델 |
| sLM (선택) | Qwen2.5-1.5B GGUF Q4 또는 미사용 | Qwen2.5-7B / EXAONE / Phi-3.5-mini GGUF Q4_K_M |
| 기본 동작 | 추출형 검색만 (sLM 기본 OFF) | 추출형 + sLM 요약 기본 제공 가능 |

설정 파일로 "경량 모드 / 권장 모드"(이전 이름 "고성능 모드", 2026-08-11 변경)를 토글하는 구조로 **단일 코드베이스**를 유지한다.

## 9. 배포 아키텍처

### 9.1 패키징 원칙
- PyInstaller로 exe화, 임베딩 모델·LibreOffice 포터블 포함 (sLM은 별도 분리, 9.3절 참고)
- **포터블 구조 원칙**: 실행 경로, 모델 경로, 인덱스/캐시 경로 모두 실행 파일 기준 **상대 경로**로 구성. 레지스트리 등 설치 PC 종속 요소 배제 → 폴더 전체 압축 이동만으로 동일 동작
- 설치 경로: 관리자 권한 불필요한 사용자 폴더(`%LOCALAPPDATA%` 등)

#### 🔴 구버전 포맷(doc/xls/ppt) 변환 — LibreOffice 포터블 동봉으로 확정 [사용자 확정, 2026-08-08]

`parser/utils/libreoffice.py`가 실행 파일을 찾는 순서는 환경변수 → PATH → 시스템 설치 경로 → **`vendor/LibreOfficePortable/App/libreoffice/program/soffice.exe`**(포터블, 코드 이미 존재)다. 배포 시 이 경로에 LibreOffice 포터블(PortableApps 배포판)을 **압축만 풀어 넣으면** 별도 코드 없이 동작한다 — 실측 확인: 이 위치에 포터블을 넣은 뒤 실문서 `.doc`·`.xls`를 재인덱싱하니 그동안 0청크였던 두 파일이 각각 13청크·1청크로 정상 추출됐다.

**Office COM(Word/Excel/PowerPoint 자동화)을 대안으로 검토했으나 채택하지 않는다.** 사내 PC에는 MS Office가 항상 설치돼 있다고 간주할 수 있어(사용자 확정) COM이 기술적으로는 가능하고, 실측으로도 확인했다:
- 변환 결과(`.doc`→`.docx`, `.xls`→`.xlsx`)를 기존 파서(`docx_parser`/`xlsx_parser`)에 그대로 넣어 정상 파싱됨을 확인 — 하류 파이프라인 변경이 필요 없다
- 메일 첨부·다운로드 파일에 붙는 인터넷 출처 표시(MOTW, `Zone.Identifier`)가 있어도 COM `Documents.Open()`은 보호된 보기를 거치지 않고 정상 동작(실측)
- **단, 파일마다 새 프로세스를 띄우면 건당 7.0초**로 LibreOffice(2.47초, Phase 1 실측)보다 느리다. Word/Excel 인스턴스를 인덱싱 세션 내내 재사용하면 건당 0.5초까지 줄지만, 그러려면 상주 프로세스 수명 관리(`slm/runtime.py`의 `llama_server()`와 같은 패턴)와 좀비 프로세스 처리(실측 중 `Quit()` 후에도 EXCEL.exe가 살아남는 경우를 실제로 겪었다 — 중간 COM 객체 참조를 놓아주지 않으면 발생)를 새로 구현해야 한다

**결론: 지금은 구현 비용 대비 이득이 크지 않아 보류한다.** LibreOffice 포터블 동봉은 이미 존재하는 코드로 오늘 검증까지 끝났고, 배포 용량이 실제 걸림돌이 되는 시점(9.2절 예상 용량 참고)에 Phase 9/10에서 COM 상주 프로세스 방식을 재검토한다. 도입 시에도 완전 대체가 아니라 "COM 우선 시도 → 실패 시 LibreOffice 폴백" 구조를 검토할 것(Phase 10 T10.1의 "원문 열기" 딥링크 조사가 같은 폴백 구조를 이미 채택했다).

**LibreOffice가 없을 때의 사용자 안내 [사용자 확정]**: 자동 다운로드·설치는 넣지 않는다(PRD 4장의 완전 오프라인·무관리자권한 전제와 배치된다 — 앱 내 다운로드는 인터넷을 전제하고, 정식 설치본은 관리자 권한을 요구한다). `soffice`를 찾지 못하면 **"LibreOffice 포터블을 직접 받아 `vendor/LibreOfficePortable/`에 넣으라"는 안내만 보여준다.** 현재 `LegacyOfficeParser`는 변환 실패를 `IndexReport.failures`에 담아 반환하지만, UI(`_on_indexing_done()`)가 이를 아직 화면에 표시하지 않는다 — 안내 문구 구현은 별도 작업으로 남는다(아래 TASK 참고).

### 9.2 예상 배포 용량

| 구성 요소 | 예상 용량 |
|---|---|
| Python 런타임 + PyInstaller 번들 | 150~250MB |
| 문서 파싱 라이브러리 일체 | 100~150MB |
| LibreOffice 포터블 | 300~600MB (동봉 확정, 9.1절) |
| 임베딩 모델 (ONNX) | 100~250MB |
| 벡터DB 라이브러리 | 20~50MB |
| sLM (선택, GGUF) | 0 ~ 4.5GB |

| 시나리오 | sLM | 예상 인스톨러 크기 |
|---|---|---|
| 최소 사양용 (추출형만) | 미포함 | 약 800MB ~ 1.2GB |
| 최소 사양용 + sLM (EXAONE-4.0-1.2B Q4_K_M, 최소 사양 채택) | 약 0.8GB | 약 1.6 ~ 2.0GB |
| 권장 사양용 + sLM (Qwen3.5-4B Q4_K_M, 권장 사양 채택) | 약 2.7GB | 약 3.5 ~ 4.5GB |

sLM 후보명은 Phase 6~7 실측 후 교체됐다(2장 표 참고) — 초기 구상이던 Phi-3.5-mini·Qwen2.5-7B/EXAONE은 2024년 기준이라 전부 다른 모델로 바뀌었고, 후속 측정에서 제외된 Phi-4-mini·EXAONE-3.5-7.8B(4.77GB)는 배포 시나리오에도 올리지 않는다 [2026-08-20 정정].

인덱스/캐시는 원본 문서 대비 약 10~20% + 이미지 썸네일 캐시(문서량 비례). Inno Setup 압축 적용 시 실배포 크기 20~30% 추가 절감 가능(코드/텍스트 리소스는 효과 큼, LibreOffice·GGUF는 이미 압축된 바이너리라 효과 제한적).

### 9.3 모델 분리 다운로드 흐름
sLM처럼 용량이 큰 모델은 인스톨러에서 분리해 사용자가 필요 시 개별 다운로드하는 구조로, 인스톨러는 항상 800MB~1.2GB 수준으로 유지한다.

- **모델 매니저 화면**: sLM은 "다운로드 필요"/"설치됨" 상태로 노출. 임베딩 모델은 사양별로 다르다 — 경량(`ko-sroberta-multitask`)은 인스톨러에 포함되어 항상 "설치됨"이지만, 권장(`KURE-v1`)은 원본이 2.27GB인 데다 ONNX 미제공이라 sLM과 동일하게 별도 다운로드 대상이다(Phase 3 결정, Phase 4 UI 반영). KURE-v1의 "다운로드" 동작은 safetensors→ONNX 변환 파이프라인이 아직 없어 **"준비 중"으로만 노출**하며, 실제 다운로드는 그 파이프라인을 구현하는 후속 작업으로 미룬다
- **다운로드 안내 팝업**: 링크, 파일명, 예상 용량, 체크섬(SHA256), 저장 위치 표시
- **경로 접근성**: 숨겨진 설치 경로를 "폴더 열기" 버튼으로 탐색기에 바로 노출
- **검증**: "새로고침(파일 확인)" 버튼으로 재스캔·체크섬 검증 후 통과 시 자동 활성화 (상시 watchdog 감시는 최소 사양 리소스 부담 고려해 최초 버전에서는 미채택)
- **인터넷 PC 부재 시 대안**: 대표 1인이 다운로드 후 사내 공유 폴더에 업로드, 나머지는 동일 흐름 반복

### 9.4 대표 배포 방식 (포터블 패키지)
포터블 구조 원칙 덕분에 별도 개발 없이 지원 가능:

1. 대표자가 설치 → 모델 매니저에서 sLM 다운로드 → 정상 동작 확인 → 프로그램 폴더 전체 압축(zip)
2. 팀원은 압축 해제 후 즉시 실행 (설치/다운로드 절차 불필요)
3. 압축 해제 시 `models` 폴더에 파일이 이미 존재하므로 모델 매니저가 자동으로 "설치됨" 인식
4. 대상 폴더는 팀원마다 개별 지정. 동일 공유 폴더(사내 네트워크 드라이브)를 지정하는 경우 기존 인덱스를 그대로 재사용해 최초 인덱싱 시간 절약 (증분 갱신 로직이 파일 경로·해시 기준으로 동작하기 때문)
5. 이 경우 배포 파일 크기는 모델 포함 시나리오(약 3.5~6.5GB)에 해당하며, 사내 공유 채널을 통한 1회성 전달을 전제로 채택

## 10. 오픈 이슈 / 추가 검증 필요 사항

| 이슈 | 검증 방법 |
|---|---|
| sLM별 instruction following 편차 | 2~3개 후보 모델로 "문서에 없으면 모른다고 답하는가" 준수율을 사내 문서 샘플로 실측 (TASK Phase 6) |
| 구버전 포맷(doc/xls/ppt) 변환 안정성 | **부분 해소 (2026-08-08)**: 실문서 `.doc`/`.xls` 각 1건을 LibreOffice 포터블 동봉 환경에서 변환·인덱싱 성공 확인(13청크·1청크). `.ppt` 및 대량 표본 실패율은 아직 미검증 — Phase 9 패키징 시 재확인 |
| 파일/폴더 경로 한글 처리 | 별도 환경 테스트 필요 |
| ~~ChromaDB vs FAISS 최종 확정~~ | **해소됨 (Phase 3)**: 둘 다 채택하지 않고 SQLite BLOB으로 확정. ANN을 쓰지 않아 벡터DB가 불필요함을 실측으로 확인 (5.1절 참고) |
| 한국어 문장 분리 라이브러리 | **해소됨 (Phase 3)**: `kss`가 53자/초로 실사용 불가(8,267자에 157초)해 정규식 분리로 대체. 7장 참고 |
