# PRD: RAG와 sLM을 활용한 오프라인 문서 검색 시스템

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 기준 문서 | 오프라인_RAG_시스템_설계문서.md |
| 문서 목적 | 제품 관점의 요구사항 정의 (기술 구현 상세는 TECH 문서 참고) |

---

## 1. 배경 및 문제 정의

사내에는 doc/docx/pdf/xls/xlsx/ppt/pptx/txt/hwp/hwpx 등 다양한 형식의 문서가 폴더 단위로 흩어져 있으며, 특정 정보를 찾기 위해 파일을 하나씩 열어 확인해야 하는 비효율이 존재한다. 보안/네트워크 정책상 외부 클라우드 기반 검색·AI 서비스를 사용할 수 없는 환경이므로, **완전 오프라인으로 동작하는 사내 문서 검색/RAG 시스템**이 필요하다.

## 2. 제품 목표

완전 오프라인 환경(인터넷 연결 불필요)에서 사내 문서를 빠르게 검색하고, 필요 시 sLM을 통해 근거 기반 요약 답변을 받을 수 있는 도구를 팀 전체에 배포한다.

## 3. 핵심 설계 원칙

제품의 모든 의사결정은 아래 3가지 원칙에 우선순위를 둔다.

1. **검색 속도 우선**: 질문마다 문서 전체를 재검색하지 않고, 사전 구축된 인덱스만 조회
2. **완벽한 한국어 지원**: 문장 분리, 임베딩, 인코딩 처리 전반에 한국어 특화 처리 적용
3. **문서 근거 없는 답변(할루시네이션) 원천 차단**: 추출형 검색을 기본값으로 하고, AI 답변 생성은 근거가 확실할 때만 옵션으로 제공

> **용어 정정**: 답변 생성에 쓰이는 모델(Qwen2.5, EXAONE 등)은 파라미터 규모(1B~10B급) 기준 정확히는 **sLM(Small Language Model)**이다. 본 문서에서는 표기를 sLM으로 통일한다.

## 4. 타겟 사용자 및 사용 환경

- **사용자**: 사내 팀원 전체 (비개발자 포함, IT 승인 절차 없이 개별 PC에 설치 가능해야 함)
- **하드웨어**:

| 구분 | CPU | RAM | GPU |
|---|---|---|---|
| **최소 사양** | **i5-8265U (8세대 U형, 4코어/8스레드, 1.60GHz)** | **8GB** | 없음 |
| 권장 사양 | i5-1240P / Ultra 5 125U | 16GB | 없음 |

> **최소 사양 기준기는 실기다 [확정, 2026-08-07].** 원안은 "i5 9세대"였으나, 실제 최소 사양 검증에 쓰는 PC(i5-8265U / 8GB, 실측 가용 RAM 7.85GB)를 그대로 기준으로 삼는다. 9세대 데스크톱보다 **낮은** 저전력 U형이라 기준을 완화하는 것이 아니라 더 보수적으로 잡는 것이고, 무엇보다 성능 수치를 실측할 수 있는 기계여야 기준으로서 의미가 있다. 이후 모든 "최소 사양 실측"은 이 사양에서 잰 값을 뜻한다.

- **네트워크**: 완전 오프라인 전제. 인터넷 다운로드를 전제하지 않는 배포 채널(사내 공유 드라이브, USB)만 사용
- **설치 권한**: 관리자 권한 불필요 (사용자 폴더 설치)

## 5. 기능 요구사항

### 5.1 문서 인덱싱
- 지정 폴더를 스캔하여 지원 형식(doc, docx, pdf, xls, xlsx, ppt, pptx, txt, hwp, hwpx) 문서를 자동 인식·처리
- 파일명/제목/키워드/본문을 추출해 키워드 인덱스와 벡터 인덱스에 동시 저장
- 표와 이미지는 일반 텍스트로 뭉개지 않고 구조를 보존해 별도 저장
- 인덱싱은 백그라운드에서 진행 바와 함께 수행되어 UI를 멈추게 하지 않음
- 최초 실행 시 전체 인덱싱, 이후에는 변경/신규 파일만 재인덱싱(증분 갱신)

### 5.2 검색
- 키워드 검색과 벡터 검색을 결합한 하이브리드 검색 제공
- 결과는 **기본적으로 원문 발췌(추출형)** 로 표시되며, 검색어가 하이라이트됨
- 검색 전 필터 패널을 통해 문서 형식/폴더/기간으로 후보군을 좁힐 수 있음
- 유사도가 낮은 결과는 카드가 흐리게 표시되어 "관련성 낮음"을 시각적으로 안내

### 5.3 결과 표시 (3종 카드)

| 유형 | 표시 내용 |
|---|---|
| 텍스트 결과 | 발췌문 + 검색어 하이라이트 |
| 표 결과 | 원본 표 구조 그대로 렌더링 + "표 복사" 버튼 |
| 이미지 결과 | 캡처된 페이지 축소판 + "확대" 버튼 (텍스트 미인식 가능성 안내) |

모든 카드는 공통적으로 **파일명 + 위치(페이지/시트/슬라이드) + "원문 열기" 버튼**을 포함해 사용자가 즉시 원문을 대조할 수 있어야 한다.

### 5.4 AI 요약 (옵션 모드)
- 검색바 옆 **"AI 요약 보기" 토글**로 명시적으로 켜야만 동작 (기본값 OFF)
- 유사도 임계값 미달 시 sLM을 호출하지 않고 "관련 문서를 찾을 수 없습니다" 응답
- 답변은 문서 내용에서만 생성되며, 각 문장마다 `[파일명, 페이지/슬라이드 번호]` 형태로 근거를 표기
- 권장 사양에서는 답변-근거 교차 검증 결과에 따라 "확인 필요" 표시 제공

### 5.5 모델 관리
- sLM 설치 상태(설치됨/필요)를 보여주는 모델 매니저 화면 제공
- 다운로드 안내 팝업(링크, 파일명, 예상 용량, 체크섬)과 "폴더 열기" 버튼 제공
- 파일을 수동으로 옮긴 뒤 "새로고침" 버튼으로 체크섬 검증 및 자동 활성화

### 5.6 배포 및 설치
- exe 인스톨러를 통한 개별 PC 설치 지원
- 프로그램 폴더를 압축해 다른 PC로 옮기기만 해도 동일하게 동작하는 포터블 배포 지원 (대표 1인이 모델까지 받아 설치 완료 후 배포)
- 동일한 공유 폴더를 대상으로 지정하는 팀원은 기존 인덱스를 재사용 가능

## 6. 비기능 요구사항

| 항목 | 요구사항 |
|---|---|
| 오프라인 동작 | 인덱싱·검색·AI 요약 전 과정이 인터넷 연결 없이 동작해야 함 |
| 이식성 | 상대 경로 기반, 레지스트리 미사용 — 폴더 복사만으로 이전 가능 |
| 반응성 | 검색 응답은 인덱스 조회 기반으로 체감 지연이 없어야 함 (8GB RAM 환경 포함) |
| 안정성 | 구버전 바이너리 포맷(doc/xls/ppt) 파싱 실패율 최소화 |
| 신뢰성 | 근거 없는 AI 답변이 사용자에게 그대로 노출되지 않아야 함 |
| 확장성 | 신규 결과 유형(수식, 코드 블록 등) 추가 시 렌더러만 확장하면 되는 구조 |

## 7. 범위 정의

### In Scope (MVP, 로드맵 1~4단계)
- 문서 파서(pdf/docx/txt 우선, xlsx/pptx 포함)
- FTS5 키워드 인덱싱
- 임베딩 기반 벡터 재순위
- 추출형 검색 UI (텍스트 카드)

### In Scope (MVP 이후 확장, 로드맵 5~9단계)
- 표/이미지 카드 렌더러
- sLM 후보군 실측 검증 및 AI 요약 옵션 모드
- 증분 인덱싱 / 폴더 감시
- exe 패키징 및 개별 PC 배포(포터블 배포 포함)

### Out of Scope
- 클라우드 기반 검색/AI 서비스 연동
- 대형 LLM(10B 초과) 사용
- 실시간 다중 사용자 협업 기능
- GPU 의존적 처리 경로

## 8. 성공 기준 (제안)

설계문서에 정량 지표가 명시되어 있지 않아, 아래는 로드맵 목표에 기반한 제안 기준이다. 실측 후 조정이 필요하다.

- 최소 사양(8GB, GPU 없음)에서 인덱싱·검색이 UI 멈춤 없이 동작
- 추출형 검색만으로 실무 사용 가능한 MVP를 로드맵 4단계 시점에 확보
- sLM 옵션 모드에서 "문서에 없으면 모른다고 답하는가" 준수율을 후보 모델별로 실측해 선정 근거 확보
- 포터블 압축 배포 시 팀원 PC에서 재설치 절차 없이 즉시 구동

## 9. 리스크 및 제약사항

| 리스크 | 내용 |
|---|---|
| sLM instruction following 편차 | 모델마다 "모른다고 답하라" 지시 준수율이 달라 설계만으로 보장 불가 → 실측 검증 필요(로드맵 6단계) |
| 구버전 포맷 파싱 | doc/xls/ppt는 LibreOffice 헤드리스 변환에 의존 — 변환 실패 케이스 대응 필요 |
| 인스톨러 용량 | LibreOffice 포함 시 최소 800MB~1.2GB, sLM 포함 시 최대 6.5GB까지 증가 |
| 하드웨어 제약 | 전 기능이 CPU-only, GPU 없는 환경 기준으로 설계되어야 함 |
