# PROMPTS: 단계별 Claude Code 실행 프롬프트

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 참조 문서 | PRD_오프라인RAG시스템.md / TECH_오프라인RAG시스템.md / TASK_오프라인RAG시스템.md |
| 사용 방법 | 각 Phase 프롬프트를 그대로 Claude Code에 붙여넣기. `@파일명`은 프로젝트 루트 기준 상대 경로에 맞게 조정 |

---

## 사용 전 공통 안내

- 모든 프롬프트는 `@PRD_오프라인RAG시스템.md @TECH_오프라인RAG시스템.md @TASK_오프라인RAG시스템.md`로 세 문서를 함께 주입한다. 문서가 큰 경우 세션 컨텍스트 절약을 위해 TASK 문서에서 **해당 Phase 섹션만** 별도로 잘라 injection해도 무방하다.
- 각 프롬프트 끝에는 "완료 후 TASK 문서의 해당 체크박스를 갱신하고 CLAUDE.md에 진행 상황을 한 줄로 요약해줘"를 공통으로 포함한다. CLAUDE.md를 세션 간 포인터로 유지하는 기존 워크플로우와 이어지도록 하기 위함이다.
- Phase 4는 확정된 UI 목업(사이드바형 v3) 구조를 별도로 프롬프트에 명시했다. 다른 Phase에서 UI 관련 세부 작업이 생기면 동일하게 목업 구조를 프롬프트에 붙여 넣을 것.

## 모델 추천 (Sonnet 5 / Opus 5 조합)

토큰 효율을 위해 **Phase 전체가 아니라 결정 지점 단위로 모델을 전환**한다. 스펙·계획이 이미 확정된 구현 작업은 Sonnet, 되돌리기 비싼 설계 결정이나 여러 문서를 대조해 판단해야 하는 지점만 Opus로 전환한다.

| Phase | 기본 모델 | Opus로 전환할 지점 |
|---|---|---|
| 1. 파서 모듈 | Sonnet | ✅ 완료 — 실환경 검증 시 원인 분석(hwp 이미지 누락 등)은 Opus가 유리했음 |
| 2. FTS5 인덱싱 | Sonnet | **T2.2 스키마 설계** — 대/소문자 구분 토크나이저 결정 (잘못 정하면 인덱스 전체 재생성) |
| 3. 벡터 재순위 | **Opus 비중 ↑** | ONNX 양자화·ChromaDB 스키마 설계 자체가 새 아키텍처 결정. 설계는 Opus, T3.4/T3.8 배선·벤치마크는 Sonnet |
| 4. 추출형 UI | Sonnet | **T4.0 프레임워크 선택** + **DESIGN §8 문서 간 불일치 4건 해소** — 둘 다 되돌리기 비쌈. 이후 컴포넌트 구현은 Sonnet |
| 5. 표/이미지 카드 | Sonnet | 없음 — DESIGN 명세(§5.4~5.5)가 이미 상세해 그대로 구현 |
| 6. sLM 실측 검증 | **Opus 비중 ↑** | 준수율·속도 데이터로 모델을 채택하는 해석 작업. 측정 스크립트 자체는 Sonnet |
| 7. sLM 옵션 모드 | 혼합 | **4단계 안전장치(답변-근거 교차 검증) 설계**는 Opus, 모델 매니저 UI 등 나머지는 Sonnet |
| 8. 증분 인덱싱 | Sonnet | 없음 — mtime/해시 비교 로직이 명확함 |
| 9. 패키징/배포 | Sonnet | 실제 배포 테스트 중 예상 밖 실패가 나오면 그 디버깅만 Opus |

**진행 방식**: 각 Phase 프롬프트를 보내기 전, 위 표의 "Opus로 전환할 지점"에 해당하는 결정이 있으면 그 부분만 먼저 Opus로 확인받고, 이후 구현은 Sonnet으로 이어간다.

---

## Phase 1: 문서 파서 모듈

> **추천 모델**: Sonnet 5 (완료됨)

```
@PRD_오프라인RAG시스템.md @TECH_오프라인RAG시스템.md @TASK_오프라인RAG시스템.md

TASK 문서의 Phase 1(문서 파서 모듈)을 진행해줘.

목표: pdf/docx/txt를 우선 구현하고, xlsx/pptx → hwp/hwpx → 구버전(doc/xls/ppt) 순으로 확장한다.
표/이미지는 텍스트와 분리된 구조로 보존해야 한다 (TECH 문서 3장 참고).

작업 범위 (T1.1~T1.13):
- 프로젝트 스캐폴딩부터 시작해서, 형식별 파서를 TECH 문서 3장의 표에 명시된 라이브러리로 각각 구현해줘.
- 모든 파서의 출력은 TECH 문서 4.2절의 청크 스키마(chunk_id, doc_id, file_path, type: text|table|image 등)를 공통으로 따라야 해.
- 구버전 포맷(doc/xls/ppt)은 LibreOffice 헤드리스 변환 파이프라인으로 처리하고, 변환 실패 시 에러 핸들링을 명확히 해줘.
- 표는 행·열 구조 유지, 이미지·도면은 실제 이미지 추출 + 벡터 도형은 페이지 렌더링 캡처로 구분해서 처리해줘.
- 형식별로 샘플 문서 몇 개를 만들어서 단위 테스트를 작성해줘.

먼저 T1.1~T1.13 작업을 어떤 순서/모듈 구조로 나눌지 계획을 제시하고, 내 확인을 받은 뒤 구현을 시작해줘.

완료 후 TASK 문서의 해당 체크박스를 갱신하고 CLAUDE.md에 진행 상황을 한 줄로 요약해줘.
```

---

## Phase 2: 폴더 스캔 + FTS5 키워드 인덱싱

> **추천 모델**: Sonnet 5 — 단, **T2.2 스키마 설계**(대/소문자 구분 토크나이저 결정)는 먼저 Opus 5로 확인받고 시작할 것. 잘못 정하면 인덱스 전체 재생성이 필요하다.

```
@PRD_오프라인RAG시스템.md @TECH_오프라인RAG시스템.md @TASK_오프라인RAG시스템.md

TASK 문서의 Phase 2(폴더 스캔 + FTS5 키워드 인덱싱)를 진행해줘. Phase 1의 파서 출력 스키마를 그대로 입력으로 사용한다.

목표: sLM/임베딩 없이 키워드 검색만으로 먼저 동작을 확인한다.

작업 범위 (T2.1~T2.9):
- 폴더 재귀 스캔 + 지원 확장자 필터링 로직을 만들어줘.
- SQLite FTS5 스키마를 TECH 문서 4장 기준으로 설계해줘 (문서/청크 테이블 분리, type 필드 포함).
- kss 기반 한국어 문장 분리로 청킹하는 로직을 붙여줘.
- 표 청크는 캡션/헤더 텍스트를 키워드 인덱스에 더 강하게 반영해줘 (TECH 문서 4.3절).
- BM25 검색 쿼리 함수를 구현하고, 백그라운드 스레드 + 진행 바 연동까지 포함해줘.
- 마지막에 임시 CLI든 뭐든 좋으니 키워드 검색만 단독으로 동작하는 걸 눈으로 확인할 수 있게 해줘.

이미 진행 중인 부분이 있다면 지금까지 만든 코드를 먼저 살펴보고, 어디까지 됐는지 요약해줘.

완료 후 TASK 문서의 해당 체크박스를 갱신하고 CLAUDE.md에 진행 상황을 한 줄로 요약해줘.
```

---

## Phase 3: 임베딩 모델 연동 + 벡터 재순위

> **추천 모델**: **Opus 5 비중 높임** — ONNX 양자화·ChromaDB 스키마 설계는 새 아키텍처 결정이라 Opus로. 설계가 정해진 뒤 배선(T3.4)·벤치마크 스크립트(T3.8)는 Sonnet으로 전환해도 된다.

```
@PRD_오프라인RAG시스템.md @TECH_오프라인RAG시스템.md @TASK_오프라인RAG시스템.md

TASK 문서의 Phase 3(임베딩 모델 연동 + 벡터 재순위)을 진행해줘. Phase 2의 FTS5 키워드 필터가 1단계로 선행된다는 전제다.

목표: 2단계 하이브리드 검색(키워드 필터 → 벡터 재순위)을 완성한다.

작업 범위 (T3.1~T3.8):
- 임베딩 모델은 최소 사양용 경량 모델과 권장 사양용 KURE-v1을 둘 다 붙일 수 있는 구조로 만들어줘 (설정 파일로 토글).
- ONNX + int8 양자화 변환 파이프라인을 구축해줘.
- ChromaDB 컬렉션 스키마는 chunk_id 기반 ID 조회에 최적화된 구조로 설계해줘 — ANN이 아니라 코사인 유사도 직접 계산 방식이라는 게 핵심이야 (TECH 문서 5.1절 설계 근거 참고).
- FTS5 후보군 → 벡터 재순위로 이어지는 하이브리드 검색 함수를 통합해줘.
- 8GB RAM 환경 기준으로 검색 응답 속도를 벤치마크하는 스크립트도 같이 만들어줘.

완료 후 TASK 문서의 해당 체크박스를 갱신하고 CLAUDE.md에 진행 상황을 한 줄로 요약해줘.
```

---

## Phase 4: 추출형 검색 UI 완성 (MVP 완료 지점)

> **추천 모델**: Sonnet 5 — 단, **T4.0 UI 프레임워크 선택**과 **DESIGN §8 문서 간 불일치 4건 해소**는 먼저 Opus 5로 결정하고 시작할 것. 둘 다 이후 전 컴포넌트 작업의 전제라 되돌리기 비싸다.

```
@PRD_오프라인RAG시스템.md @TECH_오프라인RAG시스템.md @TASK_오프라인RAG시스템.md

TASK 문서의 Phase 4(추출형 검색 UI 완성)를 진행해줘. Phase 3의 하이브리드 검색 함수를 백엔드로 사용한다.

이 Phase까지 완료되면 1차 배포 가능한 MVP로 본다.

확정된 화면 구조 (사이드바 필터형, v3):
- 상단: 검색 입력창
- 좌측 사이드바 (고정폭):
  1. 문서 형식 필터 — 체크박스 그룹 (전체/docx/xlsx/pdf 등)
  2. 검색 옵션 — 토글 스위치 3종: AI 요약 보기 / 대·소문자 구분 / 일치되는 단어 (하나의 토글 컴포넌트를 재사용)
  3. PC 성능 선택 — 콤보박스 (경량 모드 / 고성능 모드)
- 우측 결과 영역: 카드 리스트. 이번 Phase에서는 텍스트 카드만 구현 (표/이미지 카드는 Phase 5)
  - 모든 카드 공통: 파일명 + 위치 + "원문 열기" 버튼
  - 유사도 임계값 미달 카드는 흐림 처리 + "관련성 낮음" 표시
- 하단 상태바: 인덱싱된 문서 수, 마지막 갱신 시각, 폴더 관리 진입점

작업 범위 (T4.0~T4.19, TASK 문서 Phase 4 참고):
- 먼저 T4.0(UI 프레임워크 확정)부터 정해줘 — 웹 임베디드(pywebview/Flet) vs 네이티브(PySide6) 중 이 프로젝트(카드형 UI, 8GB RAM, CPU-only)에 더 맞는 쪽을 근거와 함께 제안해줘.
- 그 다음 레이아웃 셸 → 검색바 → 사이드바(필터/옵션/콤보박스) → 결과 카드 → 하단 상태바 순으로 컴포넌트를 구현해줘.
- "대/소문자 구분"과 "일치되는 단어" 토글은 UI만 만들고 끝나면 안 되고, 실제 FTS5 쿼리 옵션과 연동돼야 해 (T4.8, T4.9).
- "AI 요약 보기" 토글은 이번 Phase에서는 상태만 저장하고 실제 sLM 호출은 Phase 7까지 비활성/placeholder로 둬.

완료 후 TASK 문서의 해당 체크박스를 갱신하고 CLAUDE.md에 진행 상황을 한 줄로 요약해줘.
```

---

## Phase 5: 표 카드 / 이미지 카드 렌더러 추가

> **추천 모델**: Sonnet 5 — DESIGN 문서(§5.4~5.5)가 이미 상세히 명세되어 있어 Opus 전환 지점 없음.

```
@PRD_오프라인RAG시스템.md @TECH_오프라인RAG시스템.md @TASK_오프라인RAG시스템.md

TASK 문서의 Phase 5(표 카드 / 이미지 카드 렌더러 추가)를 진행해줘. Phase 4의 텍스트 카드 렌더링 구조와 Phase 1의 표/이미지 파싱 결과를 함께 사용한다.

목표: 청크 타입(text/table/image) 기반 동적 라우팅 구조를 완성한다 (TECH 문서 6.3절).

작업 범위 (T5.1~T5.6):
- 청크 타입 판별 → 카드 컴포넌트 분기 로직을 구현해줘. 검색 로직 자체는 손대지 말고, 렌더링 단계에서만 분기해야 해.
- 표 카드: 원본 표 구조를 그대로 렌더링하고 "표 복사" 버튼을 붙여줘.
- 이미지 카드: 캡처 썸네일 + "확대" 버튼, 그리고 "이미지 내 텍스트는 인식되지 않았을 수 있습니다" 안내 문구를 넣어줘.
- 이미지 썸네일 캐시(300px 축소판)를 별도로 생성/조회하는 로직도 만들어줘 — 검색 시점마다 캡처하지 않도록.
- 텍스트/표/이미지 결과가 한 리스트에 섞여도 자연스럽게 보이는지 확인해줘.

완료 후 TASK 문서의 해당 체크박스를 갱신하고 CLAUDE.md에 진행 상황을 한 줄로 요약해줘.
```

---

## Phase 6: sLM 후보군 실측 검증

> **추천 모델**: **Opus 5 비중 높임** — 준수율·속도 데이터를 놓고 어느 모델을 채택할지 판단하는 해석 작업이 핵심. 측정 스크립트 자체를 짜는 부분(T6.1~T6.4)은 Sonnet으로 시작해도 된다.

```
@PRD_오프라인RAG시스템.md @TECH_오프라인RAG시스템.md @TASK_오프라인RAG시스템.md

TASK 문서의 Phase 6(sLM 후보군 실측 검증)을 진행해줘. Phase 4의 검색 결과 파이프라인을 근거 데이터로 사용한다.

목표: Qwen2.5-1.5B/7B, EXAONE, Phi-3.5-mini 중 "문서에 없으면 모른다고 답하는가"(근거 강제 프롬프트 준수율)가 가장 안정적인 모델을 실측으로 선정한다 (TECH 문서 5.3절 4단계 안전장치 중 2번).

작업 범위 (T6.1~T6.7):
- 후보 모델의 GGUF Q4/Q4_K_M을 확보하고 llama.cpp 기반 CPU 추론 환경을 구축해줘.
- "주어진 문서 내용에서만 답하고, 없으면 반드시 모른다고 답하라" 프롬프트 템플릿 초안을 만들어줘.
- 검증용 테스트셋을 만들어줘 — 근거가 있는 질문과 근거가 없는 질문을 각각 샘플링해서, 모델이 실제로 "모른다"고 답해야 하는 케이스를 구분해줘.
- 모델별 준수율, 응답 속도, 메모리 사용량을 최소/권장 사양 각각 기준으로 측정해줘.
- 결과를 비교표로 정리하고, 사양별(최소/권장) 최종 채택 모델을 근거와 함께 제안해줘.

완료 후 TASK 문서의 해당 체크박스를 갱신하고 CLAUDE.md에 진행 상황을 한 줄로 요약해줘.
```

---

## Phase 7: sLM 답변 생성 옵션 모드 추가

> **추천 모델**: 혼합 — **4단계 안전장치(답변-근거 교차 검증) 설계**는 Opus 5, 모델 매니저 UI·다운로드 팝업 등 나머지 구현은 Sonnet 5.

```
@PRD_오프라인RAG시스템.md @TECH_오프라인RAG시스템.md @TASK_오프라인RAG시스템.md

TASK 문서의 Phase 7(sLM 답변 생성 옵션 모드 추가)을 진행해줘. Phase 6에서 선정한 모델을 적용하고, Phase 4에서 placeholder로 남겨둔 "AI 요약 보기" 토글을 실제로 연결한다.

목표: TECH 문서 5.3절의 4단계 안전장치를 모두 구현한다.

작업 범위 (T7.1~T7.11):
- 1단계: 유사도 임계값(예: 코사인 0.5) 미달 시 sLM을 아예 호출하지 않고 "관련 문서를 찾을 수 없습니다"를 반환하도록 해줘.
- 2단계: 근거 강제 프롬프트 + low temperature(0~0.2) 설정을 적용해줘.
- 3단계: 답변의 각 문장마다 `[파일명, 페이지/슬라이드 번호]` 형태로 출처를 표기해줘.
- 4단계(권장 사양 전용): 생성된 답변과 실제 근거 청크 간 겹침도를 계산해서 낮으면 "확인 필요"로 표시해줘.
- 모델 매니저 화면(설치 상태 표시), 다운로드 안내 팝업(링크/파일명/용량/SHA256), "폴더 열기"·"새로고침" 버튼까지 TECH 문서 9.3절 흐름대로 구현해줘.
- 마지막으로 Phase 6에서 만든 테스트셋으로 회귀 테스트를 돌려서 품질을 재확인해줘.

완료 후 TASK 문서의 해당 체크박스를 갱신하고 CLAUDE.md에 진행 상황을 한 줄로 요약해줘.
```

---

## Phase 8: 증분 인덱싱 / 폴더 감시 기능

> **추천 모델**: Sonnet 5 — mtime/해시 비교 로직이 명확해 Opus 전환 지점 없음.

```
@PRD_오프라인RAG시스템.md @TECH_오프라인RAG시스템.md @TASK_오프라인RAG시스템.md

TASK 문서의 Phase 8(증분 인덱싱 / 폴더 감시 기능)을 진행해줘. Phase 2의 인덱싱 파이프라인과 Phase 5의 썸네일 캐시를 확장하는 작업이다.

목표: 파일 변경사항만 재인덱싱하고, 나머지는 기존 인덱스를 재사용한다.

작업 범위 (T8.1~T8.6):
- 파일별 mtime·해시를 기록하는 스키마를 추가해줘.
- 재실행 시 변경/신규 파일만 판별하는 로직을 만들고, 변경분만 재인덱싱하는 파이프라인을 구현해줘.
- 이미지 썸네일 캐시도 같이 증분 갱신되도록 연동해줘.
- watchdog 기반 실시간 폴더 감시는 옵션으로 구현하되, 최소 사양(8GB RAM)에서는 기본 OFF로 두는 게 맞는지 검토하고 의견을 줘.
- 대량 문서 중 일부만 변경된 시나리오로 성능 테스트를 해줘 — 전체 재인덱싱 대비 시간이 얼마나 단축되는지 수치로 보여줘.

완료 후 TASK 문서의 해당 체크박스를 갱신하고 CLAUDE.md에 진행 상황을 한 줄로 요약해줘.
```

---

## Phase 9: exe 패키징 및 개별 PC 배포 테스트

> **추천 모델**: Sonnet 5 — 절차가 TECH 9장에 명시되어 있음. 실제 배포 테스트 중 예상 밖 실패가 나오면 그 디버깅만 Opus 5로 전환.

```
@PRD_오프라인RAG시스템.md @TECH_오프라인RAG시스템.md @TASK_오프라인RAG시스템.md

TASK 문서의 Phase 9(exe 패키징 및 개별 PC 배포 테스트)를 진행해줘. Phase 1~8이 모두 끝난 상태를 전제로 한다.

목표: TECH 문서 9장의 배포 아키텍처대로 인스톨러와 포터블 배포본을 완성한다.

작업 범위 (T9.1~T9.10):
- PyInstaller 패키징 스크립트를 작성해줘.
- 실행/모델/인덱스/캐시 경로가 전부 상대 경로인지, 레지스트리를 건드리는 부분이 없는지 검증해줘.
- 설치 경로는 `%LOCALAPPDATA%` 등 관리자 권한이 필요 없는 사용자 폴더로 맞춰줘.
- Inno Setup으로 인스톨러를 빌드하고 압축 옵션을 적용해줘.
- sLM 미포함 최소 인스톨러(약 800MB~1.2GB 목표)를 먼저 만들고 검증해줘.
- 대표 배포용 포터블 패키지 절차(설치 → 모델 다운로드 → 폴더 압축 → 다른 PC에서 압축 해제 → 즉시 실행)를 실제로 재현해서 문제없는지 확인해줘.
- 동일 공유 폴더를 대상으로 지정했을 때 기존 인덱스가 재사용되는지도 같이 확인해줘.

완료 후 TASK 문서의 해당 체크박스를 갱신하고 CLAUDE.md에 진행 상황을 한 줄로 요약해줘.
```
