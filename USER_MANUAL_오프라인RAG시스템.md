# 사용자 매뉴얼 — 오프라인 문서 검색

> 기준: 2026-08-18 (Phase 11-A 시점 코드). 화면 구성·버튼 문구는 실제 코드에서 확인한 것이다.
> 개발 문서(PRD/TECH/DESIGN/PLAN/TASK)와 달리 이 문서는 **쓰는 사람** 기준으로 쓰였다.

---

## 1. 이 프로그램은 무엇인가

내 PC의 폴더 하나를 지정하면, 그 안의 문서를 전부 읽어 **검색할 수 있게 만들어 두고(인덱싱)**,
이후 질문을 입력하면 문서 안에서 관련 부분을 찾아 보여준다.

- **완전 오프라인** — 인터넷 없이 동작한다. 문서가 밖으로 나가지 않는다.
- **2가지 사용법** — ① 검색: 관련 문단·표·이미지를 목록으로 본다. ② AI 챗봇: 질문에 대해 문서를 근거로 답을 만들어 준다.
- **AI는 문서에 없는 내용은 답하지 않는다** — 근거가 없으면 "문서에서 찾을 수 없습니다"라고 기권한다. 지어낸 답을 막기 위한 의도된 동작이다.

### 지원 문서 형식

```
.txt .md .log .csv .rtf
.pdf
.docx .xlsx .xlsm .pptx
.hwp .hwpx
.doc .xls .ppt   (구버전 — 아래 3-3 참고)
```

---

## 2. 실행하기

```bash
./.venv/Scripts/python.exe -m ui.app
```

창이 뜨면 준비 끝이다. 처음 실행하면 검색할 문서가 없으므로 **3장(처음 사용 준비)**부터 진행한다.

---

## 3. 처음 사용 준비

### 3-1. 문서 폴더 지정하고 인덱싱하기

1. 왼쪽 사이드바에서 **문서 관리** 클릭
2. **폴더 선택** 버튼 → "폴더 관리" 창이 뜬다
3. 창 안의 **폴더 선택**으로 검색할 폴더를 고른다 (하위 폴더까지 전부 포함된다)
4. **다시 인덱싱** 클릭

인덱싱이 시작되면 진행률 팝업이 뜬다.

- 지금 처리 중인 파일 경로, **경과 시간**, **예상 남은 시간**이 표시된다
- 이 팝업은 **작업을 막지 않는다** — 인덱싱 중에도 검색·다른 화면 이동이 가능하다
- **취소** 버튼으로 언제든 중단할 수 있다

> 소요 시간: 문서 19건 기준 약 2분(구버전 `.doc`/`.xls` 변환 포함). 한 번 인덱싱한 뒤에는
> **바뀐 파일만** 다시 읽으므로 재인덱싱은 1초 안팎으로 끝난다.

### 3-2. (선택) 실시간 감시 켜기

"폴더 관리" 창의 **실시간 감시** 토글을 켜면, 폴더 안 파일이 추가·수정·삭제될 때마다
자동으로 조용히 재인덱싱한다(팝업 없이 하단 상태바로만 알려준다). 기본값은 꺼짐이다.

### 3-3. 구버전 문서(.doc/.xls/.ppt)가 안 잡힐 때

구버전 Office 형식은 변환기가 필요하다. 변환기가 없으면 상태바에
"구버전 문서를 변환하지 못했습니다(파일명) …" 안내가 뜬다.
`vendor/LibreOfficePortable/` 위치에 LibreOffice 포터블을 넣으면 해결된다(자동 다운로드는 하지 않는다).

> ⚠️ **인덱스는 다른 PC에서 가져올 수 없다.** 문서 폴더를 복사해 오더라도 인덱싱은
> 그 PC에서 새로 해야 한다. 다른 PC에서 만든 인덱스를 쓰면 검색 품질이 조용히 나빠진다.

---

## 4. 화면 구성

```
┌─────────────┬────────────────────────────────────┐
│ 오프라인      │  [검색어: …]                    ✕ │  ← 결과 헤더(검색 결과가 있을 때만)
│ 문서 검색     │                                    │
│             │                                    │
│ 검색/대화  ⌄ │        결과 카드 / 대화             │
│  └ 검색 옵션 │                                    │
│  └ 문서 형식 │                                    │
│ 문서 관리    │                                    │
│ 설정        ├────────────────────────────────────┤
│             │  [질문을 입력하세요]      [보내기]   │  ← 공용 입력창
│ 최근 검색    ├────────────────────────────────────┤
│  · …        │  문서 N개 인덱싱됨 · 마지막 인덱싱 …  │  ← 상태바
└─────────────┴────────────────────────────────────┘
```

**왼쪽 사이드바 = 3개 페이지 이동**

| 항목 | 내용 |
|---|---|
| **검색/대화** | 실제로 검색·대화하는 화면 (기본) |
| **문서 관리** | 대상 폴더 선택·인덱싱 |
| **설정** | PC 성능(임베딩 모델) 선택, 모델 관리 |

**`검색/대화` 오른쪽의 `⌄` 버튼**을 누르면 검색 옵션이 펼쳐진다.
이 버튼은 **페이지를 바꾸지 않는다** — 항목 글자를 누르면 페이지 이동, `⌄`를 누르면 펼침/접힘이다.

펼친 영역에는 두 가지가 있다.

- **검색 옵션 → AI 챗봇 사용** 토글 (5장 참고)
- **문서 형식** — 특정 확장자만 검색 대상으로 좁힌다. "전체"를 켜면 형식 조건 없음.

**최근 검색**은 사이드바 아래쪽에 최신순으로 쌓인다. 클릭하면 그 검색어로 바로 다시 검색한다.

---

## 5. 검색하기 (기본 모드)

화면 아래 입력창에 찾고 싶은 내용을 쓰고 **Enter** 또는 **보내기**.

키워드(`휴가 규정`)도 되고 자연스러운 문장(`연차는 며칠까지 쓸 수 있어?`)도 된다.

### 결과 카드 읽는 법

결과는 **5개씩** 나오고, 아래 **더보기 (N개 더)** 버튼으로 나머지를 펼친다.
카드는 내용 종류에 따라 세 가지로 나온다.

| 카드 | 보이는 것 | 카드 안 버튼 |
|---|---|---|
| **텍스트** | 문단 발췌 + 검색어 하이라이트 | 원문 열기 ↗ |
| **표** | 실제 표 그리드 | ⧉ 표 복사(TSV로 클립보드) · 원문 열기 ↗ |
| **이미지** | 썸네일 | 확대 · 원문 열기 ↗ |

모든 카드 공통:

- **원문 열기 ↗** — 원본 파일을 연다. docx/pptx/xlsx는 **찾은 위치(문단/슬라이드/셀)까지 이동**한다. hwp/hwpx는 파일만 열린다.
- **근처 내용 더보기** — 그 부분 바로 다음 내용을 이어서 보여준다. 제목 한 줄만 잡혔을 때 실제 본문(표 등)을 확인하는 용도다.
- **AI 요약 보기** — 이 카드 하나만 대상으로 AI 요약을 만든다(모델이 설치돼 있을 때만 보인다).

카드에 붙는 회색 배지:

- **관련성 낮음** — 관련도가 낮아 카드가 흐리게 표시된다.
- **파일명 매치** — 본문이 아니라 **파일 이름**에만 검색어가 있다는 뜻이다. 본문을 기대했다면 무시해도 된다.

### 헤더의 ✕

결과 위쪽 헤더의 **✕**를 누르면 검색어와 결과를 모두 지우고 처음 화면으로 돌아간다.

### 결과가 안 나올 때

| 화면 문구 | 뜻 |
|---|---|
| 먼저 대상 폴더를 지정해 주세요. | 인덱싱을 아직 안 했다 → 3장 |
| 검색 결과가 없습니다. | 인덱스에 그 내용이 없다. 다른 단어로 시도 |
| 검색 중 오류가 발생했습니다: … | 인덱스 파일 문제. 재인덱싱 |

---

## 6. AI 챗봇 쓰기

사이드바 `검색/대화` 옆 **⌄** → **AI 챗봇 사용** 토글을 켠다.
(모델이 설치돼 있지 않으면 이 토글은 비활성이다 → 7장)

화면이 대화창으로 바뀐다. 질문을 보내면 **2단계**로 답이 온다.

1. **즉시 (0.01초 수준)** — 문서에서 찾은 관련 발췌가 검색과 똑같은 카드 형태로 바로 뜬다.
2. **잠시 후 (수십 초)** — 그 발췌를 근거로 한 **AI 요약 답변**이 그 아래 자동으로 만들어진다.

> AI 답변 생성은 이 PC에서 직접 모델을 돌리는 것이라 **약 1~2분 걸릴 수 있다.**
> 생성 중에는 문구가 부드럽게 깜박인다. 기다리는 동안에도 화면은 멈추지 않는다.
> 답을 기다리는 중에 **새 질문을 보내면 직전 생성은 취소**되고 새 질문이 처리된다.

### 답변에 붙는 표시

| 표시 | 뜻 |
|---|---|
| `[1]` `[2]` 같은 번호 | 그 문장의 근거가 된 발췌 번호. 아래 카드와 대조해 확인할 수 있다 |
| **확인 필요** 배지 | 답변 중 발췌와 잘 맞지 않는 문장이 있다. 반드시 원문으로 확인할 것 |
| 문서에서 찾을 수 없습니다 | 모델이 근거 부족으로 **기권**했다. 아래 검색 결과를 직접 볼 것 |
| 관련 문서를 찾을 수 없습니다 | 관련도가 너무 낮아 **모델을 부르지도 않았다**. 질문을 바꿔 볼 것 |

### 대화 이어가기

- 이전 대화 내용을 참고해 답한다 — "그건 얼마야?" 같은 후속 질문이 통한다.
- 챗봇을 껐다 켜도 **이전 대화가 남아 있다.**
- 검색 모드와 챗봇 모드는 **서로 완전히 분리**돼 있다 — 모드를 바꿔도 상대 쪽 내용이 넘어오지 않는다.

### 질문 요령

- 짧은 키워드보다 **완결된 문장**이 잘 먹는다. (`응시료` ✕ / `코치 인증자격시험 응시료가 얼마야?` ○)
- 문서에 없는 상식·일반 지식은 물어도 답하지 않는다. 그게 정상이다.

---

## 7. 설정 — 모델 관리

사이드바 **설정** 페이지.

### PC 성능 선택 (검색 정확도용 임베딩 모델)

| 모드 | 대상 | 특징 |
|---|---|---|
| **경량 모드** | 최소 사양(8GB) | 기본값. 인덱싱이 5배 이상 빠르다 |
| **권장 모드** | 권장 사양(16GB) | 별도 준비 필요(자동 다운로드 불가) |

> 모드를 바꾸면 그 모델용 데이터를 **자동으로 채운다**(문서 수에 따라 수 분).
> 앱을 켠 직후에는 자동 실행하지 않고 상태바로만 안내한다.
> 권장 모드로 바꿨는데 계속 "관련 문서를 찾을 수 없습니다"가 나오면 이 보완이 아직 안 끝난 것이다.

### 모델 관리

**모델 관리** 버튼 → 설치 상태를 실제 파일로 확인해 보여준다.

- **임베딩 모델** — 검색 정확도용
- **AI 챗봇 모델(sLM)** — 답변 생성용
  - 권장 사양(16GB): **Qwen3.5 4B** (약 2.74GB)
  - 최소 사양(8GB): **EXAONE 4.0 1.2B** (약 0.81GB)

배지 의미: `설치됨` / `설치됨 · 검증됨` / `다운로드 필요` / `미설치` / `확인 필요`(파일 손상 의심).

**다운로드 안내** 버튼을 누르면 받을 링크·파일명·저장 위치가 나온다.
인터넷 되는 PC에서 받아 `models/slm/` 에 넣고 **새로고침 (파일 확인)**을 누르면 인식된다.

명령줄로 받는 방법:

```bash
./.venv/Scripts/python.exe -m slm.download qwen3.5-4b
```

임베딩 모델(경량):

```bash
./.venv/Scripts/python.exe -m indexer.vector.download
```

> AI 모델은 쓸 때만 켜지고 **5분간 안 쓰면 자동으로 내려간다** — 다른 작업용 메모리를 계속 물고 있지 않기 위해서다. 그래서 오랜만에 첫 질문은 몇 초 더 걸린다.

---

## 8. 자주 겪는 상황

| 증상 | 원인 / 해결 |
|---|---|
| 검색해도 "먼저 대상 폴더를 지정해 주세요" | 인덱싱 안 함 → 문서 관리 → 폴더 선택 → 다시 인덱싱 |
| `.doc`/`.xls` 문서가 검색이 안 됨 | 변환기 없음 → 3-3 참고 |
| **AI 챗봇 사용** 토글이 눌리지 않음 | AI 모델 미설치 → 7장 |
| AI가 계속 "문서에서 찾을 수 없습니다" | 근거 부족으로 기권. 질문을 완결된 문장으로 바꾸고, 아래 발췌를 직접 확인 |
| 권장 모드에서 무엇을 물어도 결과 없음 | 권장 모델용 데이터 보완이 아직 안 끝남 → 재인덱싱 한 번 |
| AI 답변이 너무 느림 | 정상 범위(수십 초~2분). 발췌(1단계)는 즉시 나와 있으니 먼저 읽으면 된다 |
| 원문 열기를 눌러도 안 열림 | 파일이 이동·삭제됐다. 상태바에 사유가 표시된다 → 재인덱싱 |
| 문서 개수가 실제보다 많음 | 예전 폴더 흔적. **다시 인덱싱**하면 현재 폴더 기준으로 정리된다 |

---

## 9. 알아두면 좋은 원칙

- **문서에 없으면 답하지 않는다.** 기권은 고장이 아니라 안전장치다.
- **AI 답변은 항상 근거 번호와 함께 나온다.** 중요한 판단은 `원문 열기 ↗`로 확인하는 것이 전제다.
- **인덱싱은 백그라운드다.** 진행 중에도 검색·대화·화면 이동이 전부 가능하다.
- **모든 처리는 이 PC 안에서 끝난다.** 검색어도 문서도 외부로 나가지 않는다.

---

## 10. 아직 준비 중인 기능 (2026-08-18 기준)

- **문서 관리** 페이지의 인덱스 통계·파일별 오류 표시
- **설정** 페이지의 모델 상주/유휴 종료 시간/AI CPU 사용량, 런타임 정보 표시
- hwp/hwpx의 "원문 열기" 위치 이동(현재는 파일만 열림)
