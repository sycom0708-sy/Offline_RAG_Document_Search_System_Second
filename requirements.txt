# Phase 1: 문서 파서 모듈 의존성
PyMuPDF>=1.23.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23
chardet>=5.2.0
pyhwp>=0.1b15
six>=1.16.0  # pyhwp가 선언하지 않은 런타임 의존성

# Phase 2: FTS5 키워드 인덱싱 의존성
kss>=6.0.0  # 한국어 문장 분리. numpy/scipy/networkx/pecab 등 약 15개 의존성을 끌고 옴 (Phase 9 패키징 재검토 대상)

# Phase 3: 임베딩 + 벡터 재순위 의존성 (런타임)
# torch는 의도적으로 제외한다 — 사용하는 모델이 int8 ONNX를 이미 제공하므로
# 추론에 torch가 필요 없고, torch만으로 117MB가 늘어 인스톨러 예산(TECH 9.2)을 압박한다.
onnxruntime>=1.17.0
tokenizers>=0.15.0
numpy>=1.24.0

# Phase 4: 추출형 검색 UI 의존성
# PySide6-Essentials만 설치한다 (Addons·Quick/QML 제외) — DESIGN §0 근거대로
# 외부 런타임(WebView2 등) 의존 없이 포터블 배포가 가능한 유일한 후보.
PySide6-Essentials>=6.7.0

# Phase 8 T8.5: 실시간 폴더 감시 의존성 (옵션, 기본 OFF)
watchdog>=4.0.0

# 테스트
pytest>=7.4.0
pytest-qt>=4.4.0
reportlab>=4.0.0
Pillow>=10.0.0

# Phase 9: 패키징 의존성 (빌드 전용 — 배포본에는 포함되지 않는다)
pyinstaller>=6.10.0
