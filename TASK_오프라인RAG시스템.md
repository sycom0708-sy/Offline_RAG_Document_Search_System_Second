# TASK: RAG와 sLM을 활용한 오프라인 문서 검색 시스템 — 작업 분해 문서

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 기준 문서 | 오프라인_RAG_시스템_설계문서.md 10장 개발 로드맵 |
| 문서 목적 | 로드맵을 실행 가능한 Phase/Task 단위로 분해, 완료 기준(DoD) 명시 |

---

## 마일스톤 개요

| Phase | 내용 | 구분 |
|---|---|---|
| Phase 1 | 문서 파서 모듈 | MVP |
| Phase 2 | 폴더 스캔 + FTS5 키워드 인덱싱 | MVP |
| Phase 3 | 임베딩 연동 + 벡터 재순위 | MVP |
| Phase 4 | 추출형 검색 UI (텍스트 카드) | **MVP 완료 — 1차 배포 가능** |
| Phase 5 | 표/이미지 카드 렌더러 | 확장 |
| Phase 6 | sLM 후보군 실측 검증 | 확장 |
| Phase 7 | sLM 답변 생성 옵션 모드 | 확장 |
| Phase 7.5 | KURE-v1 임베딩 변환 파이프라인 | 확장 (우선순위 상향 — 사용자 확정) |
| Phase 8 | 증분 인덱싱 / 폴더 감시 | 확장 (완료) |
| Phase 9 | exe 패키징 및 배포 테스트 | 확장 |

> Phase 1~4까지 완료되면 실무에서 바로 쓸 수 있는 안전한 버전(추출형 검색 MVP)이 완성된다. Phase 5에서 결과 유형을 표/이미지까지 확장하고, Phase 6 검증 결과에 따라 Phase 7부터는 권장 사양 팀원 대상으로 점진적으로 활성화하는 것을 권장한다.

---

## Phase 1: 문서 파서 모듈

**목표**: pdf/docx/txt를 우선 지원하고, xlsx/pptx → hwp/구버전 포맷 순으로 확장. 표/이미지 구조 보존을 포함한다.

**의존성**: 없음 (착수 Phase)

### 작업 목록
- [x] T1.1 프로젝트 스캐폴딩 (디렉토리 구조, 의존성 관리, 가상환경)
- [x] T1.2 PDF 파서 구현 (PyMuPDF 기반 텍스트 추출)
- [x] T1.3 DOCX 파서 구현 (python-docx)
- [x] T1.4 TXT 파서 구현 (chardet 인코딩 자동 감지, CP949/EUC-KR 대응)
- [x] T1.5 XLSX 파서 구현 (openpyxl)
- [x] T1.6 PPTX 파서 구현 (python-pptx)
- [x] T1.7 HWP 파서 구현 (pyhwp/hwp5txt) — 실문서 검증 완료
- [x] T1.8 HWPX 파서 구현 (zip+XML 자체 파싱)
- [x] T1.9 LibreOffice 헤드리스 변환 파이프라인 구축 (doc/xls/ppt 구버전 대응) — 실변환 검증 완료
- [x] T1.10 표 구조 보존 로직 (행·열 구조 유지, 텍스트 청크와 분리 저장)
- [x] T1.11 이미지·도면 추출 로직 (삽입 이미지 추출 + 벡터 도형 페이지 렌더링 캡처)
- [x] T1.12 파서 공통 출력 스키마 정의 (type: text/table/image 필드 포함)
- [x] T1.13 형식별 샘플 문서로 파싱 정확도 테스트

**완료 기준(DoD)**: 9종 형식(doc, docx, pdf, xls, xlsx, ppt, pptx, txt, hwp/hwpx) 샘플 문서가 오류 없이 파싱되고, 표/이미지가 텍스트와 분리된 구조로 추출됨을 확인.

**진행 상태**: **DoD 충족 — 9종 형식 전부 실검증 완료.** 테스트 126 passed / 0 skipped. txt·pdf·docx·xlsx·pptx·hwpx는 생성 샘플로, hwp는 실문서(139KB 기출문제)로, doc·xls·ppt는 LibreOffice 실변환으로 검증. 표 행·열 구조 보존, 이미지 분리 추출, 벡터 도형 페이지 렌더링 캡처 모두 확인.

**검증 과정에서 발견·수정한 버그 3건** (모두 실환경에서만 드러남):
- HWP `BinData`가 `.tmp` 확장자로 저장돼 이미지 38개가 전량 누락 → 확장자 대신 시그니처(매직 넘버) 판별로 변경 (`parser/utils/imaging.py`)
- 1행짜리 표의 첫 행을 헤더로 승격해 `rows`가 비는 문제 → `TableData.from_rows()` 공통 팩토리로 전 파서 통일
- 한국어 Windows에서 soffice가 CP949로 출력하는데 UTF-8 고정 디코딩(`text=True`)이라 변환 실패 사유가 유실 → 바이트 수신 후 다단계 디코딩으로 변경

**Phase 9로 넘길 과제**:
- 구버전 포맷은 파일마다 soffice 프로세스를 새로 띄워 **건당 2.47초**(순정 docx 0.01초의 약 250배). 대량 최초 인덱싱 시 배치 변환 또는 LibreOffice 데몬 재사용 검토 필요
- docx/pptx 벡터 도형 캡처는 도형 유무를 사전 판별할 수단이 없어 전 페이지를 캡처함(PDF는 선별 캡처). Phase 5에서 켤 때 페이지 수 비례 비용 고려 필요

**산출물**: `parser/` 모듈, 형식별 파서 단위 테스트, `parser/README.md`

---

## Phase 2: 폴더 스캔 + FTS5 키워드 인덱싱

**목표**: sLM/임베딩 없이 키워드 검색만 먼저 동작 확인 — **완료**

**의존성**: Phase 1 (파서 출력 스키마)

### 작업 목록
- [x] T2.1 폴더 스캔 로직 (재귀 탐색, 지원 확장자 필터링)
- [x] T2.2 SQLite FTS5 스키마 설계 (문서/청크 테이블, 메타데이터 컬럼)
- [x] T2.3 파서 출력 → FTS5 저장 파이프라인 연결
- [x] T2.4 한국어 문장 분리 적용한 청킹 로직 — **Phase 3에서 정규식 기반으로 교체**(kss 53자/초로 실사용 불가), 토큰 수 기준 분할로 변경
- [x] T2.5 제목/키워드 추출 로직 — 제목은 Phase 1 `ParsedDocument.title` 재사용, 청크 키워드는 파서가 표/이미지에 이미 채운 값 인덱싱 (범위 축소, PLAN 참고)
- [x] T2.6 표 청크 캡션/헤더 텍스트의 키워드 인덱스 가중치 반영
- [x] T2.7 BM25 기반 키워드 검색 쿼리 함수 구현
- [x] T2.8 백그라운드 스레드 인덱싱 + 진행 바 연동
- [x] T2.9 키워드 검색 단독 동작 테스트 (CLI 또는 임시 UI)

**완료 기준(DoD)**: 샘플 폴더 전체를 인덱싱한 뒤, 키워드 검색만으로 관련 문서/청크가 정확히 조회됨 — CLI로 실측 확인. UI 프리즈 없이 백그라운드 인덱싱 진행 바 확인 — `IndexingThread`가 메인 스레드와 분리되어 동작함을 테스트로 확인(실제 UI 프리즈 여부는 Phase 4에서 최종 검증).

**진행 상태**: 테스트 177 passed / 0 skipped (Phase 1의 126 + 이번 51). 상세 설계·실측 데이터·계획 대비 변경점은 `PLAN_오프라인RAG시스템.md` Phase 2 절 참고.

**산출물**: `indexer/` 모듈(`scanner.py`, `chunker.py`, `pipeline.py`, `cli.py`, `fts5/{schema,store,search}.py`), 단위 테스트 51건

---

## Phase 3: 임베딩 모델 연동 + 벡터 재순위

**목표**: 1단계 키워드 필터 결과에 대해 2단계 벡터 재순위를 추가한다.

**의존성**: Phase 2 (FTS5 키워드 필터가 선행되어야 함)

### 작업 목록
- [x] T3.1 임베딩 모델 선정/다운로드 — 경량은 `ko-sroberta-multitask` int8 ONNX(111MB) 확정, KURE-v1은 구조만 열어둠(아래 참고)
- [x] T3.2 ONNX + int8 양자화 — **변환 불필요**: 선정 모델이 int8 ONNX를 이미 배포해 torch 없이 추론
- [x] T3.3 벡터 저장소 스키마 — **ChromaDB → SQLite BLOB으로 변경** (TECH 5.1 갱신, 아래 참고)
- [x] T3.4 청크 임베딩 계산 및 저장 파이프라인 (인덱싱 단계에 연결)
- [x] T3.5 코사인 유사도 직접 계산 기반 재순위 로직 구현 (ANN 미사용)
- [x] T3.6 하이브리드 검색 함수 통합 (FTS5 후보 → 벡터 재순위)
- [x] T3.7 최소/권장 사양별 임베딩 모델 토글 설정 파일 구조 (`config/settings.py`)
- [x] T3.8 검색 응답 속도 벤치마크 (`scripts/benchmark_search.py`)

**완료 기준(DoD)**: 충족. 자연어 질의 "계약서 검토 기준이 뭐였지"에서 키워드 단독은 0건(AND 조건), 하이브리드는 정답 청크를 유사도 0.655로 1위 노출. 검색 지연은 질의당 **7~14ms**로 체감 지연 없음(단, 측정 PC는 8GB 최소 사양기가 아니므로 참고치).

**진행 상태**: 테스트 233 passed / 0 skipped (Phase 1·2의 177 + 이번 56).

**주요 결정 변경 2건** (근거는 `PLAN` 문서 §3-B):
- **벡터 저장소를 SQLite BLOB으로** — ChromaDB는 79개 패키지(75MB, `kubernetes`·`grpcio` 포함)를 끌고 오는데 실제 쓰는 기능은 ID 조회뿐이었다. 기존 인덱스 DB에 `chunk_vectors` 테이블을 두어 추가 의존성 0개로 해결
- **권장 모드(KURE-v1)는 구조만** — 원본 2.27GB에 ONNX 미제공이라 int8 변환 후에도 ~570MB로 TECH 9.2 예산(100~250MB)을 넘는다. 토글·추론 코드는 완성했고, TECH 9.3의 sLM과 동일한 분리 다운로드 대상으로 둔다

**Phase 2 결함 2건을 발견해 함께 수정**:
- `kss` 문장 분리가 **53자/초**(8,267자에 157초)로 실사용 불가 → 정규식 기반으로 전환
- 자연어 질의가 **0건 반환** (AND 조건 + 한국어 조사 미처리) → 조사 제거 + AND 실패 시 OR 폴백

**산출물**: `indexer/vector/` 모듈, `search/hybrid_search.py`, `config/settings.py`, `scripts/benchmark_search.py`

---

## Phase 4: 추출형 검색 UI 완성 (MVP 완료 지점)

**목표**: 텍스트 카드 우선 구현. 여기까지 완료되면 1차 배포 가능한 MVP.

**의존성**: Phase 3 (하이브리드 검색 백엔드)

> **참조 목업**: 사이드바 필터형 레이아웃(v3) 확정 — 상단 검색바 / 좌측 사이드바(문서 형식 필터 → 검색 옵션 토글 3종 → PC 성능 선택 콤보박스) / 우측 결과 카드 리스트 / 하단 상태바 구조. 아래 작업은 이 구조를 컴포넌트 단위로 분해한 것이다.

### 작업 목록

**레이아웃 셸**
- [x] T4.0 UI 프레임워크 확정 — **PySide6**. 외부 런타임 의존 없이 TECH 9.4 포터블 배포("압축 해제 후 즉시 실행")가 가능한 유일한 후보 (근거는 DESIGN §0)
- [x] T4.1 전체 레이아웃 셸 구현 (상단 검색바 / 좌측 사이드바 고정폭 / 우측 결과 영역 / 하단 상태바 2단 그리드 구조)

**검색바**
- [x] T4.2 검색 입력창 컴포넌트 (아이콘 + placeholder + 입력 상태)
- [x] T4.3 검색 실행 트리거 연결 (Enter/입력 debounce → Phase 3 하이브리드 검색 함수 호출)

**사이드바 — 문서 형식 필터**
- [x] T4.4 문서 형식 체크박스 그룹 구현 (전체/docx/xlsx/pdf 등, FTS5 메타데이터 기반 동적 목록)
- [x] T4.5 형식 필터 선택 상태 → 검색 쿼리 조건 반영

**사이드바 — 검색 옵션 (토글 3종)**
- [x] T4.6 토글 스위치 공통 컴포넌트 제작 (AI 요약 보기 / 대·소문자 구분 / 일치되는 단어가 동일 컴포넌트 재사용)
- [x] T4.7 "AI 요약 보기" 토글 배치 — Phase 7 전까지는 상태만 저장, 실제 sLM 호출은 비활성/placeholder
- [x] T4.8 "대/소문자 구분" 토글 → 키워드 검색(FTS5) 쿼리 옵션 연동
- [x] T4.9 "일치되는 단어" 토글 → 부분 일치 대신 완전 일치 검색 조건으로 전환하는 로직 연동

**사이드바 — PC 성능 선택** (설계 확정, 시안은 PLAN §4-C 참고)
- [x] T4.10 PC 성능 선택 콤보박스 구현 (경량 모드/권장 모드). 각 옵션에 설치 상태 배지(설치됨/준비 중) 표시
- [x] T4.11 콤보박스는 **선택 트리거만** 담당하고, 실제 전환은 모델 관리 팝업에서만 일어나도록 연결 (Option A 채택 — 미설치인데 선택된 상태 자체를 차단). Phase 3 `config.settings.get_profile()`과 연동
- [x] T4.11a **모델 관리 팝업에 임베딩 모델 섹션 추가** (원래 Phase 7 T7.6 범위였으나 앞당김) — `ko-sroberta`(경량, 항상 설치됨) + `KURE-v1`(권장, **"준비 중"으로만 표시, 다운로드 버튼 비활성** — 아래 참고)
- [x] T4.11b 권장 모드 선택 완료 후 재인덱싱 트리거 코드 연결 — `indexer.vector.store.embed_missing(conn, new_embedder)` 재사용 (신규 로직 불필요). **KURE-v1이 실제로 설치될 수 없어 이번 Phase에서는 종단 검증 불가** — 코드 경로만 연결됨, PLAN §4-D 참고

> **KURE-v1 다운로드는 이번 Phase에서 미구현.** 허깅페이스 레포에 ONNX가 없어(`safetensors`만 제공) torch 기반 변환 파이프라인이 별도로 필요하다 — Phase 3에서 "구조만 열어두자"고 미룬 작업. 지금은 "준비 중" 배지만 노출하고 다운로드 버튼은 비활성 상태로 둔다. 변환 파이프라인 구현 여부는 별도 Phase에서 재검토.

**결과 카드 — 공통/텍스트**
- [x] T4.12 카드 공통 프레임 구현 (파일명 + 위치 표시 + "원문 열기" 버튼, 표/이미지 카드도 재사용할 기반 컴포넌트)
- [x] T4.13 텍스트 결과 카드 구현 (발췌문 + 검색어 하이라이트, 대소문자구분/일치단어 옵션에 따른 하이라이트 규칙 반영)
- [x] T4.14 유사도 임계값 미만 결과 흐림 처리 (카드 opacity + "관련성 낮음" 라벨)
- [x] T4.15 결과 리스트 스크롤/빈 결과 상태(빈 화면 문구) 처리

**하단 상태바**
- [x] T4.16 인덱싱 상태 표시 (문서 수, 마지막 갱신 시각) 구현
- [x] T4.17 폴더 관리 진입점 연결 (클릭 시 폴더 관리 화면으로 이동 — 화면 자체는 별도 작업으로 분리 가능. 최소 `QDialog`로 구현: 폴더 선택 + 다시 인덱싱)

**통합 테스트**
- [x] T4.18 엔드투엔드 사용자 시나리오 테스트 (검색 → 필터/옵션 조합 → 텍스트 결과 확인 → 원문 열기)
- [x] T4.19 사이드바 옵션 조합별 회귀 테스트 (형식 필터 × 대소문자구분 × 일치단어 × PC 성능 선택 교차 케이스)

**완료 기준(DoD)**: 실제 팀 문서 샘플로 검색→필터/옵션 적용→발췌 결과 확인→원문 열기까지 전체 흐름이 오류 없이 동작. 사이드바의 모든 토글·콤보박스가 검색 결과에 실제로 반영됨을 확인. 이 시점을 **1차 MVP 배포 가능 지점**으로 간주. **충족 완료** — 실제 데모 폴더(실 hwp 문서 포함)로 검색→필터/토글 교차→하이라이트 확인→원문 열기 흐름을 `QWidget.grab()` 스크린샷으로 시각 검증.

**산출물**: 검색 UI 애플리케이션 (MVP 빌드), 참조 목업(v3) 기준 컴포넌트 일람

---

## Phase 5: 표 카드 / 이미지 카드 렌더러 추가

**목표**: 6.3절 타입 기반 동적 라우팅 구조를 적용해 결과 유형을 확장한다.

**의존성**: Phase 4 (텍스트 카드 렌더링 구조), Phase 1 (표/이미지 파싱 결과)

### 작업 목록
- [x] T5.1 청크 타입 기반 렌더러 분기 로직 구현 (text/table/image)
- [x] T5.2 표 카드 컴포넌트 (원본 표 구조 렌더링 + "표 복사" 버튼)
- [x] T5.3 이미지 카드 컴포넌트 (썸네일 + "확대" 버튼)
- [x] T5.4 이미지 텍스트 미인식 안내 문구 추가
- [x] T5.5 이미지 썸네일 캐시 생성/조회 로직 (300px 축소판, 로컬 캐시)
- [x] T5.6 혼합 결과 리스트 렌더링 테스트 (text/table/image 동시 노출)

**완료 기준(DoD)**: 표/이미지가 포함된 문서 검색 시 타입에 맞는 카드가 자동 분기되어 표시되고, 하나의 결과 리스트에서 3종 카드가 자연스럽게 혼합 노출됨. **충족 완료** — 실제 데이터(text+table+image 혼합 문서)로 검색→3종 카드 동시 렌더링을 `QWidget.grab()` 스크린샷으로 시각 검증.

**산출물**: `ui/cards/table_card`, `ui/cards/image_card`, 썸네일 캐시 모듈

---

## Phase 6: sLM 후보군 실측 검증

**목표**: Qwen2.5-1.5B/7B, EXAONE, Phi-3.5-mini 등 2~3개 후보로 "문서에 없으면 모른다고 답하는가"(근거 강제 프롬프트 준수율)를 실제 사내 문서 샘플로 테스트해 가장 안정적인 모델을 선정한다.

**의존성**: Phase 4 (근거로 사용할 검색 결과 파이프라인)

### 작업 목록
> 후보군은 2026년 기준으로 갱신했다(원안은 2024년 기준). EXAONE-4.0-1.2B / Phi-4-mini / Qwen3.5-4B / EXAONE-3.5-7.8B — 근거는 `PLAN` §6-A ②.

- [x] T6.1 후보 모델 GGUF Q4_K_M 확보 — EXAONE-4.0-1.2B·Phi-4-mini·Qwen3.5-4B·EXAONE-3.5-7.8B **4종 전부 설치·측정 완료**(EXAONE-3.5-7.8B는 권장 사양 PC에서 측정, 2026-08-10)
- [x] T6.2 llama.cpp 기반 추론 환경 구축 (CPU-only) — 사전 빌드 바이너리 b10306 + `llama-server` 서브프로세스, 종단 응답 확인
- [x] T6.3 "근거 강제 프롬프트" 템플릿 초안 작성 — 4회 반복. **system 메시지가 템플릿에서 버려지는 함정**을 잡고 user 메시지로 전환(`PLAN` §6-B)
- [x] T6.4 검증용 테스트셋 구성 — 실문서 25문항(근거 있음 14 / 없음 11) + 커밋용 합성 6문항 + 권장 사양 PC용 26문항(2026-08-10, exdoc 폴더 기준 재작성 — 저사양 PC 인덱스와 chunk_id가 맞지 않아 새로 작성). 실문서용은 본문 대신 `chunk_ids`만 담아 유출을 스키마로 차단
- [x] T6.5 모델별 준수율 측정 — 최소 사양 기준기 3종 + 권장 사양 기준기 2종(Qwen3.5-4B·EXAONE-3.5-7.8B) 측정 완료. **기권 정확도 EXAONE-4.0-1.2B 100% / Phi-4-mini 27.3% / Qwen3.5-4B 100%(최소 사양)**, 권장 사양은 두 모델 다 81.8%. 표본 육안 검증으로 채점기 오판 1건 수정
- [x] T6.6 모델별 응답 속도/메모리 사용량 측정 — 최소 사양 완료(중앙 지연 5.6/15.5/34.0초, 메모리 피크 1.7/3.5/4.2GB). **권장 사양도 완료**(Qwen3.5-4B 18.3초/4.8GB, EXAONE-3.5-7.8B 11.4초/8.3GB)
- [x] T6.7 결과 비교표 작성 및 최종 모델 선정 — **최소 사양 결론 확정**(sLM 기본 OFF 유지, 켠다면 EXAONE-4.0-1.2B / Phi-4-mini는 전 사양 제외). **권장 사양 채택 모델 확정: Qwen3.5-4B** [사용자 확정, 2026-08-10] — 응답정확도·기권정확도는 EXAONE-3.5-7.8B와 동일(81.8%/80.0%)하나 메모리 4.8GB로 절반이라, 안드로이드 스튜디오 등 다른 무거운 작업과 동시에 돌려도 시스템이 안 느려지는 쪽을 우선했다(속도는 18.3초로 EXAONE 대비 7초 느림). 근거는 `PLAN` §6-B T6.8

**완료 기준(DoD)**: 후보 모델별 준수율·속도·메모리 데이터가 문서화되고, 사양별(최소/권장) 채택 모델이 확정됨.

**산출물**: sLM 후보 비교 리포트, 최종 채택 모델 결정 기록

---

## Phase 7: sLM 답변 생성 옵션 모드 추가

**목표**: 4단계 안전장치를 포함한 AI 요약 옵션 모드를 구현하고, Phase 6에서 선정된 모델을 적용한다. 모델 매니저 및 다운로드 흐름도 함께 구현한다.

**의존성**: Phase 6 (모델 선정), Phase 4 (검색 결과 파이프라인)

### 작업 목록
- [x] T7.1 1단계 안전장치 — 유사도 임계값 필터 구현 (미달 시 sLM 미호출). `slm/summarize.py:select_excerpts()`. `SIMILARITY_THRESHOLD`(0.5)를 DESIGN §5.6 "관련성 낮음"과 **같은 상수로 공유** — 화면에서 흐리게 본 문서가 요약 근거로 쓰이면 설명할 수 없다. **`similarity=None`(임베딩 미사용)도 부적격 처리**
- [x] T7.2 2단계 안전장치 — 근거 강제 프롬프트 + low temperature 설정 적용. Phase 6에서 4회 반복해 다듬은 `slm/prompt.py`를 그대로 재사용, `SlmService.chat()`이 `temperature=0.0` 고정
- [x] T7.3 3단계 안전장치 — 문장 단위 출처 표기 로직 (`[파일명, 페이지/슬라이드]`). **모델에게 파일명을 쓰게 하지 않는다** — 모델은 `[N]`만 달고 `expand_citations()`가 결정론적으로 치환(4B급에 파일명을 인라인으로 적게 하면 파일명 자체를 지어낼 여지가 생긴다). 범위 밖 번호는 일부러 남겨 4단계가 잡게 한다
- [x] T7.4 4단계 안전장치 — 답변-근거 겹침도 계산 및 "확인 필요" 표시. `slm/verify.py`, 문자 bigram 재현율(한국어는 조사·어미 때문에 어절 매칭이 잘 안 걸린다). **"권장 사양 전용"은 적용하지 않고 항상 켠다** [사용자 확정, 2026-08-10] — 계산 비용이 사실상 0이고, 코드에 PC 사양 판정 근거가 없어 그대로 두면 영영 안 켜진다
- [x] T7.5 "AI 요약 보기" 토글 실제 동작 연결 (Phase 4의 placeholder 대체). **모델 미설치 시에는 여전히 비활성** — DESIGN §4.2의 "켜도 아무 일이 없으면 고장으로 보인다"는 Phase 7 이후에도 유효하다
- [x] T7.6 모델 매니저 화면에 **sLM 섹션** 추가 (설치 상태 표시: "다운로드 필요"/"설치됨"/"확인 필요"). **채택된 2종만 노출**(`SLM_OFFERED`) — 측정 후보 4종은 벤치마크 하네스의 관심사다
- [x] T7.7 다운로드 안내 팝업 구현 (링크, 파일명, 용량, SHA256 체크섬). 노출 2종의 해시는 이 저장소의 실제 파일에서 계산해 `SlmProfile.sha256`에 기록. **자동 다운로드는 하지 않는다**(오프라인 전제 — LibreOffice 포터블과 같은 방침)
- [x] T7.8 "폴더 열기" 버튼 구현 (숨김 설치 경로 탐색기 노출) — 기존 `_open_folder()` 재사용
- [x] T7.9 "새로고침(파일 확인)" 버튼 구현 (재스캔 + 체크섬 검증 + 자동 활성화). 크기는 즉시, **SHA256은 백그라운드 스레드**(GB 단위라 UI가 얼어붙는다). 같은 파일을 매번 다시 해시하지 않도록 (크기·mtime) 캐시 추가
- [x] T7.10 사양별(최소/권장) 모델 토글 설정 연동 — 권장=`qwen3.5-4b` / 최소=`exaone-4.0-1.2b`, 선택값은 `AppState.slm_profile`
- [x] T7.11 AI 요약 응답 품질 회귀 테스트 (Phase 6 테스트셋 재사용) — `scripts/regress_summary.py`로 26문항을 **앱과 같은 2~4단계 경로**에 통과시켜 Phase 6 순수 추론 수치와 대조

**완료 기준(DoD)**: AI 요약 토글 ON 시 4단계 안전장치가 모두 동작하며, 모델 미설치 상태에서 다운로드 안내→수동 설치→새로고침 검증까지 전체 흐름이 오류 없이 완료됨.

**산출물**: `slm/` 추론 모듈, 모델 매니저 UI, 4단계 안전장치 통합 테스트 결과

---

## Phase 7.5: KURE-v1 임베딩 변환 파이프라인

**목표**: `KURE-v1`(권장 모드 임베딩)을 safetensors → ONNX(int8)로 변환하는 파이프라인을 만들어, Phase 3~4에서 "구조만 열어두고" 미뤄온 권장 모드를 실제로 다운로드·설치·전환 가능하게 만든다.

**우선순위를 올려 별도 Phase로 신설한 배경 [사용자 확정, 2026-08-10]**: 원래 PLAN 문서 여러 곳(§4-C, §4-D)에 "Phase 7 이후 별도 Phase에서 재검토"로만 적혀 있고 Phase 10 백로그(T10.1~T10.7)에도 정식 항목으로 오르지 못한 채 방치돼 있었다. 이 PC가 권장 사양(16GB)으로 확정된 이상, 검색 품질을 실제로 끌어올릴 수 있는 권장 임베딩을 계속 "준비 중" 배지로만 남겨둘 이유가 약해져 Phase 8(증분 인덱싱)보다 앞당긴다.

**의존성**: Phase 3(임베딩 프로파일 구조, `config.settings.HEAVY`) — 토글·추론 코드는 이미 있고 변환된 아티팩트만 없는 상태

### 착수 전 재확인 필요 [착수 시 재검토]

- **원본 용량 불일치**: PLAN §4-C는 "원본 2.27GB"라 적었지만, 2026-08-10 허깅페이스 API 조회로는 `model.safetensors`가 약 568MB로 확인됐다. 어느 쪽이 맞는지(문서 오기 vs 레포가 그 사이 바뀜) 착수 시 재확인 필요 — 인스톨러 예산(TECH 9.2) 판단에 영향을 준다.
- **`config/settings.py`의 `HEAVY.max_seq_length=512`가 틀렸을 가능성**: 허깅페이스 모델 카드 기준 KURE-v1의 실제 최대 시퀀스 길이는 8192다. 청크 자체는 이 프로젝트가 이미 토큰 수 기준으로 짧게 자르므로(Phase 3) 실사용 영향은 적겠지만, 프로파일 값 자체는 정정 대상.
- **베이스 아키텍처**: `BAAI/bge-m3` 파생, `sentence-transformers` 구조(`modules.json`/`1_Pooling/config.json` 존재) — 기존 `Embedder`가 이미 mean pooling을 코드에서 직접 구현하고 있어(`indexer/vector/embedder.py`) **런타임 추론 코드는 그대로 재사용 가능**할 것으로 보이나, KURE-v1의 실제 풀링 방식(mean vs CLS)이 `ko-sroberta-multitask`와 같은지는 `1_Pooling/config.json` 실물을 열어 확인해야 한다 — 다르면 검색 품질이 조용히 나빠진다.

### 작업 목록

- [x] T7.5.1 변환 스크립트(`scripts/convert_kure.py`) — safetensors → fp32 ONNX(`optimum`) → `onnxruntime.quantization.quantize_dynamic`으로 int8. **`.venv-convert`(gitignore)에서만 실행, 프로젝트 `.venv`는 torch-free 유지**. 사내망 TLS 검사 프록시로 requests 다운로드가 막혀 `truststore.inject_into_ssl()` 추가(원인·해결 스크립트에 문서화)
- [x] T7.5.2 `HEAVY` 프로파일 정정 — `max_seq_length` 512→8192. **용량은 원래 문서(2.27GB)가 맞았다**(착수 전 조사에서 568MB로 의심한 건 API의 Xet 중복제거 필드를 오독한 것 — 실제 다운로드로 재확인 후 정정). `pooling: str` 필드 신설(LIGHT="mean", HEAVY="cls")
- [x] T7.5.3 풀링 방식 검증 — `scripts/verify_kure.py`. 🔴 **KURE-v1은 CLS 풀링**(mean 가정은 틀렸다) — 실측 코사인 CLS 0.9826 vs mean 0.7377. 참조 벡터(sentence-transformers 공식) 대비 변환 정확성 최소 0.9758
- [x] T7.5.4 양자화 재현성 — 배치 vs 단건 자기 유사도 최소 0.9796 / 중앙 0.9842 (이 PC의 경량 모델 실측치 0.98대와 동등)
- [x] T7.5.5 모델 관리 UI 연동 — 다운로드가 아니라 **"변환 또는 폴더 복사" 안내**로 구현(허깅페이스에 완성 아티팩트가 없어 sLM과 같은 이어받기 다운로드가 성립 안 함). "준비 중"→"미설치"/"설치됨", "설치 안내" 버튼이 실제 변환 명령어 안내
- [x] T7.5.6 T4.11b 종단 검증 — 완료. 🔴 **부수 발견**: `chunk_vectors` PK가 `chunk_id` 단독이라 모델 전환 시 이전 모델 벡터가 삭제되는 버그를 실제 재인덱싱으로 처음 발견 — `(chunk_id, model)` 복합키로 수정 + 기존 DB 자동 마이그레이션. 이후 실제 앱에서 경량→권장 전환 후 검색까지 정상 확인
- [x] T7.5.7 경량 대비 검색 품질 비교 — 처리량 **5.4배 느림**(28.5 vs 5.3청크/초), 품질은 10문서 코퍼스로는 결론 미확정(질의 6건 중 top1 일치 2건, top10 겹침 0~8/10). **결론[사용자 확정]: 권장을 기본값으로 밀 근거 부족 — 경량 기본값 유지**

**완료 기준(DoD)**: 모델 매니저에서 KURE-v1을 실제로 다운로드→설치→새로고침 인식까지 오류 없이 완료되고, 권장 모드로 전환한 뒤 재인덱싱·검색이 정상 동작하며, 경량 모드 대비 검색 품질 변화가 실측 데이터로 문서화됨.

**산출물**: 변환 스크립트, 정정된 `HEAVY` 프로파일, 양자화 재현성·검색 품질 실측 리포트, T4.11b 종단 검증 결과

---

## Phase 7.6: AI 요약을 AI 챗봇으로 교체 (2단 응답 구조)

**목표**: Phase 7의 "AI 요약 보기"(검색마다 자동 1회 요약)를 실사용해보니 필요성이 불분명해, 검색 결과를 근거로 후속 질문을 이어가는 챗봇으로 대체한다.

**우선순위를 올려 별도 Phase로 신설한 배경 [사용자 확정, 2026-08-11]**: LLM 생성 답변은 11~79초(모델·발췌 수에 따라)가 걸려 "은행 앱 챗봇처럼 빠르게"라는 요구와 정면 충돌했다. 사용자가 목업 2장으로 직접 제시한 해법으로 **응답을 2단으로 분리**했다: ① 메시지를 보내면 `hybrid_search()`의 top-1 발췌를 LLM 없이 즉시 원문 그대로 보여주고(검색 지연 7~14ms), ② 그 아래 "AI 요약 보기"를 **눌렀을 때만** LLM이 돈다. 느릴 걸 스스로 선택한 것이므로 대기가 자연스럽다. 메시지마다 독립 처리(stateless) — 매번 검색어 전체 범위로 새로 검색하고, "두 번째는요?" 같은 대명사 참조는 지원하지 않는다.

**의존성**: Phase 7 (`SearchWorker`/`SummaryWorker`/`SummaryCard`/4단계 안전장치 — 전부 무수정으로 재사용)

### 작업 목록
- [x] T7.6.1 토글 리네임 — "AI 요약 보기" → **"AI 챗봇 사용"** (`ui/widgets/search_options.py`). 시그널·메서드명(`ai_summary_changed`/`is_ai_summary()`/`set_ai_summary()`)은 배선 최소 변경을 위해 그대로 유지. `AppState.ai_summary_enabled` → `ai_chat_enabled`로 리네임(`_load_raw()`가 미지 키를 걸러내 마이그레이션 불필요)
- [x] T7.6.2 `ui/widgets/chat_panel.py` 신설 — 턴(turn) 단위 구조. `_AnswerBubble`이 1단계(즉시 발췌, `QLabel` PlainText)와 2단계(선택, 기존 `SummaryCard`를 그대로 끼워 넣어 상태 분기를 재구현하지 않음) 둘 다 갖는다. 워커는 만들지 않고 신호(`message_sent`/`summarize_requested`)로만 요청 — sqlite·sLM 서비스에 직접 접근하지 않는다
- [x] T7.6.3 `ResultList.show_chat_mode(panel)` 추가 — 카드 목록·안내 메시지와 같은 "특수 상태" 취급. 옛 `summary_card()`/`clear_summary()`/`has_summary()` API는 챗봇 패널이 자동 요약 카드를 완전히 대체하므로(추가가 아니라 교체) 함께 제거
- [x] T7.6.4 `MainWindow` 배선 — 토글 ON 시 `show_chat_mode()` + 검색어를 첫 메시지로 자동 전송. 메시지 전송 → **기존** `SearchWorker`(무수정, T10.9의 `profile=` 전달 로직도 그대로 상속) 기동 → 발췌 렌더링. "AI 요약 보기" 클릭 → 그 턴이 ①에서 이미 받은 결과를 그대로 **기존** `SummaryWorker`(무수정)에 넘겨 검색을 다시 하지 않음. `_active_workers`/`_active_summary_workers` 두 세트를 그대로 재사용(이제 턴마다 여러 개 누적 가능)
- [x] T7.6.5 테스트 — 옛 `tests/test_ui_summary.py`를 `tests/test_ui_chat.py`로 이름·대상 모두 교체(챗봇 패널 22건 신규, `SummaryCard` 자체 테스트는 그대로 유지 — 말풍선이 그 위젯을 재사용하므로 검증 대상이 안 바뀜). `test_ui_main_window.py`의 `TestAiSummaryWiring` → `TestAiChatWiring`으로 재작성(2단 응답 흐름·다중 턴 GC 안전성·"AI 요약 보기" 전까지 LLM 미호출 확인 포함). 🔴 T10.9 재발 방지 회귀 테스트(`TestChatSearchProfileRegression`, `heavy_embedder`로 챗봇 검색 경로의 `profile=` 전달을 확인) 신설. 테스트 11건 추가(누적 603 passed / 5 skipped)
- [x] T7.6.6 실제 앱 종단 검증 — 실제 인덱스(문서 19건·청크 607개)로 "코치인증자격시험 응시방법 찾아줘" → 즉시 발췌(체감 무지연) → "AI 요약 보기" 클릭 → 채택 모델(Qwen3.5-4B)이 실제로 근거([코치인증자격시험 세부사항_인증_1_28_v3.doc]) 딸린 정답 생성, "확인 필요" 배지 없음(4단계 통과)까지 스크린샷으로 확인. 표·이미지 청크가 top-1인 질의 2건 연속 전송도 크래시 없이 렌더링(표는 `TableData.to_text()` 평탄화, 이미지는 캡션 텍스트 — 가독성은 다듬어지지 않았지만 화면이 무너지진 않는다). 창 닫은 뒤 `llama-server` 고아 프로세스 없음(`tasklist` 확인)

**완료 기준(DoD)**: "AI 챗봇 사용" 토글 ON 시 메시지 전송 즉시(LLM 미사용) 발췌가 뜨고, "AI 요약 보기"를 눌렀을 때만 LLM이 호출되며, 여러 턴을 연속 생성해도 크래시 없이 각 턴이 독립적으로 동작함.

**산출물**: `ui/widgets/chat_panel.py`, `ResultList.show_chat_mode()`, `MainWindow`의 챗봇 배선, `tests/test_ui_chat.py`

**알려진 한계(이번 범위 아님)**: 표·이미지 청크가 top-1일 때 발췌가 다듬어지지 않은 원시 텍스트로 보인다(표는 `|` 구분 평탄화, 이미지는 짧은 캡션) — 실사용 빈도를 더 보고 필요하면 별도 렌더링을 검토한다. 발췌의 원문 위치로 스크롤 이동은 T10.1 백로그와 동일하게 이번 범위 밖(파일만 열림).

---

## Phase 7.7: UI 전면 재구성 — 목업 컨셉 적용 (완료)

**목표**: 사용자가 제시한 HTML 목업 2장(`rag_ui_concept_chatbot.html`/`rag_ui_concept_searchresult.html`)에 맞춰 화면 구조를 재구성한다. Phase 4~7.6까지 완성된 기능(검색·필터·2단 응답 챗봇)은 무수정으로 재사용하고, **입력창 위치**만 바꾼다.

**착수 배경 [사용자 확정, 2026-08-13]**: 기존 앱은 상단 고정 검색바(debounce 자동검색)와 챗봇 모드 전용 하단 입력창이 공존해 입력 지점이 두 곳이었다. 목업 두 화면 모두 **하단 입력창 하나**로 통일돼 있어, 그 구조로 전면 재구성하기로 확정했다. 함께 확정한 사항: 사이드바에 최근 검색·모델/폴더 관리 버튼 추가, 강조색은 목업의 `#1e90ff` 대신 기존 `#2563EB` 유지, 슬라이드 애니메이션은 이번 범위 제외, 검색 트리거는 300ms debounce 폐기하고 Enter·보내기로만.

**의존성**: Phase 4~7.6 (`SearchWorker`/`ChatPanel`/`SummaryCard`/4단계 안전장치 등 전부 무수정 재사용)

### 작업 목록
- [x] T7.7.1 `AppState.recent_searches` 필드 추가 — `add_recent_search()`로 최신순 승격·중복 제거·최대 10건 절단. `_load_raw()`의 미지 키 필터링으로 마이그레이션 불필요(Phase 7.6의 `ai_chat_enabled` 리네임과 같은 경로)
- [x] T7.7.2 `ui/widgets/search_bar.py`를 `InputBar`로 개조 — 300ms debounce 제거, `submitted(str)` 신호로 Enter·보내기 버튼 제출만 지원. 제출과 동시에 입력창을 비운다. `submit_text()`로 외부(최근 검색 클릭)에서 대신 제출 가능
- [x] T7.7.3 `MainWindow` 레이아웃 재조립 — 최상위를 `QHBoxLayout`(사이드바 | 메인 영역)으로, 메인 영역을 `QVBoxLayout`(결과 헤더 → 결과 리스트 → 공용 입력창 → 상태바)으로. 사이드바가 창 왼쪽 세로 전체를 차지하도록 변경(기존엔 상태바와 나란한 폭이었음)
- [x] T7.7.4 `ChatPanel`에서 자체 입력창(`ChatInput`/`ChatSendButton`) 제거 — `send_message(text)`를 유일한 진입점으로 승격. `MainWindow._on_input_submitted()`가 챗봇 모드 여부를 보고 `ChatPanel.send_message()`/검색 실행으로 분기. 기존 `send_message()` 호출부(자동 전송)와 `tests/test_ui_chat.py`(공개 API만 사용)는 **무수정으로 통과**
- [x] T7.7.5 말풍선 좌/우 정렬 — 사용자 메시지(우측, 파란)·AI 말풍선(좌측, 흰색)을 `QWidget`+`QHBoxLayout` 래퍼로 감싸 `addStretch()` 위치로 정렬. 최대 폭 65%는 Qt QSS가 `max-width`를 지원하지 않아 `setMaximumWidth()`로 직접 계산, `resizeEvent()`에서 창 크기 변화에 맞춰 갱신. 위젯이 창에 부착되기 전(폭 0) 첫 프레임에 상한이 안 걸려 폭 전체로 그려졌다가 줄어드는 것을 막기 위해 생성 시점에 자신의 폭 또는 고정 임시값(640)으로 미리 상한을 건다
- [x] T7.7.6 `ResultHeader` 신설 — 검색 결과 모드에서만 표시(챗봇 모드는 숨김), 현재 검색어 + ✕ 버튼. ✕ 클릭 시 `_last_query`/`_last_results`까지 모두 비워야 한다 — 안 비우면 ① 옵션 토글이 지운 검색을 되살리고 ② 챗봇 모드 진입 시 지운 질문이 유령 첫 메시지로 자동 전송된다(둘 다 회귀 테스트로 확인)
- [x] T7.7.7 사이드바 확장 — `RecentSearches`(최근 검색, 화면엔 최대 5건만 노출 — 10건을 다 늘어놓으면 세로 오버플로로 위쪽 블록이 찌그러진다) + 모델·폴더 관리 버튼 2개 신설. `PerformanceCombo`의 기존 "모델 관리" 링크 버튼은 제거(사이드바 버튼과 중복이라 일원화), `StatusBar`의 "폴더 관리" 버튼도 제거(사이드바로 이동)
- [x] T7.7.8 QSS 정리 — `#SearchBar`/`#SearchIcon`/`#SearchInput`/`#PerformanceComboManageButton`/`#StatusBarFolderButton` 삭제, `#ChatInput`/`#ChatSendButton`을 `#InputBarField`/`#InputBarSendButton`으로 이름 변경(검색·챗봇 공유를 반영), `#ResultHeader*`/`#RecentSearchItem`/`#SidebarFooterButton`/`#MainArea` 신설. 기존 토큰(`#2563EB` 등)만 사용
- [x] T7.7.9 테스트 정리 — `test_ui_main_window.py`의 `search_bar.set_text()` 16곳을 `input_bar.submit_text()`로 치환(debounce 대기용 `qtbot.wait()`도 함께 제거), 헤더 ✕ 회귀 테스트 2건 신설. `test_ui_widgets_basic.py::TestSearchBar` → `TestInputBar`로 교체(debounce 테스트 제거, 제출·클리어 테스트로 대체). `test_ui_widgets_options.py`의 `PerformanceCombo` 관리 버튼 테스트 2건 제거(사이드바 버튼으로 기능 이관). `test_ui_status_bar.py`의 폴더 버튼 테스트 제거. `test_ui_chat.py`에 말풍선 정렬·최대 폭 회귀 테스트 4건, `test_ui_state.py`에 최근 검색 테스트 7건 신설. 전체 626 passed / 5 skipped(누적)
- [x] T7.7.10 실제 앱 종단 검증 — 실제 인덱스(문서 19건)로 초기 화면 → 검색("코치인증자격") → 헤더·결과 카드 표시 → 헤더 ✕ → 초기 복귀 → AI 챗봇 토글 ON → 메시지 전송 → 말풍선 좌/우 정렬 확인 → 창 리사이즈 시 말풍선 폭 65% 추종 → 모델 관리·폴더 관리 다이얼로그 정상 표시까지 스크린샷으로 확인. 사이드바 "최근 검색"이 `app_state.json`에서 실제로 복원되는 것도 확인

**완료 기준(DoD)**: 목업 두 화면(검색 결과 모드/챗봇 모드)과 동일한 레이아웃(하단 공용 입력창, 사이드바 최근 검색·관리 버튼)으로 재구성되고, 기존 검색·필터·2단 응답 챗봇 기능이 회귀 없이 그대로 동작하며, 자동화 테스트 전체 통과 + 실제 인덱스로 시각 검증 완료.

**산출물**: `ui/widgets/search_bar.py`(`InputBar`), `ui/widgets/result_header.py`, `ui/widgets/recent_searches.py`, `ui/main_window.py`/`ui/widgets/sidebar.py`/`ui/widgets/chat_panel.py` 개정, `ui/qss/app.qss` 갱신

---

## Phase 8: 증분 인덱싱 / 폴더 감시 기능

**목표**: 파일 변경사항만 재인덱싱하는 증분 갱신과 이미지 썸네일 캐시 갱신을 구현한다.

**의존성**: Phase 2 (인덱싱 파이프라인), Phase 5 (썸네일 캐시)

### 작업 목록
- [x] T8.1 파일별 mtime·해시 기록 스키마 추가 — **Phase 1부터 이미 충족**(`documents`/`chunks`의 `source_mtime`/`source_hash` 컬럼과 `parser/utils/hashing.py`가 Phase 1 착수 시점에 이미 "Phase 8에서 재사용" 전제로 만들어져 있었다). 이번 Phase에서는 확인만 하고 스키마 변경 없음.
- [x] T8.2 재실행 시 변경/신규 파일 판별 로직 (mtime·해시 비교) — `indexer/incremental/needs_reindex()`. mtime을 먼저 보고 다를 때만 해시를 계산(2단계 비교).
- [x] T8.3 변경 파일만 재인덱싱하는 증분 파이프라인 구현 — `index_folder()`가 파일마다 `needs_reindex()`로 걸러 변경 없으면 파싱·저장을 생략(`report.skipped`).
- [x] T8.4 이미지 썸네일 캐시 증분 갱신 연동 — `store_document()`가 교체 전 이미지 청크 id를 반환 → `IndexReport.stale_image_chunk_ids` → `MainWindow`가 `thumbnail_cache.evict_thumbnails()` 호출.
- [x] T8.5 watchdog 기반 실시간 폴더 감시 옵션 구현 — 핵심(T8.1~T8.4) 완료 후 후속 작업으로 분리했다가 이어서 완료. `indexer/incremental/watcher.py`의 `FolderWatcher`(디바운스 3초)가 감지한 변경을 기존 `_start_reindex()`(무음 모드 `silent=True` 추가)에 그대로 넘긴다 — 변경 없는 파일은 T8.1~T8.4가 알아서 건너뛰므로 별도 "파일 하나만 재파싱" 경로를 새로 만들지 않았다. 토글은 폴더 관리 다이얼로그(`FolderDialog`)에 두었다(DESIGN에 위치 명시 없음, 사이드바는 Phase 7.7에서 막 확정돼 건드리지 않음). 기본 OFF(`AppState.folder_watch_enabled`). 실사용 검증(스크래치 폴더, 실제 watchdog): 파일 수정 → 자동 무음 재인덱싱 → DB에 최신 내용 반영 → 토글 끄면 감시 정리 → 창 닫기 정상, 전부 확인.
- [x] T8.6 증분 갱신 성능 테스트 (대량 문서 중 일부만 변경된 시나리오) — `tests/test_indexer_incremental.py`. 시간 기반 assert는 flaky해서 피하고, "변경 없는 파일에서는 `parse_file` 호출 자체가 안 일어난다"는 결정론적 증거로 대체. 실사용 검증(`../exdoc` 19개 문서, CLI)에서 전체 재인덱싱 117초 → 무변경 재실행 0.87초, 1개 파일만 수정 시 1.0초(18/19 스킵)로 실측 확인.

**완료 기준(DoD)**: ✅ 폴더 내 일부 파일만 수정한 뒤 재실행 시, 변경된 파일만 재인덱싱되고 나머지는 기존 인덱스를 재사용함을 확인. 전체 재인덱싱 대비 소요 시간이 유의미하게 단축됨(실측 117초 → 1.0초). T8.5까지 포함해 Phase 8 전체 완료.

**산출물**: `indexer/incremental/` 모듈

---

## Phase 9: exe 패키징 및 개별 PC 배포 테스트

**목표**: 인스톨러를 완성하고, 9.3절 대표 배포용 포터블 압축 배포 방식을 포함해 실제 배포 테스트를 수행한다.

**의존성**: Phase 1~8 전체 (전체 기능 완성 후 패키징)

### 작업 목록
- [ ] T9.1 PyInstaller exe 패키징 스크립트 작성
- [ ] T9.2 상대 경로 기반 포터블 구조 검증 (실행/모델/인덱스/캐시 경로 전부)
- [ ] T9.3 레지스트리 미사용 검증
- [ ] T9.4 사용자 폴더(`%LOCALAPPDATA%`) 설치 경로 적용 및 무권한 설치 테스트
- [ ] T9.5 Inno Setup 인스톨러 빌드 + 압축 옵션 적용
- [ ] T9.6 최초 구동 경험(대상 폴더 지정 → 백그라운드 인덱싱 안내) 검증
- [ ] T9.7 sLM 미포함 최소 인스톨러(약 800MB~1.2GB) 빌드 및 테스트
- [ ] T9.8 대표 배포용 포터블 패키지 절차 검증 (설치 → 모델 다운로드 → 폴더 압축 → 팀원 PC 압축 해제 → 즉시 실행)
- [ ] T9.9 동일 공유 폴더 대상 지정 시 기존 인덱스 재사용 여부 검증
- [ ] T9.10 최소/권장 사양 실기(또는 동급 스펙) 각각에서 설치~구동 전 과정 실측

**완료 기준(DoD)**: 최소 사양 PC와 권장 사양 PC 각각에서 인스톨러 설치, 최초 인덱싱, 검색, (권장 사양의 경우) AI 요약까지 전 과정이 IT 승인 없이 정상 동작. 포터블 압축 배포본을 별도 PC에서 압축 해제만으로 구동 확인.

**산출물**: exe 인스톨러, 포터블 배포 zip, 배포 테스트 리포트

---

## Phase 10: 배포 후 개선 백로그

**목표**: Phase 1~9 진행 중 "지금 당장은 아니지만 언젠가 하면 좋을" 아이디어가 나올 때마다 여기에 항목을 추가해 쌓아둔다. 정식 배포(Phase 9 DoD 충족) 이후, 우선순위를 다시 매겨 착수한다 — 이 절 자체가 진행 상황이 아니라 **todo list**다.

**의존성**: Phase 9(정식 배포) 완료 후 착수. 개별 항목은 관련 Phase 산출물에 의존(항목별로 명시).

### 백로그 항목

- [x] T10.1 **"원문 열기" 시 페이지/슬라이드/시트 위치로 딥링크** — ✅ **완료 (docx/pptx/xlsx만, 2026-08-13)**. PowerShell 서브프로세스로 Word/PowerPoint/Excel COM을 구동한다(pywin32 미사용). **hwp/hwpx는 이 PC에 한글이 없어 실측 검증이 불가능해 이번 범위에서 제외**(사용자 확정) — 별도 후속 과제로 남는다. Word/Excel은 읽기 전용으로 연다(사용자 확정). `search/office_link.py`의 순수 함수 `plan_open(hybrid_result)`가 청크 타입별로 무엇을 검색어로 쓸지 판단한다: pptx는 전 타입이 정확한 `slide_index`를 가져 `GotoSlide()`만으로 충분, docx TEXT/TABLE은 needle ladder(점점 짧게 재시도)로 `Range.Find`, xlsx TABLE은 `TableData.caption`(시트명)으로 시트 활성화 후 가장 긴 셀 값으로 `Cells.Find()`. **순수 점진적 개선**: COM이 없거나 실패하면 예외를 삼키고 조용히 `QDesktopServices.openUrl()`로 폴백 — "파일이 열리기는 한다"는 기존 보장을 절대 깨지 않는다. ~1~3초(프로세스 기동 포함) 걸려 `SearchWorker`와 같은 `QThread` 배경 작업(`OpenFileWorker`)으로 돌리되, "파일 없음" 판정만 클릭 즉시 동기로 해 기존 실패 테스트를 한 줄도 안 건드렸다. 🔴 **실제 Office COM 종단 검증에서 진짜 버그를 하나 잡았다** — 청커가 여러 Word 문단을 `"\n"`으로 이어붙이는데, needle ladder가 그 이어붙인 문자열을 글자 수로만 잘라 문단 경계를 넘는 needle을 만들면 Word의 문단 구분자(`\r`)와 안 맞아 20자짜리도 못 찾았다(실측). **문단 단위로 먼저 쪼갠 뒤 문단 안에서만** 사다리를 만들도록 고쳐 해결 — pptx·xlsx는 첫 실측에서 바로 성공했지만 docx는 이 수정 전까지 실패였다. 테스트 31건 추가(누적 722 passed / 5 skipped). 상세는 `PLAN` Phase 10 절.

- [x] T10.2 **구버전 포맷 변환 실패를 사용자에게 안내** — ✅ **완료 (2026-08-09)**. 근본 원인은 `LegacyOfficeParser`가 예외를 던지지 않고 `document.status=FAILED`만 남겨 `IndexReport.failures`가 애초에 채워지지 않던 것(`indexer/pipeline.py` 수정). `soffice`(LibreOffice 포터블)를 못 찾으면 상태바에 "구버전 문서를 변환하지 못했습니다(파일명). LibreOffice 포터블을 내려받아 vendor/LibreOfficePortable/ 폴더에 넣으세요." **정적 안내**를 표시한다(자동 다운로드·설치 없음, 사용자 확정). CLI(`indexer.cli`)도 같은 수정으로 실패 목록을 정상 출력하게 됐다. 테스트 9건 추가, 전체 469 passed. 상세는 PLAN Phase 10 절
- [x] T10.3 **"원문 열기" 실패가 화면에 안 뜨는 버그 수정** — ✅ **완료 (2026-08-09)**. `ResultCard`/`TableCard`/`ImageCard`가 파일을 못 찾으면 `open_failed`를 emit하지만, 지금까지 이 신호를 받는 곳이 어디에도 없었다 — 클릭해도 화면에 아무 반응이 없는 것처럼 보이던 실제 버그(다른 PC에서 만든 인덱스를 이 PC로 옮겨왔더니 파일 경로 대부분이 존재하지 않아 실사용 중 드러났다). `ResultList`가 카드의 `open_failed`를 한 자리로 relay하고, `MainWindow`가 T10.2에서 만든 `set_warning()`으로 사유를 상태바에 표시하도록 연결했다. 테스트 2건 추가(릴레이·종단), 전체 471 passed. 상세는 PLAN Phase 10 절
- [x] T10.4 **인덱싱 진행률 비모달 팝업 + 취소 버튼** — ✅ **완료 (2026-08-09, 사용자 요청)**. `ui/widgets/indexing_progress_dialog.py` 신설. TECH 4.6("메인 UI가 멈추지 않도록")을 지키기 위해 `exec()`가 아니라 `show()`로 띄우는 **비모달**로 확정(사용자 확정 — 완전 모달 전환 여지는 코드 주석에 남겨둠). 취소 버튼은 우측 하단, 클릭 시 `IndexingThread.stop_event`를 세팅해 다음 파일 처리 전에 멈춘다. 진행률 바 아래에 지금 처리 중인 파일 경로를 보여준다(길면 가운데 생략, 툴팁에 전체 경로) — 사용자 추가 요청으로 `_IndexingBridge.progress` 시그널에 경로 인자를 추가했다. 같은 DB에 인덱싱 스레드 두 개가 동시에 뜨지 않도록 가드 추가, 새 인덱싱 시작 시 이전 "원문 열기 실패" 경고가 진행률 문구와 한 줄에 겹쳐 보이던 것도 정리. **부수 발견(별건, 중요)**: `closeEvent`가 `IndexingThread`(threading.Thread)에 `QThread` 전용 API(`isRunning`/`wait`)를 호출해 인덱싱을 한 번이라도 돌린 뒤 창을 닫으면 항상 `AttributeError`가 나던 버그를 잡아 `is_alive()`/`join()`으로 수정 — Phase 4·6에서 각각 만든 크래시 수정 커밋 이후 한 번도 실제로 실행된 적 없는 경로였다. 테스트 10건 추가, 전체 481 passed. 상세는 PLAN Phase 10 절
- [x] T10.5 **재인덱싱해도 이전 문서가 계속 쌓이는 버그 수정** — ✅ **완료 (2026-08-09)**. 사용자가 "다시 인덱싱했는데 문서 개수가 이상하다"고 보고해 발견. 두 가지가 겹쳐 있었다. **① 진짜 결함**: `index_folder()`가 이전에 인덱싱된 문서를 지우는 로직 자체가 없어, 대상 폴더를 바꿔도 옛 폴더 문서가 영구히 쌓였다(다른 PC 세션 흔적까지 검색에 섞여 나옴). "새 폴더로 완전히 교체"로 사용자 확정 — 매 인덱싱마다 **이번 스캔에서 발견되지 않은 문서를 먼저 지운다**(`_prune_stale_documents()`). 처음엔 "대상 폴더 바깥이면 지운다"(경로 접두사)로 짰다가, **같은 폴더 안에서 파일이 서브폴더로 옮겨진 경우**를 못 잡는 걸 재검증 중 실제로 겪어(파일 10개인데 문서 17개로 나옴) 접두사 비교를 버리고 이번 스캔 결과와 직접 대조하도록 다시 고쳤다. **② 별건, 테스트 위생 문제**: `AppState.save()`가 인자 없이 불리면 항상 진짜 `data/app_state.json`에 썼다 — 테스트가 `state=AppState()`로 만든 뒤 인덱싱을 트리거하면 **실제 앱 상태 파일이 pytest 임시 경로로 오염**됐다(Phase 4부터 있던 버그, 이번에 처음 발견). `AppState`가 `load()`한 경로를 기억해 `save()` 시 그 경로를 쓰도록 구조를 고치고, 오염 경로였던 기존 테스트 9곳을 격리된 `tmp_path`로 옮겼다. 오염된 실제 `data/index.sqlite3`·`data/app_state.json`을 정리하고 올바른 폴더로 재인덱싱(10개 문서, 522청크 확인). **부수 영향**: 재인덱싱으로 `chunk_id`가 바뀌어 Phase 6 sLM 테스트셋(`data/slm_testset.json`)이 무효화됨 — 권장 사양 PC 측정 전에 재생성 필요. 테스트 7건 추가(격리 수정 9곳은 기존 테스트 재사용), 전체 488 passed. 상세는 PLAN Phase 10 절
- [x] T10.6 **파일명 매치가 본문 매치처럼 보이는 문제** — ✅ **완료 (2026-08-13)**. T10.8이 재순위용으로 이미 계산해두고 있던 "본문+캡션에 실제로 포함된 검색어 수"(`HybridResult.matched_terms`, 파일명은 의도적으로 제외하고 셈)가 정확히 필요한 신호였다 — `matched_terms == 0`이면 파일명에만 걸린 결과다. 새 계산 로직 없이 `HybridResult.is_filename_only_match` property 하나만 추가하고, `card_common.build_card_header()`(세 카드 타입 공유)에 "파일명 매치" 배지를 붙였다(§5.6 "관련성 낮음"과 동일 스타일, 독립적으로 동시에 뜰 수 있음). 백로그가 제시한 3가지 방향 중 "표시"만 채택 — 문서당 1건으로 묶기·컬럼 가중치 재검토는 범위 밖으로 남겼다(파일명 검색 자체는 의도된 사용성이라 신중하게 접근). 실사용 검증(실제 인덱스, "김성용" 검색): 원래 버그 리포트와 동일한 20개 결과 전부에 배지가 정확히 붙는 것을 위젯 조회로 확인. 테스트 8건 추가(누적 683 passed / 5 skipped). 상세는 PLAN Phase 10 절, DESIGN §5.6b.
- [x] T10.7 **인덱싱 진행률 팝업에 경과/예상 남은 시간 표시** — ✅ **완료 (2026-08-13)**. 최근 5개 표본의 이동평균으로 남은 시간을 추정한다(전체 평균이었다면 구버전 포맷 구간이 지나가도 계속 느리게 잡힌다). 표본이 2개 모이기 전에는 "남음" 자체를 안 보여주고, 총량 미확정(marquee)·이미 완료 상태에서도 안 보여준다 — 경과 시간은 항상 정확하니 첫 업데이트부터 바로 보여준다. `IndexingProgressDialog`가 이미 파일마다 `set_progress(done, total, path)`를 받고 있어 **`_IndexingBridge`/`IndexingThread`/`main_window.py`는 전혀 안 건드리고 다이얼로그 내부에서만** 시각을 잰다(TASK 메모의 예상보다 범위가 작아짐). `time_source` 매개변수로 시계를 주입 가능하게 만들어 가짜 시계로 결정론적 테스트를 작성했다(Phase 8이 시간 대신 호출 횟수로 검증했던 것과 같은 이유). 실사용 검증(실제 exdoc 사본, 19개 문서 재인덱싱): 경과 시간이 계속 올라가고, 느린 구버전 포맷 구간에서 "남음" 추정치가 늘어났다가 빠른 구간에서 다시 줄어드는 것을 실측 로그로 확인, 완료 시 "남음" 문구가 정상적으로 사라짐. 테스트 8건 추가(누적 691 passed / 5 skipped).
- [x] T10.8 **검색어를 모두 포함한 결과를 상단에 노출** — ✅ **완료 (2026-08-11, 사용자 요청)**. 재순위가 벡터 유사도만 보고 정렬해, 사용자가 입력한 단어를 전부 담은 청크가 한 단어만 담은 청크에 밀리고 있었다(실측: `rpm 패키지 삭제 옵션` 질의에서 4/4 청크가 1/4 청크(유사도 0.671 vs 0.582)에 밀려 2위, `리눅스 프로세스 명령어`도 3/3이 2/3에 밀려 2위). **FTS5 후보 수집은 손대지 않았다** — 전체 일치 청크가 BM25 상위에 이미 들어오는 것을 확인했고(62개 중 1위, 100개 중 6위) 문제는 그다음 재순위였다. `search/hybrid_search.py:_rerank()`의 정렬 키를 **(관련성 낮음 여부, 일치 개수 ↓, 유사도 ↓)**로 바꿨다 [사용자 확정]: 일치 개수를 유사도보다 우선하되, 흐림 처리된 카드가 1위에 올라 고장처럼 보이지 않도록 "관련성 낮음"은 그보다 앞선 정렬 키로 뒀다(DESIGN §5.6과 충돌 방지). **일치 개수는 본문+표 캡션으로만 세고 파일명은 제외한다** — 파일명까지 세면 같은 파일의 청크가 전부 동점이 돼 순위가 하나도 안 바뀐다(실측: "코치 윤리규정 준수"에서 상위 10건 전부 3/3 동점). T10.6이 지적한 "파일명 매치가 본문 매치처럼 보이는 문제"를 **순위 측면에서는 부분적으로 완화**한다(본문 매치가 위로 올라옴) — 다만 파일명만 걸린 청크가 결과에 끼는 것 자체는 그대로라 T10.6은 계속 열려 있다. 조사 처리는 FTS5와 같은 로직(`query_term_variants()`)을 재사용해 판정이 어긋나지 않게 했다. 테스트 9건 추가(정렬 규칙은 유사도를 직접 지정한 단위 테스트로 분리 검증 — 실제 임베딩으로는 "전체 일치인데 유사도가 낮은" 상황을 의도적으로 만들 수 없었다), 전체 585 passed.
- [x] T10.9 **권장 모드에서 AI 요약이 항상 "관련 문서를 찾을 수 없습니다"만 나오는 버그 수정** — ✅ **완료 (2026-08-11, 사용자 보고)**. `ui/search_worker.py`가 `hybrid_search()`를 부를 때 `embedder=`만 넘기고 **`profile=`은 안 넘기고 있었다**. `hybrid_search`는 `profile`이 없으면 내부적으로 `get_profile()`(기본 경량)로 저장된 벡터를 조회하는데, 권장 모드(HEAVY)에서는 이게 `embedder`가 실제로 쓴 프로파일과 어긋난다 — 벡터는 있는데 차원(768 vs 1024)이 안 맞는 것으로 처리돼 **모든 결과의 `similarity`가 조용히 `None`이 됐다**. AI 요약 1단계 안전장치는 `similarity is None`을 "관련성을 판단할 수 없다"로 보고 전부 차단하므로, 권장 모드에서는 검색 결과가 정상적으로 나와도 AI 요약만 항상 "관련 문서를 찾을 수 없습니다"였다. `SearchWorker._search()`가 `profile=self._embedder.profile`을 함께 넘기도록 수정. **Phase 7.5 전까지는 이 경로가 한 번도 실사용된 적이 없어(권장 모드가 실제로 설치될 수 없었다) 드러나지 않았다** — `SearchWorker`를 단독으로 검증하는 테스트가 그동안 하나도 없었고, `MainWindow` 통합 테스트는 전부 경량(기본값)으로만 돌아 `profile=` 누락이 가려져 있었다. `tests/test_ui_search_worker.py` 신설(3건) — 수정 전 상태로 되돌려 실제로 실패하는지 확인 후 복원. 전체 588 passed. 실제 앱에서 권장 모드로 전환→검색→AI 요약까지 실제 답변이 나오는 것을 종단 확인. 부수 발견: 실제 인덱스(문서 19개·청크 607개)에 권장 모델 벡터가 아예 없던 것도 같이 확인돼 재인덱싱으로 채웠다(PC 성능을 바꿔도 재인덱싱은 자동으로 안 된다는 Phase 7.5의 기존 발견과 같은 원인).
- [x] T10.10 **표·이미지 카드는 "관련성 낮음"이어도 흐림 처리가 안 되던 버그 수정** — ✅ **완료 (2026-08-11, 사용자 보고)**. DESIGN §5.6의 흐림(opacity 0.5) + `[relevance="low"]` QSS 훅은 Phase 4에서 텍스트 카드(`result_card.py`)에만 구현됐고, Phase 5에서 표·이미지 카드를 만들 때 `card_common.py`의 `build_card_header()`가 "관련성 낮음" **라벨**은 세 카드 공통으로 붙여줬지만 실제로 흐려 보이게 하는 `QGraphicsOpacityEffect` 적용은 옮겨 붙이는 걸 빠뜨렸다 — 그래서 표·이미지 카드는 라벨만 뜨고 안 흐려졌다. `card_common.py`에 `apply_low_relevance_style()`을 신설해 세 카드가 공유하도록 고쳤다(`result_card.py`의 중복 상수·로직 제거). **표·이미지 카드에는 이 라벨/흐림을 검증하는 테스트가 그동안 하나도 없었다** — 텍스트 카드만 테스트돼 있었다. 회귀 테스트 4건 추가(표·이미지 각 2건, 수정 전으로 되돌려 실제로 실패하는 것 확인 후 복원). 실제 앱에서 표·이미지 카드가 각각 `relevance=low`·`dimmed=True`로 뜨는 것 확인. 전체 592 passed.
- [x] T10.11 **일치 개수가 같으면 본문이 긴(자세한) 결과를 우선 노출** — ✅ **완료 (2026-08-14, 사용자 요청)**. 실제 검색 결과 화면을 보던 중 "이렇게 내용이 있는 항목을 최상단에 올릴 수 있나"는 요청으로 시작. T10.8이 확정한 정렬 키(관련성 낮음 여부 → 일치 개수 ↓ → 유사도 ↓)에 **본문 길이 ↓**를 일치 개수와 유사도 사이에 추가했다[사용자 확정 — 유사도보다 본문 길이를 우선]. `search/hybrid_search.py:_rerank()`의 두 갈래(벡터 있음/없음)와 임베딩을 못 쓸 때의 폴백 정렬까지 세 곳 모두 같은 규칙으로 맞췄다. 새 계산 로직 없이 이미 있는 `HybridResult.content`의 길이를 그대로 쓴다. 기존 T10.8 테스트 중 "같은 일치 개수 안에서는 유사도 내림차순"을 확인하던 테스트가 이제 본문 길이까지 버킷 키에 넣어야 성립해 수정했고, "같은 일치 개수라도 본문이 길면 유사도가 더 높은 결과를 앞선다"를 직접 검증하는 테스트를 추가했다. 테스트 1건 순증(1건 수정 + 1건 추가 − 1건 대체), 전체 723 passed.
- [x] T10.13 **검색·챗봇 결과 개수를 상위 5개로 통일 + "더보기" 버튼** — ✅ **완료 (2026-08-14, 사용자 요청)**. 일반 검색은 `SearchWorker`가 가져온 최대 20개를 전부 카드로 그렸고, 챗봇 즉시 발췌는 top-1 하나만 보여줘 개수 기준이 서로 달랐다. 둘 다 **5개 + "더보기" 버튼**(클릭 시 나머지 전부 펼침, 재클릭 없음)으로 통일했다 — 처음엔 챗봇 쪽만 "N개 결과가 더 있습니다" 텍스트 안내로 갔다가, 사용자가 같은 날 바로 "챗봇도 검색과 같은 버튼으로" 요청해 `ResultList`와 완전히 같은 컴포넌트(`ResultListMoreButton`)로 통일했다. **사용자가 "2~5순위도 각자 원문 열기 버튼을 갖게" 확정한 것이 설계를 크게 단순화했다** — 챗봇 발췌 항목 하나하나가 검색 카드와 완전히 같은 것(출처·타입별 렌더링·원문 열기·표 복사·이미지 확대)이 필요해져서, 손으로 새로 만들지 않고 검색 카드(`ResultCard`/`TableCard`/`ImageCard`)를 말풍선 안에 그대로 재사용했다 — T10.12가 챗봇 전용으로 짰던 렌더링(표/이미지 갈아 끼우기, 높이 재계산, 복사/확대 버튼)이 전부 필요 없어져 코드가 더 단순해졌다. `result_list.py`의 카드 분기 함수를 `ui/widgets/card_dispatch.py`(신규)로 옮겨 `chat_panel.py`와 공유했다(두 모듈이 서로를 import해 순환 임포트가 생기는 문제 회피). 백엔드(`SearchWorker`, 최대 20개)는 손대지 않고 표시 단계에서만 자른다. 실제 인덱스로 "검진" 검색(13건 히트) → 검색 카드 5개+더보기(8개 더)→클릭 시 13개, 챗봇 카드 5개+"8개 결과가 더 있습니다" 안내로 정확히 일치하는 것 확인. 테스트 다수 갱신·추가(누적 735 passed / 5 skipped). 상세는 `DESIGN` §5.1·§5.8.
- [x] T10.12 **챗봇 즉시 발췌도 표/이미지를 검색 화면처럼 렌더링** — ✅ **완료 (2026-08-14, 사용자 요청)**. Phase 7.6 완료 시점에 "표·이미지 청크가 top-1일 때 원시 텍스트로 나온다"고 알려진 채 범위 밖으로 남겨뒀던 것을 해결. `TableCard`/`ImageCard`에만 갇혀 있던 렌더링 로직(`_build_grid`/`_fix_grid_height`/`_to_tsv`/`_load_thumbnail`/`_zoom`)을 `card_common.py`로 끌어올려 공개 함수화했다 — T10.10이 "카드 하나에만 있어서 다른 카드가 빠뜨렸다"로 겪었던 함정을 처음부터 피하기 위해서다. `_AnswerBubble`의 고정 `QLabel` 자리를 갈아 끼울 수 있는 본문 컨테이너로 바꿔, `show_excerpt()`가 top-1 타입에 따라 텍스트/표 그리드/썸네일+안내로 다시 그린다(`ResultList.show_results()`와 같은 "지우고 다시 그리기" 패턴). 검색 화면과 동일하게 "표 복사"·"확대" 버튼도 얹었다[사용자 확정 — 표뿐 아니라 이미지도]. `TableCard.showEvent()`의 "화면 부착 전/후" 높이 재계산 함정(Phase 5)을 챗봇 말풍선에도 그대로 옮겨 붙였다. 실제 인덱스로 "검진항목표" 검색 → 챗봇 발췌가 175행×12열 실제 그리드로 렌더링되는 것을 확인. 테스트 7건 추가(누적 730 passed / 5 skipped).
- [x] T10.14 **일반 검색 결과 카드마다 개별 "AI 요약 보기" 버튼 추가** — ✅ **완료 (2026-08-15, 사용자 지적)**. "AI 요약은 검색 결과 각 항목마다 있어야 하지 않나"는 질문으로 시작 — 확인해보니 Phase 7.6 이후 AI 요약은 챗봇 모드에만 있고, 그마저도 턴 전체를 대상으로 한 버튼 하나뿐이었다(일반 검색 화면에는 요약 기능 자체가 없었다). 사용자가 "카드마다 개별 버튼"으로 확정. `card_common.py`에 `SummarySection`(버튼 + `SummaryCard` 한 벌)을 신설해 `ResultCard`/`TableCard`/`ImageCard` 세 카드가 공유하고, `ResultList.show_results(..., ai_summary_available=...)`로 카드 생성 시점에 켤지 결정한다 — **모델 미설치 시 자동으로 숨긴다**(`_refresh_ai_chat_availability()`가 계산하는 값을 그대로 재사용). 챗봇 말풍선은 카드를 그대로 재사용하지만(T10.13) `show_summary=False`로 꺼서 턴 단위 버튼과 중복되지 않게 했다. `SummaryWorker`의 신호(`started_loading`/`succeeded`/`failed`)를 `SummarySection`의 메서드에 **직접 연결**한다(중간에 람다를 두지 않음) — Qt는 연결된 슬롯이 속한 QObject가 파괴되면 연결을 자동으로 끊어주므로, 새 검색으로 카드가 사라진 뒤 뒤늦게 응답이 와도 죽은 위젯을 건드리는 크래시로 이어지지 않는다(새 검색 도중 요약 요청 테스트로 확인). 🔴 **테스트 작성 중 이 PC의 실제 임베딩 유사도가 실행마다 흔들리는 것을 다시 확인**(Phase 3·7이 이미 겪은 int8 양자화 배치 스케일 문제) — 작은 테스트 픽스처의 실제 유사도가 요약 임계값(0.5)을 못 넘겨 배선 자체를 검증할 수 없었고, Phase 7과 같은 해법(유사도를 직접 지정해 배선만 분리 검증)으로 해결했다. 테스트 8건 추가(누적 749 passed / 5 skipped, 기존에 알려진 `TestPerformanceComboStartupSync`의 ONNX 워밍업 스레드 크래시는 무관 — clean tree에서도 동일하게 재현됨을 확인). 실제 앱에서 카드별 버튼 표시 → 클릭 → 그 카드에만 "AI 요약" 섹션이 펼쳐지고 다른 카드는 영향받지 않는 것을 스크린샷으로 확인.
- [x] T10.15 **검색 모드↔챗봇 모드 질문/결과 완전 분리** — ✅ **완료 (2026-08-15, 사용자 보고)**. "AI 챗봇 미사용으로 검색 후 챗봇으로 이동하면 검색 결과가 그대로 나온다"는 보고로 확인 — 버그가 아니라 Phase 7.7이 의도적으로 넣은 동작이었다(`_activate_chat_mode()`가 `_last_query`가 있으면 그대로 챗봇 첫 메시지로 자동 전송, "패널이 빈 채로 뜨면 토글이 안 먹는 것처럼 보인다"는 이유). 세 선택지(완전 분리/현행 유지/자동전송만 제거)를 제시해 **완전 분리**로 확정[사용자 선택, 추천안]. `_activate_chat_mode()`의 자동 전송 코드 6줄을 제거 — 챗봇을 켜면 항상 빈 챗창으로 시작한다. **반대 방향(챗봇에서 검색으로 복귀)은 이미 분리돼 있었다** — `_on_chat_message_sent`가 애초에 `_last_query`/`_last_results`를 건드리지 않고 `_chat_questions` 딕셔너리에 턴별로 따로 기록하므로, 챗봇을 끄면 챗봇에서 한 것과 무관하게 직전 검색 결과가 그대로 보인다(코드 변경 불필요, 재확인만). 자동 전송을 전제로 한 기존 테스트(`test_toggle_on_auto_sends_last_query_as_first_message`)를 반대 기대값(`turn_count() == 0`)으로 교체. 전체 57 passed(기존에 알려진 `TestPerformanceComboStartupSync` ONNX 크래시 제외). 상세는 `PLAN` Phase 10 절.
- [x] T10.16 **챗봇 껐다 켜도 이전 대화가 남아 있도록 수정** — ✅ **완료 (2026-08-15, 사용자 요청)**. T10.15 직후 실사용 테스트 중 "껐다 켜도 이전 챗봇 대화가 남아 있으면 좋겠다"는 요청 — `_activate_chat_mode()`가 토글마다 `ChatPanel()`을 새로 만들어 대화가 매번 초기화되고 있었다. `MainWindow`에 `_chat_panel_cache`(생성된 인스턴스를 계속 들고 있는 자리)를 신설해 첫 활성화 때만 만들고 이후엔 재사용한다 — `_chat_panel`(지금 화면에 붙어 있는지 여부를 나타내는 기존 플래그)은 그대로 둬서 나머지 코드 전체(`if self._chat_panel is not None: ...`가 여러 곳에 있음)를 손대지 않았다. 🔴 **`ResultList._clear()`가 챗봇 패널까지 통째로 `deleteLater()`하고 있어서 재사용이 원천적으로 막혀 있었다** — 모드 전환마다 이전 위젯을 없애는 범용 로직인데, 패널은 예외로 두어야 했다. `objectName() == "ChatPanel"`(패널이 `card_count()`에 안 잡히려고 이미 갖고 있던 마커, T7.6)로 판별해 그 경우만 `deleteLater()`를 건너뛰도록 수정 — 새 계산 로직 없이 기존 마커를 재활용했다. 테스트 2건 추가(`ResultList`가 패널을 파괴하지 않는지, `MainWindow`가 같은 인스턴스를 재사용하고 턴이 유지되는지). 전체 751 passed / 5 skipped. 실제 앱에서 챗봇 메시지 전송 → 끄기 → 켜기 시 같은 대화가 그대로 보이는 것을 스크린샷으로 확인.
- [x] T10.17 **챗봇 AI 요약이 이전 대화를 참고하도록 수정 (대화 맥락 유지)** — ✅ **완료 (2026-08-15, 사용자 요청)**. "이어나가는 대화를 구현했으면 좋겠다"는 요청 — 검색(①)과 생성(②) 두 단계를 분리해서 접근했다[사용자 확정]. **검색(①)은 그대로 매 턴 독립(stateless)** — Phase 7.6이 "검색에 LLM을 끼워 넣는" 설계를 이미 두 번 반려한 전례(지연 11~79초, "은행 앱처럼 빠르게" 요구와 충돌)를 반복하지 않기 위해 질의 재구성용 LLM 호출은 추가하지 않았다. **생성(②, "AI 요약 보기")만 대화형으로 만들었다** — 이미 사용자가 클릭해서 LLM 지연을 감수하는 지점이라 여기에 이전 턴을 얹어도 체감 지연이 늘지 않는다. `slm/prompt.py`에 `HistoryTurn`·`format_history()`를 신설하고 `build_messages()`에 `history` 매개변수를 추가했다 — **`history=()`(기본값)일 때는 이전과 완전히 동일한 문자열**을 만들도록 회귀 테스트로 고정해뒀다(Phase 6/7이 실측한 기권정확도 81.8% 등 수치가 이 "이력 없음" 경로 기준이라, 히스토리 기능을 안 쓰는 호출까지 조용히 달라지면 그 수치가 무효화된다). 이력은 "맥락 참고용, 근거로 쓰지 말 것"을 프롬프트에 명시해 TECH 5.3의 근거 강제 취지와 충돌하지 않게 했다. **새 저장소를 만들지 않았다** — T10.16이 챗봇 패널을 계속 들고 있게 만들어둔 덕에, `ChatPanel.history_before(request_id)`가 기존 말풍선들(`_bubbles`)에서 질문·완성된 답변을 그대로 읽어간다(성공(`SummaryStatus.OK`) 턴만 포함 — 기권·근거없음·실패는 노이즈만 된다). `MainWindow._on_chat_summarize_requested()`가 이 이력을 `SummaryWorker`에 실어 보낸다. 최근 3턴, 답변 최대 300자로 제한(n_ctx 4096에 발췌 5건까지 이미 여유가 빠듯해서). 테스트 다수 추가(`slm/prompt.py` 7건, `slm/summarize.py` 2건, `ChatPanel.history_before()` 4건, `MainWindow` 통합 1건 — 실제 QThread/SummaryWorker 경로로 두 번째 턴 프롬프트에 첫 턴의 질문·답변이 실제로 실리는 것을 스텁 LLM으로 확인). 전체 764 passed / 5 skipped. 실제 로컬 sLM으로 종단 확인을 시도했으나 이 세션에서 응답 지연이 예상(측정치 18~34초)보다 길어(스크립트 대기 시간 초과) 결론을 못 냈다 — 스텁 기반 통합 테스트가 실제 프로덕션 코드 경로(LLM 백엔드만 대체)로 이미 배선을 증명해 추가 조사는 보류.
- [x] T10.18 **대명사만 있는 챗봇 후속 질문이 검색 0건으로 막히는 문제 수정** — ✅ **완료 (2026-08-15, 사용자 보고 + 실사용 디버깅)**. T10.17 검증 중 실제 인덱스로 "코치 자격시험 응시 방법이 뭐야?" → "그건 얼마야?"를 이어 물었더니 후속 질문이 **검색(①) 단계에서부터 0건**이 나와, "AI 요약 보기" 버튼 자체가 비활성화돼(Phase 7.6부터 있던 로직: `show_excerpt([])`가 버튼을 안 켜고 조기 반환) 대화 이어가기를 시도조차 할 수 없었다. 처음엔 "이력 텍스트만으로 LLM이 답하게 하자"는 안을 검토했으나, **TECH 5.3의 근거 강제 원칙(답은 오직 `[발췌]`에서만)과 정면 충돌**한다는 걸 재검토 중 확인 — 이력만으로 만든 답은 `[N]` 인용을 붙일 근거 청크가 없어 3·4단계 안전장치(출처 표기·겹침 검증)가 무력화된다. 대신 **더 앞단(① 검색)에서 고쳤다** — `SearchWorker`에 `fallback_query` 매개변수를 추가해, 이번 메시지만으론 결과가 0건일 때만 **직전 턴의 질문 텍스트를 붙여 한 번 더 검색**한다(`hybrid_search()` 무수정, LLM 호출 없음). 검색 결과가 생기면 "AI 요약 보기" 버튼도 기존 로직 그대로 자연히 켜져 **버튼 문제가 부수적으로 함께 해결**됐다 — 별도 UI 수정이 필요 없었다. 이미 결과가 있는 검색에는 폴백이 전혀 개입하지 않아(일반 검색 모드는 `fallback_query`를 안 넘겨 영향 없음) T10.15의 "검색·챗봇 완전 분리" 원칙과도 충돌하지 않는다. **원인 조사 과정에서 실제 재현 실패가 반복됐다** — 유사도 재현(3회 완전히 동일한 값)으로 배치 잡음 가설을 기각했고, 실시간 로그(`print(flush=True)` 임시 계측)로 "관련 문서를 찾을 수 없습니다"(NO_EVIDENCE, 모델 미호출)와 "문서에서 찾을 수 없습니다"(ABSTAINED, 모델이 실제로 기권)가 문구는 거의 같지만 원인이 다르다는 걸 구분해냈다 — 키워드 스타일 질문("코치 응시")은 유사도가 임계값 근처(0.5대)라 통과해도 모델이 "뭘 답해야 할지 불분명"하다며 기권하는 경우가 있었고, 완결된 질문("...뭐야?")으로 바꾸자 유사도(0.73)도 응답(OK)도 확실히 개선됐다(관련 없는 별개 현상). 테스트 8건 추가(`SearchWorker` 폴백 로직 4건, `MainWindow` 통합 1건 — 직전 질문을 붙인 폴백 검색이 실제로 결과를 살리고 버튼이 켜지는지 확인). 전체 769 passed / 5 skipped. 실제 앱(스크립트 기반) 종단 검증: "그건 얼마야?" 단독 0건 → 폴백 검색으로 1건 발견 → 버튼 활성화까지 확인.
- [x] T10.19 **챗봇 검색·AI 요약 진행 중 스크롤이 안 따라가는 문제 수정** — ✅ **완료 (2026-08-15, 사용자 보고)**. "AI 요약 버튼을 누르면 이전 메시지 위치로 올라가 버림", "다음 검색을 하면 직전 검색 끝부분에 멈춰 방금 보낸 질문이 안 보임" 두 가지 보고. 원인은 `_scroll_to_bottom()`이 `_send()`(메시지 전송)에서만 불렸고, 검색 결과 도착(`show_excerpt`)이나 AI 요약 진행 단계 전환(`show_summary_starting`/`_generating`/실제 답변/오류) — 전부 말풍선 높이를 바꾸는 지점들 — 에서는 한 번도 재호출되지 않던 것이었다. `ChatPanel`의 이 여섯 지점 전부에 스크롤 재조정을 추가했다. **처음엔 기존 `QTimer.singleShot(0, ...)` 패턴을 그대로 재사용했는데 실측으로 두 가지 결함을 더 발견했다**: ① 0ms 뒤 타이머가 실행되는 시점에도 레이아웃이 아직 안 끝나 있어(`processEvents()`를 10번 돌려도 `maximum()`만 갱신되고 `value()`는 그대로 멈춤) `bar.maximum()`이 갱신 전 값을 돌려주는 경우가 실제로 있었다 — `rangeChanged` 신호에 맞춰 스크롤하도록 바꿔 해결 ② 레이아웃이 **한 번에 안 끝나고 여러 번(예: 348→418→482)에 걸쳐 재계산되는 경우**도 있어, `rangeChanged`를 한 번만 받고 바로 연결을 끊으면 중간값에서 멈췄다 — 짧은 유휴 타이머(120ms)로 디바운스해 레이아웃이 잠잠해질 때까지 계속 따라가도록 고쳤다. 테스트 4건 추가(작은 뷰포트로 실제 오버플로를 만들어 각 지점에서 스크롤이 맨 아래에 도달하는지 확인). 전체 773 passed / 5 skipped. 실제 앱(스크린샷)으로 AI 요약 완료 시점·두 번째 메시지 전송 후 모두 최하단에 정확히 도달하는 것을 확인.
- [x] T10.20 **결과 카드 텍스트 드래그·복사(Ctrl+C) 안 되던 문제 수정** — ✅ **완료 (2026-08-15, 사용자 보고)**. AI 요약 카드(`SummaryCard`)와 챗봇 안내 문구는 이미 `TextSelectableByMouse`가 걸려 있었지만, **정작 가장 많이 보는 검색 결과 카드의 본문·파일명·위치 라벨에는 처음부터 빠져 있었다.** `result_card.py`의 본문(`ResultCardBody`)과 `card_common.py`의 헤더 공유 함수(`build_card_header()`)에 있는 파일명·위치 라벨에 플래그를 추가 — 표/이미지 카드도 같은 헤더 함수를 쓰므로 세 카드 타입 전부 한 번에 적용됐다(T10.10과 같은 "공유 함수 하나만 고치면 세 카드 다 해결" 패턴). 테스트 1건 추가(선택 플래그가 실제로 걸려 있는지 확인), 전체 774 passed / 5 skipped. 실제 앱에서 본문 텍스트를 드래그 선택 → 클립보드 복사까지 스크립트로 종단 확인.
- [x] T10.21 **결과 카드에 "근처 내용 더보기" 추가 (헤딩만 매치돼 실제 내용이 안 보이는 문제)** — ✅ **완료 (2026-08-15, 사용자 요청)**. "'코치 자격시험 응시 방법이 뭐야'로 검색하면 헤딩 한 줄짜리 카드가 1위로 나오는데, 같은 섹션에 있는 표(실제 내용)를 보여줄 수 있나"는 요청. **원인은 파서의 청킹 구조다** — 헤딩 문단("-코치인증자격시험 응시방법-", 15자)과 바로 뒤에 오는 실제 내용(단계별 설명 표)이 서로 다른 청크로 저장돼 있고(Phase 1 결정: 표는 구조 보존을 위해 의도적으로 분리), 검색어가 짧은 헤딩과 거의 그대로 겹치면 헤딩 청크만 상위로 올라온다. 세 방향(더보기 버튼 / 짧은 헤딩 자동 병합 / 파서 수정) 중 **"근처 내용 더보기" 버튼**으로 확정[사용자 선택] — 자동 병합은 "짧은 텍스트" 판정 기준이 필요해 오판 위험이 있고(진짜 짧은 답변에도 꼬리가 붙음), 파서 수정은 전체 재인덱싱 + Phase 1 청킹 원칙 변경이라 범위가 크다. `search/chunk_neighbors.py`(신규)의 `fetch_next_chunk()`가 같은 문서 안에서 삽입 순서(내부 PK) 기준 바로 다음 청크를 찾는다 — **`chunks`에 문서 내 순서 컬럼이 따로 없지만 `store_document()`가 원본 순서대로 삽입하므로 PK 순서 = 문서 내 순서**다. UI는 `card_common.py`에 `NearbySection`을 신설해 세 카드 타입이 공유하고(T10.14 `SummarySection`과 같은 패턴), `ResultList`/`ChatPanel`이 `open_failed`와 같은 방식으로 `nearby_requested`를 한 자리로 릴레이한다. **AI 요약과 달리 모델 설치 여부와 무관하게 항상 보인다**(LLM·임베딩을 안 쓰는 단순 SQLite 조회라 워커 스레드도 없이 메인 스레드에서 바로 처리). 다음 청크가 표·이미지여도 그리드/썸네일로 다시 그리지 않고 `content`를 텍스트로 보여준다 — `content`가 이미 표를 읽을 수 있는 텍스트로 담고 있어 이번 문제 해결에는 충분하고, 그리드 렌더링은 `showEvent` 높이 재계산(Phase 5) 같은 "화면 부착 후" 로직이 얽혀 있어 필요해지면 나중에 붙인다. 테스트 10건 추가(`fetch_next_chunk` 4건 — 다음 청크/마지막 청크/문서 경계 안 넘기/없는 chunk_id, 카드 위젯 4건, `MainWindow` 실제 SQLite 왕복 통합 2건), 전체 784 passed / 5 skipped. 실제 인덱스로 사용자가 보고한 바로 그 헤딩 청크(`..._text_00006`)에서 다음 청크(`..._table_00007` = 두 번째 스크린샷의 그 표)가 정확히 나오는 것을 확인.
- [x] T10.22 **AI 요약 진행 표시 개선 + 최근 검색 최신순 정렬** — ✅ **완료 (2026-08-15, 사용자 요청)**. 두 가지를 함께 반영했다. **① AI 요약 진행 표시**: 문구를 사용자 지정안으로 교체(모델 준비 중 → "AI 모델을 준비하는 중입니다… (첫 실행은 시간이 걸립니다.)", 생성 중 → "AI 요약을 진행하고 있습니다. 성능에 따라 시간이 소요됩니다.")하고, **"생성형 AI처럼 지금 동작 중임을 알려주는 효과"** 요청에 따라 부드러운 깜박임을 추가했다 — `QGraphicsOpacityEffect` + `QPropertyAnimation`(1.2초 주기, 불투명도 1.0↔0.35, `InOutSine`으로 부드럽게, 무한 반복). **본문 라벨에만 효과를 걸었다** — 카드 전체가 깜박이면 제목·배지까지 흔들려 산만하다. 효과 객체는 계속 붙여두고 애니메이션만 시작/정지한다(붙였다 뗐다 하면 그때마다 레이아웃이 재계산돼 미세하게 흔들린다). `state == "pending"`일 때만 돌고, 결과·기권·실패로 전이하면 즉시 멈추면서 불투명도를 1.0으로 복원한다 — 안 그러면 멈춘 자리의 흐린 값이 그대로 굳어 완성된 답변이 흐리게 보인다. 실측으로 불투명도가 1.0→0.377→1.0으로 실제 왕복하는 것과 완료 즉시 1.0으로 복원되는 것을 확인. **② 최근 검색 최신순**: `RecentSearches._render()`가 `AppState.recent_searches`(이미 최신이 맨 앞)를 **뒤집어서** 최신을 맨 아래에 쌓고 있었다(Phase 7.7의 "위에서 아래로 자란다" 해석) — 방금 쓴 검색어를 목록 아래에서 찾아야 해 불편하다는 지적으로, 뒤집지 않고 그대로 쓰도록 바꿨다(최신이 맨 위, 공간이 부족하면 아래쪽 오래된 항목부터 빠짐). 기존 정렬을 검증하던 테스트 3건을 반대 기대값으로 갱신. 테스트 4건 추가(깜박임 시작/정지/복원), 전체 788 passed / 5 skipped. 실제 앱에서 3번 연속 검색 후 마지막 검색어가 맨 위에 오는 것을 스크린샷으로 확인.

- [x] T10.23 **AI 챗봇 모드에서 AI 요약 자동 생성 (2단 응답의 ②를 버튼 없이)** — ✅ **완료 (2026-08-18, 사용자 제안)**. "시간이 걸리더라도 챗봇 모드에서는 검색 결과에 대한 AI 요약을 보여주면 어떨까"는 제안에서 출발. 직전 대화에서 **검색 모드와 챗봇 모드가 첫 턴에 사실상 같은 화면**이라는 문제가 드러났는데(같은 `SearchWorker`에 같은 필터를 넘기고, T10.13이 개수·카드까지 통일해 실제로 다른 건 `fallback_query`(T10.18)·이력 누적(T10.16)·이력 주입(T10.17) 셋뿐이고 **셋 다 2번째 턴부터 켜진다**), 이 변경이 그 구분을 만든다 — 검색은 목록, 챗봇은 답변. **Phase 7.6이 자동 생성을 두 번 반려했던 사유("11~79초 동안 화면이 빈다")는 이제 성립하지 않는다** — ①(즉시 발췌)이 7~14ms에 화면을 채우고 ②가 뒤이어 채워지는 구조라 빈 화면 대기가 아니고, T10.22의 깜박이는 진행 표시와 T10.19의 스크롤 추적이 이미 그 대기 구간을 위해 만들어져 있다. 붙인 코드는 `_on_chat_search_succeeded()`가 ①을 그린 직후 `_on_chat_summarize_requested()`를 부르는 것뿐(근거 0건이거나 모델 미설치면 부르지 않는다 — 1단계가 어차피 막지만 여기서 걸러야 헛된 "생성 중" 표시가 안 뜬다). **동시 요청 정책은 "직전 생성 취소"로 확정[사용자 선택]** — `SlmService`가 요청을 한 줄로 세우기 때문에(`chat()`이 `_lock`을 추론 내내 쥔다) 버려질 추론을 살려두면 방금 한 질문의 답변이 그만큼 밀린다. 🔴 **결과만 버리는 소프트 취소로는 부족해 `LlamaClient`에 진짜 중단 경로를 새로 넣었다** — `stream=False`라 서버는 생성이 끝나야 응답을 시작하고, 즉 블로킹 구간에는 응답 객체가 아직 없어 `urllib`으로는 끊을 수단이 없다. `http.client`를 직접 써서 연결 객체를 손에 쥐고 다른 스레드에서 닫는다(llama-server는 클라이언트가 끊으면 생성을 멈춘다). 🔴 **`SlmService.abort_active_request()`는 `_lock`을 잡지 않는다** — 그 락은 `chat()`이 중앙 18.3초 동안 쥐고 있어서, 취소 함수가 락을 기다리면 **취소를 부르는 UI 스레드가 그만큼 얼어붙는다**(멈춤을 없애려던 기능이 멈춤을 만든다). 락이 잡힌 상태에서도 즉시 반환하는지 테스트로 못박았다. 취소된 턴은 실패가 아니므로 오류 카드 대신 요약 자리를 숨기고 버튼을 다시 열어 원하면 그 턴만 재생성할 수 있게 뒀다(①의 발췌는 그대로 남는다). 기존 테스트 3건은 전제가 바뀌어 갱신 — 특히 "①은 LLM을 부르지 않는다"를 **"①은 LLM을 기다리지 않는다"**로 바꿔(응답을 붙드는 스텁으로) 2단 구조의 핵심 불변조건을 계속 지키게 했다. **부수로 기존 결함 1건 발견·수정**: 자동 요약으로 스크롤 재조정이 잦아지자 T10.19의 지연 스크롤이 패널 파괴 뒤에 발사돼 삭제된 `QScrollArea`를 건드리는 것이 테스트에서 재현됐다 — `QTimer.singleShot`에 컨텍스트 객체를 넘겨(그 객체가 죽으면 타이머가 자동 취소된다) 해결. 실물 llama-server로 종단 확인: 버튼을 안 눌렀는데 요약 카드가 스스로 뜨고 실제 답변까지 도달, 취소 시 llama-server CPU 증가가 6초당 59초분 → 10초당 0초분으로 **실제로 멈추는 것**을 확인. 테스트 다수 추가, 전체 801 passed / 5 skipped.
- [x] T10.24 **AI 요약 발췌에서 완전 중복 청크 제거** — ✅ **완료 (2026-08-18)**. "응시료가 얼마야?" 답변이 한 단계 값만 내놓는 문제를 파다가 발견 — 프롬프트에 들어가는 발췌 5칸 중 **2칸을 내용이 완전히 같은 1,945자 청크가 차지**하고 있었다(같은 표가 문서 안에서 반복되는 경우). 근거가 5칸뿐이라 중복 하나가 곧 근거 하나의 손실이다. `select_excerpts()`가 내용 기준으로 중복을 걷어내도록 했다. 🔴 **순서는 바꾸지 않았다** — 처음엔 "유사도 순으로 뽑기"를 추천했다가 실제로 계산해보니 **역효과**였다(유사도 상위는 536·450·455·47자짜리 헤딩 조각들이고, 정작 금액이 실린 표는 전부 밀려나 프롬프트에 남는 금액이 20/35만원뿐이 됐다). 호출부의 순위(T10.8 일치 개수 → T10.11 본문 길이)가 내용이 실린 표를 위로 올려주고 있어 그대로 따르는 편이 맞다. 기존 테스트 2건이 우연히 같은 내용의 결과를 만들어 쓰고 있어(임계값·개수 제한을 보려던 테스트) 서로 다른 내용을 쓰도록 고쳤다 — 중복 제거가 도입되자 드러난 픽스처 결함이다. 테스트 4건 추가.
- [x] T10.25 **AI 요약 발췌에서 표를 행·열 구조 그대로 넣기** — ✅ **완료 (2026-08-18, 사용자 지적)**. "응시료는 1·2·3단계가 각각 다른데 2단계 값만 나온다. 단계별로 나오게 할 수 없나"는 지적에서 출발. 조사해보니 **검색은 세 단계를 다 물어오고 있었다** — 프롬프트에 20/35·30/45·40/60이 전부 들어가 있는데도 모델이 하나만 골랐고, 발췌를 늘리자 이번엔 "KPC 응시자는 40만원"처럼 **다른 등급의 열 값을 등급 이름에 붙이는** 오답을 냈다. 🔴 **원인은 평문화**다 — 표를 펼치면서 열 머리글(ACPK 지원/포트폴리오 지원)이 셀에서 멀어져 모델이 등급×지원유형 2축을 뭉갠다. `chunks.table_json`에 `header_row`와 행 구조가 **이미 보존돼 있고**(Phase 1), 등급 이름은 표에 없지만 **바로 앞 청크**에 있다("KAC(Korea Associate Coach) : 코치인증자격 1단계" 다음이 그 등급의 표)는 것을 확인해, `render_table_text()`(신규)가 머리글+행을 `서류종류 | ACPK 지원 | 포트폴리오 지원` 형식으로 렌더링하고 `heading_before()`(신규, T10.21 이웃 조회의 역방향)가 앞 문단 제목을 얹는다. **형식은 SYSTEM_PROMPT의 기존 예시와 맞춰 프롬프트 문구를 한 글자도 바꾸지 않았다** — Phase 6·7 실측치(기권정확도 81.8% 등)가 그대로 유효하다. 절단은 **행 단위**로 한다(`format_excerpts`가 발췌당 1,200자로 자르는데 글자 수로 자르면 "40만" 같은 반쪽 셀이 남는다). 실측: 발췌 10,498→5,693자, 지연 206.5→142.6초, 프롬프트 안의 응시료 행이 세 등급 전부 머리글·등급명과 함께 살아남는 것 확인. 테스트 12건 추가, 전체 820 passed / 5 skipped.
- [x] T10.26 **모드 전환 시 그 모델 벡터가 없으면 자동으로 채우기 (권장 모드가 조용히 죽는 문제)** — ✅ **완료 (2026-08-18, 사용자 보고)**. "권장 모드로 바꿨더니 무엇을 물어도 '관련 문서를 찾을 수 없습니다'"는 보고. 원인은 인덱스에 경량 모델 벡터만 있고 KURE-v1 벡터가 0개인 것 — 모든 결과의 유사도가 `None`이 되고 AI 요약 1단계가 이를 "관련성 판단 불가"로 보고 전부 막는다(T10.9와 겉보기 증상이 같지만 그쪽은 코드 버그였고 이번은 데이터 부재다). 벡터는 `(chunk_id, model)` 복합키로 모델마다 따로 저장되므로(Phase 7.5) **모드 전환만으로는 절대 생기지 않는다.** ① 현재 인덱스에 KURE-v1 벡터를 채웠고(재파싱 없이 임베딩만, 607청크 136초 = 4.5청크/초), ② 재발 방지로 **모드 전환 시 자동 보완**을 넣었다 — 새 스레드를 만들지 않고 기존 재인덱싱 경로를 그대로 쓴다(`index_folder()`가 변경 없는 파일을 전부 건너뛰어도 마지막에 `embed_missing()`은 무조건 부르므로 재파싱 비용 없이 빠진 벡터만 채워지고, 진행률 팝업·취소·종료 처리가 이미 붙어 있다). **시작 시점에는 자동 실행하지 않고 상태바로 알리기만 한다** — 앱을 켜자마자 몇 분짜리 작업이 튀어나오면 곤란하다. `missing_vector_count()`(신규)는 id 목록을 만들지 않고 개수만 센다. 테스트 10건 추가, 전체 826 passed / 5 skipped. 권장 모드 검색 실측으로 상위 5건 유사도 0.596~0.678(임계값 0.5 통과) 확인.
- [x] T10.27 **AI 요약 답변이 중간에 끊기는 문제 (컨텍스트 한계) + 표에서 질문 관련 행만 남기기** — ✅ **완료 (2026-08-18, 사용자 보고)**. "권장 모드에서 답이 `응시료는 자격 단계와 지원 방식에 따라 다릅니다. [ ` 로 끝난다"는 보고. 🔴 **권장 모드 특유의 문제가 아니었다** — 두 모드의 프롬프트 크기는 6,594자로 완전히 동일하고, 실측하니 `prompt_tokens=3,922`로 `n_ctx=4096`에 **답변용 여유가 174토큰뿐**이었다. 답이 조금만 길면 생성이 중간에 끊기고, 하필 `[` 직후에 끊기면 화면엔 반쪽 대괄호가 남는다(`expand_citations`는 완전한 `[N]`만 치환한다). 권장 모드는 발췌 **순서**만 달라 답변이 길어지면서 이 벽에 먼저 부딪힌 것. **같은 측정이 지연도 설명했다** — 150초 중 **140초가 프롬프트 처리**(35.6ms/토큰 × 3,922토큰)이고 생성은 11초뿐이라, 잘림과 지연이 같은 원인이다. 두 가지를 함께 적용[사용자 확정]: **① 표에서 질문 관련 행만 남기기** — 조사 처리는 FTS5·재순위와 **같은 `query_term_variants()`**를 재사용한다(따로 구현하면 판정이 갈려 "검색엔 걸렸는데 발췌엔 없다"가 된다는 T10.8의 교훈). 한 행도 안 걸리면 표를 비우지 않고 원래대로 채운다 — 좁히려다 근거를 없애면 안 된다. **② `n_ctx` 4096 → 6144** — 안전 여유. 줄이는 것과 달리 늘리는 것은 발췌를 자르지 않으므로 Phase 6~7 품질 수치가 그대로 유효하다(주석에 근거를 남겼다). 실측(권장 모드, 같은 질문): 프롬프트 3,922→**2,151토큰**, 답변 여유 174→**3,885토큰**, 프롬프트 처리 140→**71초**, 첫 응답 150→**85초**. 답변도 **세 등급 × 두 열이 전부 정확**하게 나왔다(KAC 20/35, KPC 30/45, KSC 40/60). 테스트 6건 추가, 전체 832 passed / 5 skipped. **남은 과제**: `needs_review` 배지가 정답에도 붙는 보수적 오판, 인용 번호가 엉뚱한 파일을 가리키는 경우(숫자는 맞고 출처 표기만 틀림), 85초도 여전히 긴 지연(발췌 수 5→3이 다음 후보).
- [x] T10.28 **답변 카드 폭을 대화 영역의 80%까지 사용** — ✅ **완료 (2026-08-18, 사용자 요청)**. "답변 내용 카드를 배경 회색영역의 최대 80%까지 사용하는 것으로 바꿔달라"는 요청. `MAX_BUBBLE_WIDTH_RATIO`를 0.70 → 0.80으로 올렸다(목업 기준 65% → 70%[2026-08-13] → 80%[2026-08-18], 세 번째 조정). 🔴 **비율 상수만 올려서는 화면이 하나도 안 바뀐다** — `setMaximumWidth()`는 "이 이상 크지 말라"는 상한일 뿐이라 위젯은 자기 `sizeHint`만큼만 차지하고 남는 공간은 `addStretch()`가 전부 먹는다(실측: 회색 영역 877px에 표 카드가 475px만 씀). 상수를 바꾸고 끝냈다면 사용자에겐 아무 변화가 없었을 것이다. 그래서 답변 말풍선에 **늘어날 권한**을 줬다 — 크기 정책을 `Expanding`으로 바꾸고 `_add_row()`에 `expand` 인자를 추가해 `addWidget(widget, 1)` + `addStretch(0)`로 배치한다. **stretch 인자가 핵심**이다: 둘 다 기본값(1)이면 남는 공간을 반씩 나눠 가져 상한(80%)에 못 미친 채 멈춘다. 위젯이 먼저 상한까지 자라고 남는 20%를 여백이 받도록 여백 쪽을 0으로 준다. **질문 말풍선(`align_right=True`)은 그대로 뒀다** — 짧은 질문이 폭 전체를 차지하면 안 된다. 실측(실제 인덱스 발췌로 `QWidget.grab()` 검증): 대화 영역 868px / 상한 694px / **답변 카드 704px(81%)** / 질문 말풍선 243px(28%). 테스트 2건 추가하고 **`expand`를 잠시 꺼서 실제로 실패하는 것을 확인 후 복원**했다 — 기존 폭 테스트가 비율을 하드코딩(0.70)하고 있어 상수를 참조하도록 함께 고쳤다(다음에 비율을 바꿔도 테스트가 깨지지 않는다). `tests/test_ui_chat.py` 54건 통과. **검증 중 함정**: 화면 좌표 기반 캡처(`CopyFromScreen`)가 앱이 전면에 없을 때 다른 창을 잡았고, 같은 이유로 주입한 클릭·붙여넣기도 엉뚱한 창으로 갔다 — 이후 이 프로젝트가 원래 쓰던 `QWidget.grab()`으로 되돌려 검증했다(CLAUDE.md Phase 4·5의 시각 검증 방식).

- [x] **Phase 11-A 3페이지 셸 재구성** — ✅ **완료 (2026-08-18)**. 설계는 `DESIGN` §14, 계획·결과는 `PLAN` Phase 11. 기존 기능 **이동만** 하고 새 데이터는 만들지 않았다 — `Sidebar` 전면 재작성(다크 네이비 네비 + `검색/대화` 옆 독립 확장 버튼 + 확장 영역에 문서 형식·AI 챗봇 사용), `MainWindow`가 `QStackedWidget`으로 3페이지 전환, `DocumentPage`·`SettingsPage` 뼈대 신설, `폴더 관리`→문서 관리의 `폴더 선택`·`PC 성능`·`모델 관리`→설정으로 이동(위젯 재사용이라 동작 불변), `대/소문자 구분`·`일치되는 단어`는 UI에서만 제거하고 `AppState`에서 읽도록 전환, 본문 배경 `#F3F6FA`·사이드바 `#1E293B` QSS 신설. 테스트 10건 + 리사이즈 회귀 3건 추가 + 기존 테스트 재정비(대소문자·일치단어 UI 테스트 2건은 삭제 대신 `AppState` 기반으로 전환, 사이드바 배치 테스트 2건은 기준 변경 반영), 전체 843 passed / 5 skipped. `QWidget.grab()`으로 검색(접힘/펼침)·문서 관리·설정 4화면 시각 확인. **11-A 중 기존 결함 1건도 잡았다** — "리사이즈할 때마다 팝업이 쏟아진다"는 보고로, `RecentSearches`가 ① 리사이즈마다 목록을 통째로 다시 그리고 ② 🔴 항목 버튼을 **부모 없이** 만들고 있었다(부모 없는 QWidget은 Qt에서 최상위 창이라 순간적으로 별도 창처럼 보인다). 개수가 실제로 달라질 때만 다시 그리도록 하고 생성 시점에 부모를 줬다 — 실측 240회 리사이즈에서 `_render()` 호출 240→0회. Phase 7.7부터 있던 결함이고 11-A에서 높이 계산식이 바뀌며 드러났다.  **11-A 마감 다듬기 [2026-08-18~19]**: 확장 영역을 사이드바와 같은 다크 네이비로 바꾸고(`StyledCheckbox`를 다크용으로 재도색 — 꺼짐은 투명+밝은 테두리, 켜짐은 강조색 채움+흰 체크마크, 글자 12px·박스 16px. `ToggleSwitch` 라벨은 **확장 영역 안쪽으로만 QSS 범위를 한정** — 같은 위젯을 폴더 관리 다이얼로그가 밝은 배경에서 쓴다), 최근 검색을 하단 고정 + 10건 자리 확보로 바꿨다. 🔴 이 과정에서 **`setFixedHeight`가 창 전체의 최소 높이를 706px로 묶는 함정**을 찾았다 — Qt에서 `setFixedHeight`는 최소·최대를 동시에 묶어서, 최근 검색 270px가 사이드바를 거쳐 창의 최소 높이가 됐고, 창이 안 줄어드니 `resizeEvent`도 안 와서 재계산이 영영 돌지 않는 고리에 갇혔다. 간격 축소(16→10) + 압축 가능하게(최소 0 + 최대 예약높이, `sizeHint()`로 선호 크기 제공, 안쪽 `SetNoConstraint`) + 가용 공간을 `설정` 버튼 실제 좌표 기준으로 계산 + `MainWindow.resizeEvent`에서 직접 갱신, 네 가지로 해소. 실측: 접힘·펼침 같은 위치 유지, 창 759~455px 어디서도 창 밖으로 안 나감, 창 최소 높이 706→672px. 부수로 T10.28 때 만든 테스트 경합(3회 중 1회 실패)도 `waitUntil`로 수정. 전체 844 passed / 5 skipped. **후속**: 확장 영역이 다른 페이지에서도 보이는 배치 판단, 11-B(신규/변경 지표·현재 단계·재시도)·11-C(설정 적용 배선) 미착수.
- [x] **Phase 11-B 문서 관리 페이지** — ✅ **완료 (2026-08-19)**. 설계는 `DESIGN` §14.4·§14.10, 상세는 `PLAN` Phase 11. 11-A가 뼈대만 두고 간 자리에 카드 5장(헤더 · 선택 폴더 · 인덱스 작업 · INDEX OVERVIEW · FILE DIAGNOSTICS)과 그 값을 만들 파이프라인 지표를 채웠다. **착수 전 확정 2건**: `재시도`는 **실패한 파일만 강제 재파싱**[사용자 확정], 확장 영역은 **검색/대화에서만 표시**[사용자 확정, 11-A 후속 해소]. 🔴 **착수 조사에서 기존 결함 1건 발견** — `parser/base.py`가 파싱 **시작 시점에** mtime·해시를 채우고 `store_document()`가 `status=FAILED` 문서에도 그대로 저장해, LibreOffice 없이 실패한 `.doc`이 다음 실행에서 `UNCHANGED`로 판정돼 **영원히 건너뛰어진다**(나중에 LibreOffice를 설치해도 안 고쳐진다). Phase 8(증분 스킵)과 T10.2(FAILED 저장)가 각각은 옳은데 만나서 생긴 구멍이고, 테스트로 재현을 고정했다. **`needs_reindex` 쪽은 안 고쳤다** — 상태를 보고 되살리면 Phase 8이 측정한 "무변경 재실행 0.87초" 전제가 바뀐다. 대신 `재시도`가 증분 판정을 무시하고 실패 파일만 다시 파싱한다(사용자가 고른 방향이 이 구멍에 정확히 맞았다). **구현**: `needs_reindex()`(bool) → `classify_file()`(`FileChange` 열거형)로 넓히고 기존 함수는 얇은 래퍼로 남겨 Phase 8 호출부가 한 줄도 안 바뀌게 했다. `IndexReport`에 `scanned`/`created`/`updated` 추가(신규/변경은 **파싱 전** 판정을 쓴다 — 저장 후엔 전부 "이미 있는 문서"가 돼 구분 불가). **`총`을 DESIGN 초안의 `indexed + skipped` 파생 대신 `scanned`로 바꿨다** — 실패한 파일은 둘 중 어디에도 안 잡혀 파생값이면 진단에는 실패가 떠 있는데 총계에서 사라진다. `on_stage(단계, done, total)` 콜백 신설(파싱/임베딩/완료) — 파일 진행률과 **따로** 보낸다(임베딩은 청크 단위라 607청크 136초 동안 파일 진행률이 마지막 값에 멈춰 있어, 섞으면 "19/19에서 2분 멈춘 화면"이 된다). `embed_missing()`이 이미 `on_progress`를 받고 있었는데 파이프라인이 안 쓰고 있었다 — 팝업에도 `단계:` 줄을 붙여 기존 구멍이 공짜로 메워졌다. `reindex_files()` 신설 + `_run_index()` 공통 몸통 추출, `IndexingThread(files=...)`. 🔴 **`reindex_files()`는 `_prune_stale_documents()`를 부르지 않는다** — 대상이 폴더 일부라 그대로 불렀다면 나머지 문서가 전부 "이번 스캔에 없음"에 걸려 인덱스가 통째로 지워진다(T10.5의 정반대 사고). 조건문이 아니라 폴더 정리를 `index_folder()` 쪽에만 남기는 구조로 막고 회귀 테스트로 고정했다. **마지막 실행·실패 목록은 기동 시점에 DB에서 읽는다**(`failed_document_paths()`) — 안 그러면 앱을 켠 직후 화면이 깨끗해 보이고 재시도 버튼도 꺼져 있어, 인덱싱을 한 번 돌리기 전엔 재시도할 방법이 없다. 진행률 팝업은 **문서 관리 페이지가 열려 있으면 안 띄운다**(§14.4.3). **diff 검토에서 11-B가 새로 만든 위험 1건도 잡았다** — `IndexingThread.run()`이 `index_folder()` 예외 시 `on_done`을 아예 안 불렀는데, 지금까지는 "안 닫히는 팝업" 정도였지만 11-B부터는 문서 관리 페이지가 `인덱싱 중`에 갇히고 `인덱스 업데이트`가 계속 비활성이라 앱 재시작 없이는 회복이 안 된다(대상 폴더가 사라지면 `scan_folder()`가 실제로 예외를 던진다). 어떤 경우에도 통지하도록 고치고 사유는 `warnings`에 담았다(실행 자체가 못 선 것이라 `재시도`가 붙잡을 파일이 없다). **실사용 검증에서 결함 1건 추가 발견**: 실제 인덱스에 `~$인증자격시험 세부사항_인증_1_28_v3.doc`(Word 잠금 파일)이 실패로 남아 있었다 — 확장자가 원본과 같아 `is_supported()`를 통과하지만 내용은 스텁이라 **항상 실패**한다. 지금까지 조용히 쌓이기만 하다 11-B가 실패를 보여주기 시작하자 "재시도해도 영원히 실패하는 항목"으로 드러나, `scan_folder()`가 `~$` 접두사를 건너뛰게 했다. 테스트 23건 추가(파이프라인 8 · UI 14 · 스캐너 1), 전체 867 passed / 5 skipped, 팝업 규칙 테스트는 **수정 전으로 되돌려 실제 실패 확인 후 복원**. `QWidget.grab()`으로 6화면 시각 확인(기동/파싱 중/임베딩 중/완료/확장 펼침/확장 숨김) — 진행 바가 흰 카드 위에서 테두리 없는 막대로 보이는 것을 캡처로 발견해 QSS 추가(지금까지 팝업 안에만 있어 안 드러났다). **후속**: 없음 — 11-C로 Phase 11 전체가 닫혔다.
- [x] **Phase 11-C 설정 페이지** — ✅ **완료 (2026-08-19)**. 설계는 `DESIGN` §14.5·§14.5.2·§14.5.3, 상세는 `PLAN` Phase 11. 11-A가 옮겨 놓은 `PC 성능 선택`·`모델 관리` 옆에 sLM 실행 옵션 3개(모델 상주 · 유휴 종료 시간 · AI CPU 사용)와 실행 정보 카드 4줄을 채웠다. 🔴 **착수 조사에서 DESIGN §14.5.2의 전제가 틀렸다는 것을 확인했다** — §14.5.2는 `유휴 종료 시간`과 `AI CPU 사용`을 묶어 "둘 다 생성자 인자라 떠 있는 서버에 반영 안 된다"고 보고 1안/2안 중 고르라 했는데, 실제로는 `유휴 종료 시간`(과 그것을 끄는 `모델 상주`)은 `touch()`가 **요청마다 값을 다시 읽어** 재기동 없이 바로 먹는다. 진짜 기동 인자는 `AI CPU 사용`(`n_threads`, `runtime.start_server()`에만 전달)뿐이라 실제 결정 대상은 하나였고, **1안(즉시 내리고 다음 요청에 새 값으로)**으로 확정[사용자 확정] — 같은 방식이 이미 `set_profile()`에 있고(모델 교체 시 "떠 있으면 내린다"), 메모리 4.8GB를 그 자리에서 돌려주는 것이 Phase 7의 유휴 종료 결정(안드로이드 스튜디오 동시 작업 전제)과도 결이 맞는다. 🔴 **세 setter 모두 `_lock`을 기다리지 않는다** — 그 락은 `chat()`이 추론 내내(중앙 18.3초) 쥐고 있어 설정 화면에서 부르는 함수가 기다리면 **UI가 18초 얼어붙는다**(T10.23에서 취소 함수로 똑같은 함정을 겪고 락을 안 잡는 것으로 해결했던 자리다). 값만 바꾸고 락을 **즉시 얻을 수 있을 때만** 타이머 재장전·서버 종료를 하되, 못 얻어도 새 값이 누락되지 않게 두 경로를 뒀다 — 유휴는 `chat()` 끝의 `touch()`가, CPU는 `ensure_ready()`가 기동 당시 값(`_active_n_threads`)과 비교해 재기동한다. 락을 실제로 점유한 채 setter를 부르는 테스트로 즉시 반환·반영 누락 없음 둘 다 고정했다. **구현**: `AppState`에 3필드 추가(기본값은 Phase 7이 실물 검증한 유휴 5분·상주 OFF·CPU 자동), `config.settings.resolve_n_threads()` 신설 — 🔴 **숫자가 아니라 모드(`auto`/`half`/`max`)를 저장한다**(폴더째 다른 PC로 옮기는 것이 전제인데 저장된 "8스레드"가 4코어 PC에선 전혀 다른 의미가 된다, TECH 9.1). `auto`는 계산 없이 `None`을 넘겨 llama.cpp 판정에 맡긴다(Phase 6~7 실측치가 전부 인자 없이 나온 값이라 기본값이 그 조건과 같아야 한다). **상주 중에는 유휴 콤보를 닫는다**(효과 없는 콤보를 열어두면 "골랐는데 안 먹는다"가 된다), 실행 정보는 경로만이 아니라 없으면 `(설치되지 않음)`을 붙인다(경로만 보여주면 실제로 있는지 알 수 없다), 기동 시 저장값을 **화면과 서비스 양쪽에** 건다(화면만 맞추면 "설정은 상주인데 5분이면 내려가는" 어긋난 상태 — 11-A에서 PC 성능 콤보로 똑같은 것을 겪었다). **테스트 함정**: 복원이 신호를 쏘면 저장·적용이 도로 돌고 CPU 모드는 모델까지 내리는데, 처음 쓴 회귀 테스트는 복원값이 현재값과 같아 Qt가 애초에 신호를 안 쏴서 `blockSignals`를 빼도 통과했다 — 세 값이 **전부 달라지는** 복원으로 바꾸니 세 신호가 모두 잡혔다(수정 전 실패 확인 후 복원). 테스트 23건 추가(sLM 서비스 6 · 설정 4 · UI 13). `QWidget.grab()`으로 3화면 시각 확인 — 상주 ON 시 서비스 유휴값 300→0, CPU 절반에서 `n_threads=7`(이 PC 14스레드)이 실제로 걸리고 `app_state.json`에 저장되는 것까지 실측.

---

- [x] **Phase 11-D 이미지 자산 중앙화** — ✅ **완료 (2026-08-19, 사용자 제안)**. 상세는 `PLAN` Phase 11(11-D). "문서 폴더마다 `.assets`가 생겨 지우기 어렵다"는 지적으로 시작 — 실측(`../exdoc`)으로 확인하니 `.assets` 루트 11개·하위 폴더 90개·파일 1,173개·104.1MB가 문서 트리 곳곳에 흩어져 있었고, 더 심각한 건 **사용자의 원본 문서 폴더에 쓰기를 한다**는 점(OneDrive·백업 도구 동기화 부작용)이었다. 세 결정을 확정하고 진행[사용자 승인] — 중앙 위치 `data/assets/`(이미 `.gitignore` 전체 대상), 폴더 이름은 `doc_id`, 옛 잔재 정리는 상시 UI 버튼이 아니라 **일회성 스크립트**(`scripts/clean_assets.py`, 방안 적용 후엔 다시는 안 생기므로 반복 문제가 아니다 — `archive_plan.py`류와 같은 관례). 🔴 **착수 조사에서 CLAUDE.md 서술 오류를 발견했다** — T6.8이 "doc_id가 파일 내용 해시 기반"이라 적었는데 실제로는 **경로** 해시(`uuid5(ns, 정규화된 절대경로)`)다. "인덱스는 이식할 수 없다" 결론 자체는 유효하나(경로가 PC마다 다른 것도 같은 결과), 중앙 폴더 키로 doc_id를 쓰는 이번 작업이 그 사실에 직접 의존해 재확인 중 잡혔다 — CLAUDE.md 정정. **구현은 파서를 한 줄도 안 건드렸다** — `parse_file(path, asset_dir=...)`는 Phase 1부터 있던 배선이라 `indexer/pipeline.py`의 호출부 한 줄(`asset_dir=ASSETS_DIR / make_doc_id(path)`)만 바꿨다. `_prune_stale_documents()`(T10.5)가 문서를 지울 때 그 `doc_id` 폴더도 함께 지우도록 얹었다 — 안 그러면 중앙화한 곳이 지워진 문서의 잔재로 계속 불어난다. `scripts/clean_assets.py`는 dry-run이 기본이고, 🔴 **DB가 아직 옛 경로를 참조하면(재인덱싱 전) 삭제를 거부한다** — 순서를 지키지 않고 지우면 이미지 카드가 깨진다(`--force`로 무시 가능). 테스트 16건 추가(파이프라인 3건은 수정 전 실패 확인 후 복원 + `clean_assets` 13건). 실사용 검증: `../exdoc` 이미지 포함 문서 8건 사본 인덱싱 — 작업 폴더에 `.assets` 0개, 이미지 청크 288개 전부 중앙 위치를 가리키고 실제 파일 전부 존재 확인. **범위 밖으로 남긴 것**: 부분 재파싱 시(이미지 개수가 줄어드는 경우) 안 쓰이는 파일이 doc_id 폴더 안에 남을 수 있음 — 중앙화 이전부터 있던 문제(`.assets/<stem>`에서도 동일)라 손대지 않았다. 🔴 **전체 스위트 실행에서 별도 오염 버그를 발견·수정했다** — 개별 파일은 다 통과했지만 `index_folder()`를 부르는 기존 테스트 전부가 이 PC의 실제 `data/assets/`에 doc_id 폴더를 만들고 있었다(실측 한 번에 31개, 159KB). `indexer/pipeline.py`가 `ASSETS_DIR`을 임포트 시점에 지역 이름으로 복사해 와서 `config.settings.ASSETS_DIR`만 패치해서는 안 먹는다 — Phase 7.7의 `app_state.json` 오염, T10.5의 `index.sqlite3` 오염과 같은 함정이 이미지 자산에서 재발한 것. `tests/conftest.py`의 전역 `autouse` fixture(`_isolate_assets_dir`)로 막고, 이미 생성돼 있던 31개 폴더(실제로 참조된 적 없음)를 지웠다. 부수로 기존 테스트 1건도 고쳤다 — `parse_file`을 모킹하던 스텁이 `(path)`만 받았는데 파이프라인이 이제 `asset_dir=` 키워드 인자를 추가로 넘겨 `TypeError`가 `index_folder()`의 넓은 `except Exception`에 흡수돼 조용히 실패로 잡혔다. **후속**: 이 세션에서는 코드·스크립트만 만들었고, 실제 프로젝트 `data/index.sqlite3`와 `../exdoc`의 104MB 잔재 마이그레이션(강제 재인덱싱 → dry-run 확인 → 실삭제)은 되돌리기 어려운 작업이라 사용자 확인 후 별도 진행하기로 했다.
- [x] **Phase 11-E 팝업 3종 재도색** — ✅ **완료 (2026-08-19, 사용자 요청)**. `폴더 관리`·`모델 관리`·`인덱싱 진행률` 팝업 스크린샷을 보고 "스타일을 바꿔달라"는 요청. 셋 다 Phase 4·7 시절 "최소 구현"으로 만들어진 뒤 Phase 11의 카드형 디자인 시스템(`PageEyebrow`/`PrimaryButton`/`SidebarFooterButton`)이 자리 잡는 동안 재도색을 안 받아, 기본 Qt 버튼·여백 없는 레이아웃으로 다른 화면 옆에서 튀고 있었다. **새 색·새 클래스를 만들지 않고 기존 토큰만 재사용했다** — 다이얼로그마다 다른 색이 되면 예외가 하나씩 늘어난다. 공통 처리: 다이얼로그 자체에 `objectName` + 흰 배경, 루트 레이아웃에 여백(20px)·간격을 주고, 주 액션은 `PrimaryButton`(파랑), 보조 액션은 `SidebarFooterButton`(회색)으로 통일, 얇은 구분선(`#E5E7EB`) 적용. **폴더 관리**: 경로 라벨 위에 `PageEyebrow`("대상 폴더")를 얹고 본문에서는 중복 표기("대상 폴더: ")를 뺐다(문서 관리 카드와 같은 원칙). **모델 관리**: 행마다 카드 배경(`#ModelRow`, 문서 관리의 StatCell과 같은 톤)을 둘러 표처럼 붙어 보이던 목록을 분리하고, 상태 배지를 문서 관리의 `PageBadge`와 같은 알약(pill) 모양으로 바꿨다. 🔴 **`_ModelRow`/`_SlmRow`는 순정 `QWidget`이라 `WA_StyledBackground` 속성 없이는 QSS의 `background`가 조용히 무시된다** — `QFrame`과 다른 점이라 놓치기 쉬운 함정, 속성을 명시로 추가해 해결. 🔴 **재도색 검증 중 실제 버그를 잡았다** — 버튼에 QSS `background`를 직접 지정하면 Qt의 기본 "비활성화" 회색 처리가 더는 자동으로 안 먹어서, 비활성 버튼(이미 설치된 모델의 "설치 안내")과 활성 버튼("폴더 열기")이 스크린샷에서 구분 안 되게 나왔다 — `#SidebarFooterButton:disabled` 규칙을 추가해 해결했는데, 이 토큰은 문서 관리 페이지의 "취소"/"재시도" 버튼과 공유라 그쪽 비활성 표시도 같이 고쳐졌다. **인덱싱 진행률**: 상태 텍스트를 굵게, `IndexingProgressStage`(Phase 11-B에서 objectName만 붙이고 QSS를 빠뜨렸던 라벨)에 스타일을 채우고, 취소 버튼을 `SidebarFooterButton`으로 통일. `QWidget.grab()`으로 3개 팝업 전부 시각 확인, 전체 906 passed / 5 skipped 유지(기존 테스트는 objectName 문자열이 아니라 속성(`.folder_label`/`.reindex_button`/`.cancel_button` 등)을 참조해 재도색에 영향받지 않음).
- [x] T10.29 **상태바 "벡터 없는 문서" 문구가 실제로는 청크 수였다** — ✅ **완료 (2026-08-19, 사용자 질문)**. "권장 모드 벡터가 없는 문서 1891개 있습니다"라는 상태바 경고를 보고 "1891개가 무슨 문서냐"는 질문. 실제 인덱스(문서 72건)로 대조하니 `missing_vector_count()`는 청크 단위로 세는 함수(docstring에도 "벡터가 없는 청크 수"라고 명시)인데 경고 문구만 "문서가 {missing}개"로 잘못 표기하고 있었다. 실측으로 확인: 전체 청크 2,498 − 경량 모델 벡터 2,498(전부 있음) / 권장 모델 벡터 607 = 정확히 1,891. 문서는 72개뿐이라 애초에 "1891개 문서"는 성립할 수 없는 숫자였다. "문서" → "청크"로 문구만 고쳤다 — `missing_vector_count()` 자체의 계산은 옳았고, 잘못된 것은 그 숫자를 문서 단위로 읽은 라벨뿐이었다. 테스트 1건 갱신(`test_startup_warns_instead_of_starting_a_long_job`).

---

## 부록: Phase 간 의존관계 요약

```
Phase 1 (파서)
   └─▶ Phase 2 (FTS5 인덱싱)
          └─▶ Phase 3 (벡터 재순위)
                 └─▶ Phase 4 (추출형 UI) ◀── MVP 완료
                        ├─▶ Phase 5 (표/이미지 카드)  ◀── Phase 1 결과물도 필요
                        ├─▶ Phase 6 (sLM 검증)
                        │      └─▶ Phase 7 (sLM 옵션 모드)
                        └─▶ Phase 8 (증분 인덱싱)  ◀── Phase 2, 5 결과물도 필요
                               └─▶ Phase 9 (패키징/배포)  ◀── 전체 Phase 선행
                                      └─▶ Phase 10 (배포 후 개선 백로그)  ◀── todo list, 우선순위 재검토 후 착수
```
