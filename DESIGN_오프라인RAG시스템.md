# DESIGN: RAG와 sLM을 활용한 오프라인 문서 검색 시스템 — UI 디자인 명세서

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 기준 목업 | 사이드바 필터형 레이아웃 **v3 (확정)** |
| 기준 문서 | PRD 5.2~5.3 / TECH 6장 / TASK Phase 4~5 / PROMPTS 97~106행 |
| 문서 목적 | 확정 목업(v3)을 구현 가능한 컴포넌트 명세로 분해. Phase 4(텍스트 카드)와 Phase 5(표·이미지 카드) 구현의 기준 문서 |

> **표기 규칙**: 각 항목은 출처를 다음과 같이 구분한다.
> **[확정]** 목업 v3에 시각적으로 확정된 사항 · **[문서]** PRD/TECH/TASK에 명시된 사항 · **[제안]** 목업·문서 어디에도 없어 본 명세서가 제안하는 값 (구현 전 확인 필요)

---

## 0. UI 프레임워크 — **PySide6 확정 (T4.0)**

| 후보 | 다운로드 | 실제 배포 가능성 |
|---|---|---|
| **PySide6** | 237MB (addons 458MB 제외 시 essentials 212MB, **위젯만 번들하면 50~70MB**) | **완전 자족** — 외부 런타임 의존 0 |
| pywebview | 2.6MB | Edge **WebView2 런타임**이 대상 PC에 있어야 실행됨 |
| Flet | 3MB | Flutter 런타임을 별도로 확보해야 함 |

**결정 근거는 용량이 아니라 배포 방식이다.** TECH 9.4가 "압축 해제 후 즉시 실행"하는 포터블 배포를 약속하는데, pywebview는 대상 PC의 WebView2에 의존한다. Windows 11에는 기본 포함이지만 구형 Windows 10이나 관리된 사내 PC에서는 보장되지 않고, 오프라인 설치본(약 150MB)을 함께 넣는 순간 "압축 해제만으로 실행"이 깨진다. Flet은 런타임을 별도로 받아야 해 오프라인 전제(PRD 6장)와 더 멀다.

용량 부담은 Phase 3에서 이미 상쇄됐다 — ChromaDB(75MB)와 torch(117MB)를 덜어낸 절감분이 PySide6의 50~70MB를 덮는다.

8GB 최소 사양에도 유리하다. WebView2는 Chromium 엔진이라 브라우저·렌더러·GPU 프로세스가 별도로 뜨지만, Qt 위젯 앱은 단일 프로세스로 동작한다.

**구현상 유의점**
- `PySide6-Essentials`만 설치하고, PyInstaller에서 Quick/QML/Designer/3D/OpenGL 소프트웨어 렌더러를 제외해 번들을 줄인다
- 카드 UI(라운드 코너·테두리·음영)는 **QSS**로 구성한다 — CSS와 문법이 유사해 §10 디자인 토큰을 거의 그대로 옮길 수 있다
- 검색어 하이라이트는 `QLabel`이 지원하는 리치 텍스트 부분집합(`<span style="background-color:…">`)으로 처리한다
- 나눔고딕은 `QFontDatabase.addApplicationFont("font/NanumGothic.ttf")`로 런타임 로딩한다 (§10.4)

---

## 1. 설계 원칙

UI의 모든 결정은 PRD 3장의 3원칙을 따른다.

| 원칙 | UI에서의 구현 |
|---|---|
| 검색 속도 우선 | 결과는 인덱스 조회만으로 표시. 검색 중에도 입력창은 항상 응답 |
| 완벽한 한국어 지원 | 한글 파일명·경로·본문이 잘리거나 깨지지 않아야 함 (§9 참고) |
| 할루시네이션 원천 차단 | **추출형 결과가 기본값.** AI 요약은 명시적으로 켜야 하는 옵션이며 기본 OFF |

추가로 UI 고유의 원칙 하나를 둔다 — **결과의 신뢰도를 시각적으로 구분한다.** 유사도가 낮은 결과를 숨기지 않고 흐리게 노출해, 사용자가 "관련 없음"을 직접 판단할 수 있게 한다.

---

## 2. 전체 레이아웃

> ⚠️ **Phase 11에서 §14가 이 절을 대체한다** — 사이드바가 3페이지 네비게이션으로 바뀌고
> 본문이 `QStackedWidget`으로 전환된다. 아래 내용은 Phase 7.7 시점 기록으로 남긴다.

### 2.1 구조 **[확정, Phase 7.7 전면 재구성 — 목업 `rag_ui_concept_*.html` 기준]**

> Phase 4~7.6까지는 상단 고정 검색바 + 챗봇 모드 전용 하단 입력창, 두 입력 지점이 공존했다. 사용자가 제시한 목업 2장(검색 결과 모드 / 챗봇 모드)이 두 화면 모두 **하단 입력창 하나**로 통일돼 있어, 그 구조로 전면 재구성했다[사용자 확정, 2026-08-13]. 검색 결과 모드는 상단에 현재 검색어 헤더(+ ✕)가 붙고, 챗봇 모드는 헤더 없이 대화만 흐른다(§5.8).

```
┌───────────────┬──────────────────────────────────────────┐
│               │  현재 검색어                          ✕   │ ← 검색 결과 모드만
│  사이드바      │──────────────────────────────────────────│
│  (고정폭,      │   결과 카드 리스트 / 챗봇 대화              │
│   전체 높이)   │   (가변폭, 세로 스크롤)                     │
│               │                                          │
│  1. 문서 형식  │   ┌────────────────────────────────────┐ │
│  2. 검색 옵션  │   │ 텍스트 카드 / 말풍선                  │ │
│  3. PC 성능   │   ├────────────────────────────────────┤ │
│  4. 최근 검색  │   │ 표 카드                             │ │
│  5. 모델·폴더  │   └────────────────────────────────────┘ │
│     관리 버튼  │──────────────────────────────────────────│
│               │  질문을 입력하세요                 [검색] │ ← 공용 입력창
│               │──────────────────────────────────────────│
│               │  문서 N개 인덱싱됨 · 마지막 갱신 …          │
└───────────────┴──────────────────────────────────────────┘
```

### 2.2 영역별 치수

| 영역 | 폭 | 높이 | 비고 |
|---|---|---|---|
| 사이드바 | 고정 220px **[제안]** | 전체(창 높이 전부) | 목업 비율 기준 추정치 — 상태바와 나란한 게 아니라 창 왼쪽 전체를 차지한다 |
| 검색 결과 헤더 | 전체 | 고정 | 검색 결과 모드에서만 표시, 챗봇 모드에서는 숨김 |
| 결과/대화 영역 | 가변(잔여 전체) | 가변 | 세로 스크롤은 이 영역에만 적용 |
| 입력창 | 전체 | 고정 | 하단, 검색 결과·챗봇 두 모드가 공유 |
| 상태바 | 전체 | 고정 | 최하단 고정 |

> **[확정]** 사이드바는 창 왼쪽에서 세로 전체를 차지하는 하나의 패널(흰 배경 + 테두리)이다. 오른쪽 영역(헤더·결과·입력창·상태바)은 사이드바와 나란히, 세로로는 그 안에서 다시 쌓인다.

### 2.3 스크롤 동작 **[제안]**

사이드바·헤더·입력창·상태바는 고정하고 **결과/대화 영역만 스크롤**한다. 결과가 길어져도 필터를 다시 조작하러 위로 올라갈 필요가 없어야 한다.

---

## 3. 입력창 **[Phase 7.7 전면 개정]**

> Phase 4~7.6까지는 상단 고정 "검색바" 하나였다. Phase 7.7에서 하단 공용 입력창(`InputBar`)으로 위치를 옮기고, 검색 결과 모드·챗봇 모드가 함께 쓰도록 통합했다(§2.1) — 옛 "검색바" 절을 이 절이 대체한다.

### 3.1 명세 **[확정]**

| 요소 | 값 |
|---|---|
| 위치 | 하단 고정 (§2.1), 검색 결과·챗봇 두 모드 공유 |
| placeholder | `질문을 입력하세요` — 목업(`rag_ui_concept_*.html`) 두 화면이 동일하게 쓴다. (옛 `계약서 검토 기준이 뭐였지` 예시는 검색 전용이던 시절 문구라 폐기) |
| 형태 | 라운드 코너 입력창 + 우측 "검색" 버튼, 연한 테두리, 흰 배경 |
| 폭 | 입력창은 가변(stretch), 검색 버튼은 고정폭 |

### 3.2 동작 **[문서: T4.3, Phase 7.7에서 debounce 폐기]**

| 트리거 | 동작 |
|---|---|
| Enter | 즉시 제출 |
| 검색 버튼 클릭 | 즉시 제출 |

- **300ms debounce 자동검색은 폐기했다** **[사용자 확정, 2026-08-13]**. 검색·챗봇 두 모드가 입력창을 공유하게 되면서, 타이핑만으로 검색이 나가는 동작이 챗봇 모드에는 맞지 않는다(조합 중 문자로 메시지가 전송되면 안 된다) — Enter·검색 버튼으로만 제출하는 쪽으로 통일했다.
- 제출과 동시에 입력창을 비운다 **[확정]** — 챗봇 모드에서 보낸 메시지가 입력창에 남아 있으면 어색하고, 검색 모드에서는 현재 검색어를 §3.3의 결과 헤더가 대신 보여준다.
- 빈 문자열도 그대로 제출된다 **[확정]** — 검색 모드에서는 "빈 질의 → 초기 상태(§7.1)로 복귀"가 기존 동작이고, 챗봇 모드는 스스로 빈 문자열을 걸러낸다. 입력창 자체는 현재 모드를 모르므로 판단하지 않는다.
- 입력창은 어느 모드로 제출할지 모른다 — `MainWindow`가 챗봇 모드 활성 여부를 보고 검색 실행/챗봇 메시지 전송으로 분기한다.

### 3.3 검색 결과 헤더 **[신설, Phase 7.7]**

검색 결과 모드에서 결과/대화 영역(§2.1) 맨 위에 붙는다. 챗봇 모드에서는 숨긴다(§5.8).

```
현재 검색어                                          ✕
```

| 요소 | 명세 |
|---|---|
| 검색어 | 좌측, 현재 검색 중인 질의 전문 |
| ✕ | 우측, 클릭 시 검색어·결과를 모두 지우고 초기 상태(§7.1)로 되돌린다 **[확정]** |

---

## 4. 사이드바

> ⚠️ **Phase 11에서 §14.2가 이 절을 대체한다** — 형식 필터·AI 챗봇 토글은 확장 영역으로,
> PC 성능·모델 관리는 설정 페이지로 옮기고, 대소문자·일치단어 토글은 UI에서 제거한다.
> 아래 내용은 Phase 7.7 시점 기록으로 남긴다.

목업 v3의 사이드바는 **세 블록이 위에서 아래로** 배치된다. 각 블록은 회색 소형 레이블을 머리에 둔다. Phase 7.7에서 목업(`rag_ui_concept_*.html`)에 맞춰 하단에 **최근 검색**(§4.4)과 **모델·폴더 관리 버튼**(§4.5)을 추가했다 — 사이드바가 창 왼쪽 세로 전체를 차지하게 된 것(§2.1)과 맞물린 변화다.

### 4.1 문서 형식 필터 **[확정]**

```
문서 형식
 ☑ 전체
 ☐ docx
 ☐ xlsx
 ☐ pdf
```

| 항목 | 명세 |
|---|---|
| 컨트롤 | 체크박스 그룹 (복수 선택) |
| 기본값 | **전체** 선택 **[확정]** |
| 목록 | 목업은 예시. 실제로는 **FTS5 인덱스 메타데이터 기반 동적 생성** **[문서: T4.4]** — 인덱싱된 형식만 노출 |
| 체크 표시 | 파란색 채움 + 흰 체크 **[확정]** |

**"전체"의 상호작용 규칙** **[제안]** (목업에 동작이 정의되지 않음)

- `전체` 체크 → 나머지 전부 해제, 형식 조건 없이 검색
- 개별 형식 체크 → `전체` 자동 해제
- 개별 형식을 모두 해제 → `전체`로 자동 복귀 (결과가 0건이 되는 상태를 방지)

선택 상태는 검색 쿼리 조건으로 전달된다 **[문서: T4.5]**.

### 4.2 검색 옵션 — 토글 3종 **[확정]**

```
검색 옵션
 AI 챗봇 사용      ( ●)  OFF
 대/소문자 구분     ( ●)  OFF
 일치되는 단어      ( ●)  OFF
```

**세 토글은 동일한 토글 컴포넌트를 재사용한다** **[문서: T4.6]**. 모두 기본 OFF **[확정]**.

| 토글 | 연동 대상 | Phase |
|---|---|---|
| **AI 챗봇 사용** | ~~Phase 7까지 상태만 저장~~ → ~~Phase 7: 켜면 결과 목록 맨 위에 AI 요약 카드~~ → **Phase 7.6: 라벨을 "AI 요약 보기"에서 변경, 켜면 검색 결과 영역 전체가 챗봇 패널(§5.8)로 바뀐다**. **모델 미설치 시에는 비활성 유지** | T4.7 → T7.5 → T7.6.1 |
| **대/소문자 구분** | FTS5 쿼리 옵션 | T4.8 |
| **일치되는 단어** | 부분 일치 → 완전 일치 조건 전환 | T4.9 |

> **AI 챗봇 사용 토글의 Phase 4 처리** **[제안]**: 상태만 저장한다는 것이 "켜도 아무 일도 안 일어난다"는 뜻이면 사용자는 고장으로 인식한다. Phase 4~6 구간에서는 토글을 **비활성(disabled) + "Phase 7 예정" 툴팁**으로 두거나, 켰을 때 "AI 요약은 준비 중입니다" 안내를 노출할 것을 권한다. 어느 쪽을 택할지 확인 필요.

> **두 검색 옵션의 기술적 주의** **[제안]**: FTS5 기본 토크나이저(`unicode61`)는 대소문자를 접어(case-fold) 색인하므로, "대/소문자 구분"은 쿼리 옵션만으로는 구현되지 않는다. 색인 시점의 토크나이저 설정 또는 후처리 필터가 필요하다 — **Phase 2 스키마 설계 시 함께 결정해야 한다.**

### 4.3 PC 성능 선택 **[확정]**

| 항목 | 명세 |
|---|---|
| 컨트롤 | 콤보박스(드롭다운). 각 옵션에 설치 상태 배지(설치됨/준비 중) 표시 **[확정, 아래 참고]** |
| 선택지 | `경량 모드 (최소 사양)` / `권장 모드 (권장 사양)`(이전 이름 "고성능 모드", 2026-08-11 변경) |
| 기본값 | **경량 모드** **[확정]** — 최소 사양을 기본으로 두는 것이 안전 |
| 연동 | **Option A — 모델 관리가 단일 진입점** **[확정, T4.11]**: 콤보박스는 선택 트리거만 담당하고, 실제 프로파일 전환은 모델 관리 팝업에서만 일어난다 |
| 관리 진입점 | **사이드바 하단 "모델 관리" 버튼**(§4.5) — 미설치 옵션을 선택해야만 열리던 기존 경로 외에, 이미 다 설치된 상태에서도 설치 현황을 바로 확인·관리할 수 있는 진입점. ~~콤보 우측 하단 링크 버튼~~[2026-08-11 신설 → 2026-08-13 폐기] — 목업이 관리 버튼들을 사이드바 하단에 모아 둬 그 자리로 옮겼다(중복 진입점 정리) |

> **목업 결함** — 콤보박스 폭이 좁아 `경량 모드 (최소 사양` 에서 텍스트가 잘린다. 구현 시 사이드바 폭에 맞춰 레이블을 줄이거나(`경량 모드`) 콤보박스를 전체 폭으로 늘려야 한다.

**모델 관리 연동 (Phase 4 착수 전 확정, 시안: [artifact](https://claude.ai/code/artifact/af6d783e-7ca1-499d-be2f-be4c9a52ba4e))**

미설치 옵션(권장 모드)을 선택하면 즉시 전환되지 않고 모델 관리 팝업이 자동으로 열려 해당 모델 행에 포커스된다. "설정은 권장 모드인데 실제로는 경량으로 검색되는" 어긋난 상태 자체를 차단하기 위함이다. 다운로드 완료 후 새로고침을 누르면 콤보박스 값이 실제로 전환된다.

모델 관리 팝업에는 원래 Phase 7(T7.6) 범위였던 임베딩 모델 섹션을 이번 Phase로 앞당겨 넣는다 — `ko-sroberta-multitask`(경량, 항상 설치됨) / `KURE-v1`(권장). **`KURE-v1`은 이번 Phase에서 "준비 중" 배지 + 비활성 버튼으로만 노출한다** — 허깅페이스 레포에 ONNX가 없어(`safetensors`만 제공) 실제 다운로드가 동작하려면 별도 변환 파이프라인이 필요하고, 그 구현은 이번 범위가 아니다.

권장 모드 전환 시 재인덱싱은 `indexer.vector.store.embed_missing()`을 그대로 재사용한다(신규 로직 불필요) — 자세한 근거는 PLAN 문서 §4-C 참고.

### 4.4 최근 검색 **[신설, Phase 7.7, 2026-08-13 재작업]**

```
PC 성능 선택
[콤보박스]

최근 검색
 서류검증 2      ← 오래된 항목이 위
 서류검증 1      ← 최신 항목이 아래
```

| 항목 | 명세 |
|---|---|
| 위치 | **PC 성능 선택 바로 아래**에서 시작한다[사용자 확정, 2026-08-13] — 관리 버튼(§4.5)과의 사이 여백은 최근 검색 다음으로 옮겼다 |
| 저장 | `app_state.json`의 `recent_searches` — **영구 저장, 최대 10건**, 최신이 맨 앞(리스트 순서) |
| 화면 표시 순서 | **저장 순서를 뒤집어서** 오래된 항목이 위, 최신이 아래로 보인다 — 아래 방향으로 자라는 느낌을 준다[사용자 확정, 2026-08-13] |
| 화면 표시 개수 | **동적** — 사이드바 실제 높이에서 다른 고정 블록(문서 형식·검색 옵션·PC 성능·관리 버튼)이 쓰는 높이를 뺀 나머지에 맞춰 몇 건이 들어갈지 스스로 계산한다(`Sidebar.resizeEvent` → `RecentSearches.set_max_height()`). 창 크기가 바뀌면 다시 계산한다 |
| 공간 부족 시 | **위쪽(오래된 항목)부터 빠진다**[사용자 확정, 2026-08-13] — 저장된 10건 중 화면에 다 못 담으면 최신 항목부터 아래에서부터 채우고, 남는 공간이 없으면 그 위(더 오래된 것)는 안 보인다. 대화창이 아래로 스크롤되며 오래된 메시지가 위로 밀리는 것과 같은 방향 감각이다 |
| 항목 클릭 | 그 검색어를 입력창(§3)에 넣고 즉시 제출 — 현재 모드(검색/챗봇) 그대로 실행 |
| 긴 검색어 | 말줄임(가운데 생략 아님, 끝에서 자름) + 툴팁으로 전체 문구 표시 |
| 기록 시점 | 입력창에서 제출된 모든 질의(검색 모드·챗봇 모드 공통) — 두 모드가 입력창을 공유하므로(§2.1) 구분하지 않는다 |

> 초기 구현(2026-08-13 오전)은 화면에 최대 5건만 보여주는 고정 상한이었다 — 사이드바 폭·고정 높이에 10건을 다 늘어놓으면 위쪽 블록이 찌그러지는 걸 막기 위해서였다. 위치를 PC 성능 선택 바로 아래로 옮기면서, 고정 상한 대신 **실측 기반 동적 개수 + 상단부터 밀어내는 방식**으로 다시 구현했다[사용자 확정, 2026-08-13 오후].

### 4.5 모델·폴더 관리 버튼 **[신설, Phase 7.7, 목업 확정]**

```
[모델 관리]  [폴더 관리]
```

사이드바 맨 하단, 최근 검색(§4.4) 아래에 나란히 놓인다.

| 버튼 | 이전 위치 | 동작 |
|---|---|---|
| 모델 관리 | PC 성능 콤보 우측 하단 링크(§4.3) | 클릭 시 모델 관리 팝업을 현재 활성 프로파일에 포커스해서 연다 |
| 폴더 관리 | 하단 상태바 우측 끝(§6) | 클릭 시 폴더 관리 화면 진입 **[문서: T4.17]** — 동작은 그대로, 위치만 이동 |

목업이 두 관리 기능을 사이드바에 나란히 두고 있어, 이전까지 콤보박스·상태바에 각각 흩어져 있던 진입점을 이 자리로 통합했다. 상태바(§6)는 이제 관리 버튼을 갖지 않는다.

---

## 5. 결과 카드

### 5.1 공통 프레임 **[확정 + 문서: TECH 6.2]**

모든 카드가 공유하는 기반 컴포넌트 **[문서: T4.12]**. 표·이미지 카드도 이것을 재사용한다.

```
┌──────────────────────────────────────────────────┐
│ 파일명.확장자 · 위치                    [원문 열기 ↗] │
│                                                  │
│ (카드 유형별 본문 영역)                             │
└──────────────────────────────────────────────────┘
```

| 요소 | 표시 | Phase 1 파서 출력 연결 |
|---|---|---|
| 파일명 | 굵게, 진한 색 | `chunk.file_name` |
| 구분자 | ` · ` (가운뎃점) **[확정]** | — |
| 위치 | 회색, 파일명 우측 | §5.2 참고 |
| 원문 열기 | 우측 상단, 외부링크 아이콘 동반 | `chunk.file_path` |

카드는 **흰 배경 + 연한 테두리 + 라운드 코너**, 세로로 일정 간격을 두고 쌓인다 **[확정]**.

**결과 목록은 상위 5개까지만 카드로 그리고, 더 있으면 목록 끝에 "더보기 (N개 더)" 버튼을 둔다** **[제안, 2026-08-14]** — DESIGN에 결과 개수 상한 명세가 없어 사용자 요청으로 직접 정의했다. 클릭하면 나머지를 한 번에 펼치고 버튼은 사라진다(재클릭 없음). 챗봇 즉시 발췌(§5.8)도 같은 상한(5개)과 같은 버튼(같은 objectName)을 쓴다 — 처음엔 챗봇 쪽만 텍스트 안내였다가, 사용자 요청으로 검색과 완전히 동일하게 통일했다.

### 5.2 위치(location) 표기 규칙 **[확정 + 스키마 연결]**

목업은 형식마다 다른 표기를 쓴다. Phase 1 스키마와의 대응은 다음과 같다.

| 형식 | 목업 표기 | 데이터 출처 |
|---|---|---|
| docx | `3페이지` | `chunk.page_or_slide` |
| pdf | `12페이지` | `chunk.page_or_slide` |
| pptx | `5번 슬라이드` | `chunk.page_or_slide` |
| xlsx | `Sheet2` | **`chunk.table.caption`** (시트명). `page_or_slide`는 시트 인덱스이므로 이름 표시에는 쓸 수 없다 |

> **구현 주의**: xlsx만 숫자가 아닌 **시트명**을 노출한다. `XlsxParser`가 `TableData.caption`에 `sheet.title`을 저장하므로 이 필드를 사용해야 한다. `page_or_slide`만 보고 "2페이지"로 렌더링하면 목업과 어긋난다.

### 5.3 텍스트 카드 **[확정]** — Phase 4

```
2024_영업계약서_표준.docx · 3페이지            원문 열기 ↗
계약서 검토 시 [기준]이 되는 조항은 손해배상, 계약 해지,
지급 조건 세 가지이며…
```

| 항목 | 명세 |
|---|---|
| 본문 | `chunk.content` 발췌문 |
| 하이라이트 | 검색어에 **연노랑 배경** 적용 **[확정]** |
| 말줄임 | 발췌가 길면 `…`로 절단 **[확정]** |
| 발췌 길이 | 2줄 기준 **[제안]** — 카드 높이를 일정하게 유지 |

**하이라이트 규칙** **[문서: T4.13]** — 사이드바 옵션이 하이라이트에도 그대로 반영되어야 한다. "대/소문자 구분"이 ON이면 대소문자가 일치하는 부분만, "일치되는 단어"가 ON이면 단어 경계가 맞는 부분만 칠한다. **검색 조건과 하이라이트가 어긋나면 사용자는 결과를 신뢰하지 못한다.**

### 5.4 표 카드 **[확정]** — Phase 5

```
계약검토_체크리스트.xlsx · Sheet2      ⧉ 표 복사   원문 열기 ↗
┌──────────┬──────────────────┐
│ 항목      │ 기준              │   ← 헤더 행(음영)
├──────────┼──────────────────┤
│ 손해배상   │ 계약금액의 10% 이내 │
└──────────┴──────────────────┘
```

| 항목 | 명세 |
|---|---|
| 본문 | **원본 표 구조 그대로 렌더링** — 평문으로 뭉개지 않는다 |
| 데이터 | `chunk.table.header_row` / `chunk.table.rows` |
| 헤더 행 | 연한 배경 음영으로 구분 **[확정]** |
| 추가 버튼 | **표 복사** (아이콘 + 텍스트), `원문 열기` 좌측에 배치 **[확정]** |

> **헤더가 없는 표 처리** **[제안]**: Phase 1의 `TableData.from_rows()`는 1행짜리 표에서 `header_row`를 비워 둔다(첫 행을 헤더로 승격하면 데이터가 사라지는 문제 때문). 따라서 **렌더러는 `header_row`가 빈 경우를 반드시 처리해야 한다** — 음영 헤더 없이 데이터 행만 그린다.

**"표 복사"의 클립보드 형식** **[제안]**: TSV(탭 구분)로 복사하면 Excel·한글에 붙여넣을 때 셀 구조가 유지된다. 사내 문서 작업 흐름상 이 형식이 가장 실용적이다.

### 5.5 이미지 카드 **[확정]** — Phase 5

```
계약_흐름도.pptx · 5번 슬라이드                        확대
┌────────┐
│ 썸네일  │  이미지 내 텍스트는 인식되지 않았습니다.
└────────┘
```

| 항목 | 명세 |
|---|---|
| 썸네일 | 좌측 배치, 고정 크기 박스 **[확정]** |
| 썸네일 폭 | 300px 내외로 생성한 캐시본 사용 **[문서: TECH 4.4]** |
| 안내 문구 | `이미지 내 텍스트는 인식되지 않았습니다.` **[확정]** |
| 추가 버튼 | **확대** (우측 상단) **[확정]** |
| 데이터 | `chunk.image.image_path` |

> **문서 간 불일치 ①**: TECH 6.2는 "공통 요소: 파일명 + 위치 + **원문 열기** 버튼"이라 규정하지만, 목업의 이미지 카드에는 `확대`만 있고 `원문 열기`가 없다. 텍스트·표 카드에는 있다. **해소 필요** — §8 참고.

### 5.6 관련성 낮음 카드 **[확정]** — Phase 4

```
2023_품질기준서.pdf · 12페이지                   관련성 낮음
품질 검사 기준은 별도 부서 승인을 거쳐 확정된다…
```

유사도 임계값 미만 결과의 처리 **[문서: T4.14]**:

| 항목 | 명세 |
|---|---|
| 투명도 | 카드 전체를 흐리게 (opacity 저하) **[확정]** |
| 라벨 | 우측 상단에 `관련성 낮음` (회색) **[확정]** |
| 테두리 | 일반 카드보다 연하거나 없음 **[확정]** |
| 임계값 | 코사인 유사도 **0.5** **[문서: TECH 5.3]** |
| opacity 값 | **0.5** **[제안]** — 읽을 수는 있되 명확히 구분되는 수준 |

> **문서 간 불일치 ②**: 목업의 관련성 낮음 카드에는 `원문 열기` 버튼이 없고 `관련성 낮음` 라벨이 그 자리를 차지한다. 관련성이 낮아도 원문 확인은 필요할 수 있으므로 의도된 설계인지 확인 필요 — §8 참고.

### 5.6b 파일명 매치 배지 **[제안]** — Phase 10 T10.6

```
커뮤니케이션_나만의 코칭 질문_김성용.docx · 3페이지            파일명 매치
계약서 검토 시 기준이 되는 조항은...
```

`chunks_fts`는 본문뿐 아니라 파일명도 함께 색인해서, 검색어가 파일명에만 있어도 그 파일의 모든 청크가 결과에 낀다(FTS5가 컬럼이 아니라 행 단위로 매치, T10.6). 이 목업에는 명세가 없어 §5.6과 같은 방식(색에만 의존하지 않고 텍스트 라벨 병기, §10 원칙)으로 직접 정의했다.

| 항목 | 명세 |
|---|---|
| 라벨 | 우측 상단에 `파일명 매치` (회색, `관련성 낮음`과 동일 스타일) **[제안]** |
| 조건 | 검색어가 본문·표 캡션에는 하나도 없고 파일명에만 있을 때 (`HybridResult.is_filename_only_match`) |
| 관련성 낮음과의 관계 | 서로 다른 신호라 **동시에 뜰 수 있다**(대체 아님) |
| 흐림 처리 | 하지 않음 — §5.6의 흐림은 유사도 기준 전용, 이 배지는 텍스트 매치 여부만 알려준다 |

### 5.7 카드 타입 라우팅 **[문서: TECH 6.3]**

```
검색 결과 청크 → chunk.type 판별
   ├─ text  → 텍스트 카드
   ├─ table → 표 카드
   └─ image → 이미지 카드
                 └─ 하나의 결과 리스트에 함께 표시
```

**검색 로직은 타입과 무관하게 동일하게 동작하고, 렌더링 단계에서만 분기한다.** 신규 유형(수식, 코드 블록 등) 추가 시 렌더러만 확장하면 되는 구조를 유지한다 (PRD 6장 확장성 요구사항).

### 5.8 AI 챗봇 패널 **[확정, Phase 7.6에서 신규 정의 — Phase 7의 "AI 요약 카드"를 대체]**

> Phase 7에서 이 절은 "AI 요약 카드"(검색마다 자동 1회, 결과 목록 맨 위에 얹는 카드)였다. 실사용해보니 필요성이 불분명했고, 사용자가 원한 건 검색 결과를 근거로 후속 질문을 이어가는 **챗봇**이었다. 설계 중 **속도 실측이 발목을 잡았다** — LLM 생성 답변은 발췌 4~5개·긴 출력 기준 11~79초가 걸려 "은행 앱 챗봇처럼 빠르게 답해야 한다"는 요구와 정면 충돌했다. 문서별 팝업 챗봇, 자동 전체 생성 챗봇 두 설계안이 이 이유로 반려된 뒤 **사용자가 목업 2장으로 직접 제시한 2단 응답 구조**로 확정했다 [2026-08-11]. §4.2의 토글 라벨도 "AI 요약 보기" → **"AI 챗봇 사용"**으로 바뀐다.

**위치**: §4.2 "AI 챗봇 사용" 토글을 켜면 **결과/대화 영역**(§2.1)이 채팅 화면으로 바뀐다. 카드 목록 위에 얹는 요약 카드가 아니다 — 별도 패널로 완전히 교체된다. 토글을 끄면 카드 목록으로 되돌아간다(직전 검색 결과 그대로). **검색 결과 헤더(§3.3)는 챗봇 모드에서 숨는다** — 대화만 흐르는 화면이라 현재 검색어를 별도로 보여줄 자리가 없다.

**입력창은 챗봇 패널 자체가 갖지 않는다** **[Phase 7.7 개정]**. §3의 하단 공용 입력창(`InputBar`)을 검색 결과 모드와 그대로 공유한다 — Phase 7.6까지는 챗봇 패널이 자체 입력창(하단 고정 검색바와 별개)을 갖고 있어 입력 지점이 두 곳이었는데, 목업(`rag_ui_concept_*.html`) 두 화면이 입력창을 하나로 통일하고 있어 그 구조로 맞췄다[사용자 확정, 2026-08-13].

**2단 응답 구조**:

1. **즉시 발췌(기본, LLM 미사용)** — 메시지를 보내면 `hybrid_search()`의 상위 결과를 원문 그대로 보여준다. 검색 지연은 7~14ms(실측) — LLM을 기다리지 않는다. **[2026-08-14 개정, 같은 날 두 차례 수정]** top-1 하나만 보여주던 것을, 검색 결과 카드 목록과 개수 기준을 맞춰 **최대 5개**로 늘렸다 — 각 항목은 검색 카드(`ResultCard`/`TableCard`/`ImageCard`)를 그대로 재사용해 표는 그리드+복사 버튼, 이미지는 썸네일+확대 버튼을 자기 몫으로 갖는다. 5개를 넘으면 검색 결과 목록(§5.1)과 똑같은 "더보기" 버튼이 붙는다[사용자 확정 — 처음엔 텍스트 안내만이었다가 검색과 동일한 버튼으로 통일했다].
2. **AI 요약(선택, LLM 사용)** — 발췌 아래 "AI 요약 보기" 버튼을 **눌렀을 때만** LLM이 돈다. 그 턴이 1단계에서 이미 받은 검색 결과를 그대로 넘겨 검색을 다시 하지 않는다(top-1 컨텍스트 기준 — 결과가 여러 개 보여도 요약은 하나다). 느릴 걸 스스로 선택한 것이므로 대기가 자연스럽다.
3. **파일 열기** — 발췌 카드마다 자기 "원문 열기" 버튼을 갖는다[사용자 확정, 2026-08-14]. docx/pptx/xlsx는 T10.1 완료로 정확한 위치(문단/슬라이드/셀)까지 이동한다.

```
┌─ 결과/대화 영역 ────────────────────────────────┐
│                    코치인증자격시험 응시방법 찾아줘 [나] │  ← 우측 정렬, 파란 배경
│ [AI] 1. 시험단계: 서류전형 ---> 필기전형 --->    │  ← 좌측 정렬, ① 즉시·원문 그대로
│      실기전형 → 최종합격                        │
│      [AI 요약 보기]  [파일 열기 ↗]              │
│                                                │
│      ┄┄┄ (버튼 클릭 후) ┄┄┄                    │
│      ┌ AI 요약 ──────────────────────┐         │
│      │ 응시방법은…입니다. [1]  확인필요│         │  ← ② 클릭 시에만
│      └──────────────────────────────┘         │
└────────────────────────────────────────────┘
  (입력창은 이 패널 밖의 공용 InputBar — §3)
```

**말풍선 좌/우 정렬** **[신설, Phase 7.7, 목업 확정]**: 사용자 메시지는 우측(파란 배경), AI 발췌·요약은 좌측(흰 배경)에 정렬된다. 각 말풍선의 최대 폭은 대화 영역 폭의 **70%**로 제한한다(목업 기준값 65%에서 실사용 확인 후 조정[사용자 확정, 2026-08-13]) — Qt Style Sheets가 `max-width`를 지원하지 않아 위젯 크기 계산으로 직접 구현한다(창 크기 변경에 맞춰 다시 계산).

**메시지마다 독립 처리(stateless)** **[확정]** — 매번 검색어 전체 범위로 새로 검색한다. "두 번째는요?" 같은 대명사 참조는 지원하지 않는다. 턴(turn) 단위 구조라 각 턴이 자기 발췌·검색 결과를 따로 들고 있고, 한 턴의 "AI 요약 보기"는 그 턴에만 영향을 준다.

**② AI 요약의 상태 표시**(Phase 7의 표를 그대로 재사용 — 4단계 안전장치는 무수정):

| 상태 | 본문 | 비고 |
|---|---|---|
| 서버 기동 중 | "AI 모델을 준비하는 중입니다… (첫 실행은 몇 초 걸립니다)" | 첫 요청만. 실측 4.7초 |
| 생성 중 | "AI 요약을 만드는 중입니다…" | 중앙 18.3초(채택 모델) |
| 정상 | 답변 + 문장별 `[파일명, 위치]` | 위치 표기는 §5.2와 **같은 규칙** |
| 정상 + 검토 필요 | 답변 그대로 + 우측 상단 **확인 필요** 배지 + 사유 | 4단계 안전장치 |
| 기권 | "문서에서 찾을 수 없습니다." + "아래 검색 결과에서 직접 확인해 주세요." | 실패가 아니라 안전장치가 의도대로 동작한 것 |
| 근거 없음 | "관련 문서를 찾을 수 없습니다" + 사유 | 1단계에서 차단, sLM 미호출 |
| 실패 | 사유 그대로 (모델 미설치·기동 실패 등) | 붉은 계열 |

- **"확인 필요"일 때도 답변을 숨기지 않는다** **[확정]**. 겹침도 판정은 근사치라 옳은 환언을 낮게 볼 수 있다 — 감추면 사용자가 검증할 기회 자체가 사라진다. §5.6의 "관련성 낮음"이 카드를 지우지 않고 흐리게만 하는 것과 같은 원칙이다
- **② AI 요약 본문은 PlainText로 렌더링한다** **[확정]**. 문서에서 옮겨온 내용에 `<`가 섞일 수 있는데, RichText면 그 글자가 사라진다. **[2026-08-14 개정]** ① 즉시 발췌는 이제 검색 카드를 그대로 재사용하므로 카드 자체의 규칙(§5.3, 텍스트는 RichText+하이라이트 가능)을 따른다 — 다만 챗봇에서는 질의어 하이라이트를 넘기지 않는다(범위 밖, 필요해지면 별도로 다룬다).
- **② AI 요약 영역은 파란 계열**(① 발췌·결과 카드는 흰색) **[제안]** — 생성된 내용과 원문 발췌를 한눈에 구분하기 위함. Phase 7 요약 카드의 배색을 그대로 물려받았다
- **표·이미지 청크가 발췌에 포함될 때**: T10.12(2026-08-14)로 검색 카드와 동일하게 표는 그리드, 이미지는 썸네일+안내로 렌더링한다 — 더 이상 원시 텍스트가 아니다.
- **발췌 개수 상한 5 + "더보기" 버튼** **[제안, 2026-08-14]** — DESIGN에 결과 개수 상한 명세가 없어 사용자 요청으로 직접 정의했다. 검색 결과 카드 목록(§5.1)과 완전히 같은 버튼(같은 objectName, 클릭 시 나머지 전부 펼침)을 쓴다 — 두 화면의 결과 개수 기준·상호작용을 통일했다.
- 챗봇 패널은 "검색 결과 N건" 집계에 포함하지 않는다(집계 자체가 카드 목록 모드에서만 의미가 있다)

---

## 6. 하단 상태바 **[확정, Phase 7.7에서 폴더 관리 버튼 이동]**

```
문서 1,284개 인덱싱됨 · 마지막 갱신 10분 전
```

| 요소 | 위치 | 명세 |
|---|---|---|
| 인덱싱 문서 수 | 좌측 | 천 단위 구분 쉼표 **[확정]**, 회색 소형 텍스트 |
| 마지막 갱신 시각 | 좌측(구분자 ` · `) | **상대 시간** 표기 (`10분 전`) **[확정]** |

~~폴더 관리 | 우측 끝 | 클릭 시 폴더 관리 화면 진입~~ — **사이드바 하단(§4.5)으로 이동했다.** 목업이 모델·폴더 관리 두 버튼을 사이드바에 나란히 둬, 상태바에는 이제 인덱싱 정보만 남는다.

인덱싱 진행 중에는 이 영역에 **진행 바**를 노출한다 **[제안]** — TECH 4.6이 "진행 바와 함께 백그라운드 수행"을 요구하는데 목업에는 진행 중 상태가 없다. 문서 수 텍스트를 `인덱싱 중… 342/1,284`로 바꾸고 하단에 얇은 진행 바를 덧대는 방식을 권한다.

---

## 7. 상태 정의 **[제안]**

목업은 "결과가 있는 정상 상태" 하나만 담고 있다. 나머지 상태는 정의가 필요하다.

| 상태 | 결과 영역 표시 | 비고 |
|---|---|---|
| **초기** (검색 전) | 안내 문구 — 검색 방법 힌트 | 빈 화면 방지 |
| **검색 중** | 스켈레톤 카드 또는 로딩 인디케이터 | 인덱스 조회라 짧아야 정상 |
| **결과 없음** | `검색 결과가 없습니다` + 필터 완화 제안 | **[문서: T4.15]** |
| **인덱스 없음** | `먼저 대상 폴더를 지정해 주세요` + 폴더 관리 유도 | 최초 실행 경험 (T9.6) |
| **검색 오류** | 오류 문구 + 재시도 | — |

**결과 없음일 때 필터를 함께 안내한다** — 형식 필터나 검색 옵션이 켜져 있어서 0건인 경우가 많으므로, "docx만 검색 중입니다. 전체로 넓혀보세요" 같은 맥락을 제공하면 사용자가 원인을 즉시 파악한다.

---

## 8. 문서 간 불일치 — **해소 완료 (Phase 4 착수 전 확정)**

목업 v3와 기존 문서를 대조해 확인된 충돌 4건이며, 아래와 같이 확정했다.

| # | 항목 | PRD/TECH | 목업 v3 | **확정** |
|---|---|---|---|---|
| 1 | AI 요약 토글 위치 | "검색바 옆" (PRD 5.4, TECH 6.1) | 사이드바 검색 옵션 블록 | **v3 채택 (사이드바)** |
| 2 | 좌측 필터 범위 | 형식 + **폴더 + 기간** (TECH 6.1, PRD 5.2) | 형식만 | **MVP는 형식만.** 폴더·기간은 Phase 5 이후로 이월 |
| 3 | 이미지 카드의 원문 열기 | 전 카드 공통 (TECH 6.2) | `확대`만 존재 | **원문 열기 추가** (확대와 병기) |
| 4 | 관련성 낮음 카드의 원문 열기 | 공통 요소 | 라벨이 대체 | **원문 열기 추가** (라벨과 병기) |

**1번 근거**: v3가 확정 목업이므로 기준으로 삼는다. PRD 5.4·TECH 6.1의 "검색바 옆" 문구가 구버전이다.

**2번 근거**: PRD 5.2가 명시한 요구사항이므로 **버리는 것이 아니라 미루는 것**이다. 목업에 없는 UI를 Phase 4에서 즉흥 설계하면 "확정 목업 기준"이라는 전제 자체가 무너지고, MVP 배포 시점만 늦어진다.
다행히 백엔드 데이터는 이미 갖춰져 있다 — 폴더는 `documents.file_path`, 기간은 `documents.source_mtime`/`indexed_at`. **Phase 4에서는 검색 함수에 필터 파라미터만 미리 열어두고**, UI는 후속 Phase에서 붙인다. 그러면 나중에 검색 계층을 다시 건드릴 필요가 없다.

**3·4번 근거**: 이 제품의 핵심 가치는 "답변을 사용자가 즉시 검증할 수 있는 구조"(TECH 5.3 설계 철학)다. 특정 카드 유형에서만 원문 접근을 막을 이유가 없다. 관련성이 낮다는 판단 자체를 사용자가 원문으로 확인하고 싶을 수 있다. 목업에서 빠진 것은 공간 제약에 따른 생략으로 본다.

---

## 9. 한국어 처리 요구사항 **[문서: TECH 7장]**

UI 레이어에서 반드시 검증할 항목이다.

- **파일명 렌더링**: `2024_영업계약서_표준.docx`처럼 한글·숫자·언더바가 섞인 긴 파일명이 잘리지 않아야 한다. 잘릴 경우 **중간 생략**(`2024_영업…표준.docx`)이 확장자를 보존하므로 말줄임보다 낫다 **[제안]**
- **IME 조합 처리**: Phase 7.7에서 debounce 자동검색을 폐기(§3.2)하며 조합 중 문자로 검색·메시지가 나가는 문제 자체가 사라졌다 — Enter·검색 버튼 제출은 조합이 끝난 뒤에만 발생한다
- **줄바꿈**: 한글은 단어 경계 개념이 달라 `word-break` 설정에 따라 어색하게 끊긴다. 카드 본문에 적절한 줄바꿈 규칙 필요 **[제안]**
- **서체**: 나눔고딕을 번들해 사용한다 (§10.2). 대상 PC의 폰트 설치 상태와 무관하게 동일한 화면을 보장하기 위함이며, 오프라인·무권한 설치 요구사항과도 맞는다
- 검증 완료 사항: Phase 1에서 한글 파일명(한글·숫자·괄호가 섞인 실제 `.hwp` 파일) 파싱은 실검증 완료

---

## 10. 디자인 토큰 **[제안]**

목업 이미지에서 관측한 값을 기준으로 추정한 것이다. 정확한 값이 별도로 확정되어 있다면 교체한다.

### 10.1 색상

| 용도 | 값 | 관측 근거 |
|---|---|---|
| 페이지 배경 | `#F7F8F9` | 연한 회색 |
| 카드/패널 배경 | `#FFFFFF` | 흰색 |
| 테두리 | `#E5E7EB` | 연한 회색 |
| 본문 텍스트 | `#1F2937` | 진회색 |
| 보조 텍스트(위치·상태바) | `#9CA3AF` | 회색 |
| 강조/체크박스 | `#2563EB` | 파랑 |
| 검색어 하이라이트 | `#FEEEAD` | 연노랑(기존 `#FDE68A`에서 30% 연하게, 2026-08-21) |
| 표 헤더 배경 | `#F9FAFB` | 아주 연한 회색 |

### 10.2 서체 — 나눔고딕 **[확정]**

본문 서체는 **나눔고딕(NanumGothic)** 으로 확정한다. 프로젝트 `font/` 폴더에 실제 사용하는 2종만 동봉한다.

| 파일 | 패밀리 | 스타일 | CSS weight | 용량 |
|---|---|---|---|---|
| `font/NanumGothic.ttf` | NanumGothic | Regular | **400** | 4.6 MB |
| `font/NanumGothicBold.ttf` | NanumGothic | Bold | **700** | 4.5 MB |

> **사용 가능한 웨이트는 400과 700 두 단계뿐이다.** 그 외의 값(300·500·600·800 등)을 지정하면 렌더러가 **가짜 볼드(synthetic bold)를 합성**하거나 가장 가까운 웨이트로 대체해, 한글 획이 뭉개지고 흐릿해진다. 아래 스케일은 이 두 값만 사용한다.
>
> 나눔고딕은 Light(300)·ExtraBold(800)도 배포되지만, 현재 화면에서 쓰이지 않아 번들에서 제외했다(5.9MB 절감). 향후 디자인에서 필요해지면 해당 파일을 `font/`에 추가하고 이 표에 등록한다.

### 10.3 타이포그래피 스케일 **[제안]**

| 용도 | 크기 | 웨이트 | 색상 |
|---|---|---|---|
| 카드 파일명 | 14px | **700** (Bold) | 본문 텍스트 |
| 카드 본문 | 14px | 400 (Regular) | 본문 텍스트 |
| 위치·보조 텍스트 | 12px | 400 (Regular) | 보조 텍스트 |
| 사이드바 섹션 레이블 | 12px | 400 (Regular) | 보조 텍스트 |
| 표 헤더 셀 | 13px | **700** (Bold) | 본문 텍스트 |
| 검색 입력 | 14px | 400 (Regular) | 본문 텍스트 |
| 상태바 | 12px | 400 (Regular) | 보조 텍스트 |

- **사이드바 섹션 레이블**은 작은 크기와 회색만으로 이미 위계가 서므로 Regular를 쓴다. 위계를 더 주고 싶으면 다음 단계가 곧바로 700(Bold)이라 다소 무거워지므로, 굵기 대신 자간(letter-spacing)으로 조절할 것을 권한다.
- **검색어 하이라이트**는 배경색에 더해 **700(Bold)** 을 병용한다 — 색에만 의존하지 않기 위함이다(§11 참고).

### 10.4 폰트 번들링·로딩 **[제안]**

**폰트를 OS에 설치해서는 안 된다.** PRD 4장이 "관리자 권한 불필요"를, TECH 9.1이 "실행 파일 기준 상대 경로 / 레지스트리 등 설치 PC 종속 요소 배제"를 요구한다. 시스템 폰트 설치는 두 원칙을 모두 위반하며, TECH 9.4의 **포터블 압축 배포**(압축 해제만으로 즉시 실행)도 깨진다.

따라서 런타임에 `font/` 폴더에서 **상대 경로로 로드**한다.

**PySide6 확정**(§0)이므로 로딩 방식은 다음과 같다.

```python
from PySide6.QtGui import QFontDatabase
QFontDatabase.addApplicationFont("font/NanumGothic.ttf")
QFontDatabase.addApplicationFont("font/NanumGothicBold.ttf")
```

**폰트 폴백 스택** — 로딩 실패 시에도 한글이 깨지지 않도록 대비한다.

```
NanumGothic → Malgun Gothic → sans-serif
```

**번들 용량**: Regular·Bold 2종 약 **9.0MB**. 인스톨러 목표 용량(800MB~1.2GB) 대비 비중은 작다.

> **재배포 라이선스**: 나눔고딕은 SIL Open Font License(OFL)로 배포되며 번들·재배포가 허용된다. 다만 OFL은 **폰트와 함께 라이선스 전문을 동봉할 것**을 요구한다. 현재 `font/` 폴더에는 폰트 파일만 있으므로, 인스톨러·포터블 zip에 라이선스 파일(`font/LICENSE.txt` 등)을 함께 포함해야 한다. 이 앱은 사내 팀원에게 재배포되므로(TECH 9.3~9.4) 해당된다.

### 10.5 간격·형태

| 항목 | 값 |
|---|---|
| 카드 라운드 | 8px |
| 카드 내부 여백 | 16px |
| 카드 간 간격 | 12px |
| 사이드바 블록 간격 | 24px |

---

## 11. 접근성 **[제안]**

비개발자를 포함한 팀 전체가 사용자이므로(PRD 4장) 기본 접근성은 확보한다.

- **키보드 조작**: 검색창 → 사이드바 컨트롤 → 결과 카드 순으로 Tab 이동. 모든 토글·체크박스는 Space로 조작 가능
- **포커스 표시**: 키보드 포커스가 시각적으로 드러나야 함
- **색에만 의존하지 않기**: `관련성 낮음`은 흐림 처리에 더해 **텍스트 라벨을 병기**하므로 이 원칙을 이미 만족한다. 하이라이트도 배경색만이 아니라 굵기(Bold 700)를 함께 준다 (§10.3)
- **명도 대비**: 흐림 처리된 카드의 opacity를 과하게 낮추면 판독이 불가능해진다. 0.5 이하로는 내리지 않는다

---

## 12. Phase 매핑

| 컴포넌트 | Phase 4 (MVP) | Phase 5 | Phase 7 |
|---|---|---|---|
| 레이아웃 셸 · 검색바 | ✅ T4.1~T4.3 | — | — |
| 문서 형식 필터 | ✅ T4.4~T4.5 | — | — |
| 토글 3종 (컴포넌트) | ✅ T4.6 | — | — |
| ├ 대/소문자 · 일치단어 | ✅ T4.8~T4.9 (실동작) | — | — |
| └ AI 챗봇 사용 (Phase 7.6 이전: "AI 요약 보기") | ⚠️ T4.7 (placeholder) | — | ✅ T7.5 실동작 → ✅ T7.6.1~6 챗봇으로 교체 |
| PC 성능 콤보박스 | ✅ T4.10~T4.11 | — | — |
| 카드 공통 프레임 | ✅ T4.12 | (재사용) | — |
| 텍스트 카드 | ✅ T4.13 | — | — |
| 관련성 낮음 흐림 처리 | ✅ T4.14 | — | — |
| 빈 결과·스크롤 | ✅ T4.15 | — | — |
| 상태바 | ✅ T4.16~T4.17 | — | — |
| **표 카드** | — | ✅ T5.2 | — |
| **이미지 카드 + 썸네일 캐시** | — | ✅ T5.3~T5.5 | — |
| 타입 라우팅 | (텍스트만) | ✅ T5.1 | — |
| **AI 요약 카드 (§5.8)** | — | — | ✅ T7.5 (Phase 7에서 명세·구현) |

**Phase 4 완료 시점의 화면**: 목업 v3에서 표 카드·이미지 카드를 제외한 나머지 전부가 동작한다. 이 시점이 1차 MVP 배포 가능 지점이다.

---

## 13. 미확정 항목 요약

### 해소 완료

| # | 항목 | 결론 | 참조 |
|---|---|---|---|
| ~~1~~ | UI 프레임워크 확정 | **PySide6** — 외부 런타임 의존 없이 포터블 배포가 가능한 유일한 후보 | §0 |
| ~~2~~ | §8의 문서 간 불일치 4건 | 4건 모두 확정 (폴더·기간 필터는 MVP 제외) | §8 |
| ~~4~~ | 대/소문자 구분의 FTS5 구현 방식 | Phase 2에서 원문 보존 후처리 필터링으로 구현 완료 | §4.2 |
| ~~3~~ | AI 요약 토글의 Phase 4 처리 방식 (비활성 vs 안내 노출) | Phase 4~6은 비활성+툴팁으로 확정. **Phase 7에서 실동작으로 교체하되 모델 미설치 시에는 여전히 비활성** — "켜도 아무 일이 없으면 고장으로 보인다"는 이유가 그대로 유효하다 | §4.2 |

### 남은 항목

| # | 항목 | 결정 시점 | 참조 |
|---|---|---|---|
| 5 | 디자인 토큰 실제 값 — 색상·간격은 목업 관측 기반 추정치 (서체는 확정) | Phase 4 구현 중 | §10 |
| 6 | `font/`에 OFL 라이선스 파일 추가 — 재배포 시 동봉 필요 | Phase 9 이전 | §10.4 |

---

## 14. 3페이지 셸 재구성 **[Phase 11 확정, 2026-08-18]**

> **이 절은 §2(전체 레이아웃)와 §4(사이드바)를 대체한다.** §3(입력창)·§5(결과 카드)·
> §6(하단 상태바)은 그대로 유효하다 — 검색/대화 페이지 본문이 현재 화면과 동일하기 때문이다.

### 14.0 출처와 재해석 원칙

사용자가 제시한 목업 3장(검색/대화·문서 관리·설정)은 **이 PC의 다른 프로젝트**
(`D:/aiproj/aica/l3_2_codex/offline_document_search`, 창 제목 `Offline Document AI`)의
실제 화면이다 — 설정 화면에 그 프로젝트 경로가 그대로 찍혀 있어 확인했다.

따라서 **화면 구성만 가져오고, 각 요소가 표시할 값은 이 프로젝트가 실제로 낼 수 있는
것으로 다시 맞춘다.** 저쪽에 있는 기능이 여기 없거나(네트워크 차단 검사) 반대로 여기만
있는 기능(형식 필터·대소문자·일치단어·최근 검색)이 있어, 목업을 그대로 옮기면 기존 기능이
사라진다. 아래 배치는 그 손실을 막기 위한 조정을 포함한다.

### 14.1 전체 레이아웃

```
┌──────────────┬──────────────────────────────────────────┐
│ 사이드바      │  본문 영역 (페이지가 여기서 전환된다)      │
│ (다크 네이비) │  배경 #F3F6FA                            │
│              │                                          │
│  오프라인     │   ┌────────────────────────────────┐     │
│  문서 검색    │   │ 카드 (#FFFFFF)                 │     │
│              │   └────────────────────────────────┘     │
│  검색/대화 ▾ │                                          │
│   └ 확장영역 │                                          │
│  문서 관리    │                                          │
│  설정         │                                          │
│              │                                          │
│  최근 검색    │                                          │
│   · …        │                                          │
└──────────────┴──────────────────────────────────────────┘
```

- 사이드바 폭은 현재와 동일하게 고정한다.
- 본문 영역은 `QStackedWidget`으로 3페이지를 전환한다.
- **검색/대화 페이지만 하단 입력창(§3)과 상태바(§6)를 가진다.** 문서 관리·설정에는 없다.

### 14.2 사이드바

#### 14.2.1 구성 순서

| 순서 | 요소 | 비고 |
|---|---|---|
| 1 | 앱 타이틀 `오프라인 문서 검색` | 목업의 `Offline Document AI` 자리 |
| 2 | **검색/대화** + 확장 버튼 `▾` | 확장 버튼은 네비게이션과 **독립 동작** |
| 3 | └ 확장 영역: `AI 챗봇 사용` 토글, `문서 형식`(8종) | 접힘이 기본 |
| 4 | **문서 관리** | |
| 5 | **설정** | |
| 6 | 최근 검색 | **현 위치 유지**(T10.22의 최신순 정렬 그대로) |

#### 14.2.2 확장/축소 동작 **[확정]**

- `검색/대화` 항목 **오른쪽에 별도 버튼**을 두고, 그 버튼만 확장/축소를 담당한다.
  항목 본체를 누르면 페이지 이동만 일어나고 펼침 상태는 바뀌지 않는다.
- 확장 상태는 `AppState`에 저장해 다음 실행에서도 유지한다.
- 애니메이션은 범위에서 제외한다 — Qt QSS는 `transition`을 지원하지 않아
  `QPropertyAnimation`을 따로 붙여야 한다(Phase 7.7에서 같은 이유로 제외했다).

#### 14.2.3 `AI 챗봇 사용` 토글 **[확정 — 확장 영역 안, 기본 OFF]**

확장 영역 안에 둔다. 기본값은 **OFF**이며, 이는 현재 동작과 같다
(`AppState.ai_chat_enabled` 기본값 False, `test_toggle_is_off_by_default`가 고정하고 있다).

> ⚠️ **알려진 트레이드오프**: 이 토글은 단순 옵션이 아니라 **화면 동작을 바꾸는 모드
> 전환**이다 — 검색 모드는 결과 목록, 챗봇 모드는 자동 생성 답변(T10.23)이고 질문·결과가
> 완전히 분리돼 있다(T10.15). 접힌 상태에서는 현재 어느 모드인지 사이드바만 봐서는 알 수
> 없다. 사용자 확정 사항이므로 그대로 구현하되, 실사용에서 혼동이 보고되면 접힌 상태에도
> 모드 표식을 남기는 방안을 재검토한다.

### 14.3 검색/대화 페이지

**본문은 현재 화면과 동일하다.** §3(입력창)·§5(결과 카드)·§5.8(챗봇 말풍선)·§6(상태바)이
그대로 적용된다. 이 페이지에서 바뀌는 것은 왼쪽 사이드바뿐이다.

### 14.4 문서 관리 페이지

목업 구성을 그대로 따른다. 카드 4개로 구성한다.

| 카드 | 요소 | 데이터 출처 |
|---|---|---|
| LOCAL LIBRARY | 제목 `문서 관리`, 설명, 상태 배지 | 인덱싱 스레드 생존 여부 |
| 선택 폴더 | 경로 표시 + `폴더 선택` 버튼 | `AppState.target_folder` / 기존 `folder_dialog.py` |
| 인덱스 작업 | 상태 배지, `현재 단계`·`현재 파일`·`마지막 실행`, 버튼 3개 | 진행 콜백 `(done, total, path)`, `MAX(documents.indexed_at)` |
| INDEX OVERVIEW | 통계 7칸 | `IndexReport` |
| FILE DIAGNOSTICS | 파일별 오류 목록 + 건수 | `IndexReport.failures` |

#### 14.4.1 통계 7칸 매핑

| 표시 | 출처 | 상태 |
|---|---|---|
| 총 | `IndexReport.scanned` | ✅ **신설** [Phase 11-B] |
| 신규 | `IndexReport.created` | ✅ **신설** [Phase 11-B] |
| 변경 | `IndexReport.updated` | ✅ **신설** [Phase 11-B] |
| 삭제 | `IndexReport.pruned` | ✅ T10.5 |
| 미변경 | `IndexReport.skipped` | ✅ Phase 8 |
| 성공 | `IndexReport.indexed` | ✅ |
| 실패 | `len(IndexReport.failures)` | ✅ T10.2에서 실제로 채워지도록 수정 |

> `신규`/`변경` 분리는 `indexer/incremental`이 내부에서 이미 판별하고 있었다(`row is None`이면
> 신규). `needs_reindex()`의 bool 판정을 `classify_file() -> FileChange` 열거형으로 넓히고,
> `needs_reindex()`는 그것을 bool로 좁히는 얇은 래퍼로 남겼다(Phase 8 호출부가 그대로 산다).

> **`총`을 `indexed + skipped` 파생에서 `scanned`로 바꿨다 [Phase 11-B]** — 파싱에 실패한
> 파일은 `indexed`에도 `skipped`에도 안 잡혀서, 파생값으로 두면 파일 진단에는 실패가 떠
> 있는데 총계에서는 사라진다. 이 프로젝트에서 실패는 드문 일이 아니다(LibreOffice 미배치 시
> 구버전 포맷 전량).

#### 14.4.2 버튼

| 버튼 | 동작 | 상태 |
|---|---|---|
| 인덱스 업데이트 | `MainWindow._start_reindex()` | ✅ |
| 취소 | `IndexingThread.stop_event.set()` | ✅ T10.4 |
| 재시도 | **실패한 파일만 강제 재파싱** | ✅ **확정·구현** [Phase 11-B, 사용자 확정] |

> 🔴 **`재시도`가 "폴더 전체 재실행"이면 아무 일도 일어나지 않는다.** `parser/base.py`가
> 파싱을 **시작할 때** mtime·해시를 채우고 `store_document()`가 그것을 `status=FAILED`
> 문서에도 그대로 저장하기 때문에, 실패한 파일은 다음 인덱싱에서 `UNCHANGED`로 판정돼
> 건너뛰어진다 — LibreOffice를 나중에 설치해도 그 `.doc`은 영원히 다시 시도되지 않는다.
> Phase 8(증분 스킵)과 T10.2(FAILED 저장)가 만나 생긴 구멍이고, `재시도`가 이 구멍을 메운다.
> `indexer.pipeline.reindex_files()`가 증분 판정을 무시하고 지정한 파일만 다시 파싱한다.
>
> 🔴 **`reindex_files()`는 `_prune_stale_documents()`를 부르지 않는다.** 대상이 폴더 일부라
> 그대로 불렀다면 "이번 스캔에 없는 문서"에 **나머지 문서가 전부** 걸려 인덱스가 통째로
> 지워진다(T10.5의 정반대 방향 사고). 폴더 정리를 `index_folder()` 쪽에만 두어 구조적으로 막았다.
>
> **재시도 대상은 재시작 후에도 살아 있다** — `IndexReport.failures`는 이번 실행의 값이라
> 앱을 껐다 켜면 사라지지만, 실패는 인덱스에 `status=failed` 문서로 남는다
> (`failed_document_paths()`). 안 그러면 앱을 켠 직후에는 인덱싱을 한 번 돌리기 전까지
> 재시도할 방법이 없다.

#### 14.4.3 기존 진행률 팝업과의 관계 **[확정]**

`IndexingProgressDialog`(T10.4 비모달 팝업 + 취소, T10.7 경과·남은 시간)와 역할이 겹친다.
**문서 관리 페이지가 열려 있을 때는 팝업을 띄우지 않고 페이지 안에서 진행을 보여준다.**
다른 페이지에서 인덱싱이 시작된 경우(폴더 감시·모드 전환 자동 보완 등)에는 기존 팝업을
그대로 쓴다 — 사용자가 문서 관리 화면을 보고 있지 않은데 진행 상황을 놓치면 안 된다.

### 14.5 설정 페이지

| 그룹 | 항목 | 출처 | 상태 |
|---|---|---|---|
| OPTIONS | **PC 성능 선택**(경량/권장) | 사이드바에서 **이동** | ✅ 기존 위젯 재사용 |
| OPTIONS | **모델 관리**(sLM 설치 상태) | 사이드바에서 **이동** | ✅ 기존 다이얼로그 |
| OPTIONS | 모델 상주 | `SlmService.set_idle_timeout(0)` | ✅ **완료** [11-C] |
| OPTIONS | 유휴 종료 시간 | `SLM_IDLE_TIMEOUT_SEC`(Phase 7 결정: 5분)이 기본 | ✅ **완료** [11-C] |
| OPTIONS | AI CPU 사용 | `SlmService.set_n_threads()` ← `resolve_n_threads(모드)` | ✅ **완료** [11-C] |
| LOCAL AI & RUNTIME | Embedding 모델 경로 | `config/settings.py` 프로파일 | ✅ **완료** [11-C] |
| LOCAL AI & RUNTIME | 로컬 LLM 모델 경로 | sLM 프로파일 `local_path` | ✅ **완료** [11-C] |
| LOCAL AI & RUNTIME | llama.cpp runtime | `slm/runtime.is_available()` | ✅ **완료** [11-C] |
| LOCAL AI & RUNTIME | 런타임 데이터 | `data/` | ✅ **완료** [11-C] |
| ~~LOCAL AI & RUNTIME~~ | ~~네트워크 "외부 연결 비활성화됨"~~ | — | ❌ **제거 확정** |

- **네트워크 항목 제거 이유**: 이 프로젝트에는 외부 연결을 차단하거나 검사하는 코드가 없다.
  표시하면 근거 없는 문구가 된다.
- **표시 전용 항목도 "없으면 없다"고 말한다** [11-C] — 경로만 보여주면 그 파일이 실제로
  있는지 알 수 없다. 설치 안 된 것은 `(설치되지 않음)`을 덧붙인다.
- **모델 상주는 이 프로젝트에서 실제로 동작한다.** 목업 원본에는 *"현재 runtime에서는
  상주가 아직 적용되지 않지만 선택값은 저장됩니다"*라고 적혀 있지만, 이쪽은 Phase 7에서
  유휴 5분 자동 종료를 실제로 구현했고 실물로 검증했다(안드로이드 스튜디오 동시 작업 전제).

#### 14.5.1 PC 성능 전환의 부작용 **[주의]**

모드를 바꾸면 그 모델의 벡터가 없을 때 **자동 보완(재인덱싱)이 시작된다**(T10.26).
설정 페이지에서 콤보를 바꾸면 그 자리에서 진행률이 뜨게 되므로, 14.4.3과 같은 규칙을 적용해
**문서 관리 페이지로 이동시키지 않고 기존 팝업으로 처리**한다.

#### 14.5.2 적용 시점 **[Phase 11-C 확정]**

> 🔴 **이 절의 원래 전제가 셋 중 하나에만 해당한다는 것이 11-C 착수 조사에서 드러났다.**
> `유휴 종료 시간`은 생성자 인자이기도 하지만 `touch()`가 **요청마다 `self._idle_timeout_sec`를
> 다시 읽어** 타이머를 건다 — 값만 갈아 끼우면 재기동 없이 다음 요청부터 적용된다.
> 실제로 기동 인자인 것은 `AI CPU 사용`(`n_threads`)뿐이다(`runtime.start_server()`에만 전달).

| 항목 | 적용 방식 |
|---|---|
| 모델 상주 | **즉시**(재기동 없음) — 유휴 타이머를 아예 안 거는 것으로 구현. `set_idle_timeout(0)` |
| 유휴 종료 시간 | **즉시**(재기동 없음) — `set_idle_timeout(초)` |
| AI CPU 사용 | **떠 있으면 즉시 내리고 다음 요청에서 새 값으로** [사용자 확정] — `set_n_threads()` |

`AI CPU 사용`이 1안인 이유는 같은 방식이 이미 코드에 있기 때문이다 — `set_profile()`이 모델을
바꿀 때 "떠 있으면 내린다"로 동작한다. 메모리 4.8GB를 그 자리에서 함께 돌려주는 것이 Phase 7의
유휴 종료 결정(안드로이드 스튜디오 동시 작업 전제)과도 결이 맞는다.

🔴 **세 setter 모두 `_lock`을 기다리지 않는다.** 그 락은 `chat()`이 추론 내내(채택 모델 중앙
18.3초) 붙들고 있어서, 설정 화면에서 부르는 함수가 락을 기다리면 **UI가 그만큼 얼어붙는다**
(T10.23에서 취소 함수로 같은 함정을 겪었다). 값만 바꾸고, 락을 **즉시 얻을 수 있을 때만**
타이머 재장전·서버 종료를 한다. 즉시 못 내렸더라도 `ensure_ready()`가 기동 당시 값
(`_active_n_threads`)과 비교해 다시 올리므로 **새 값이 누락되지는 않는다** — 즉시성만 한 요청
늦어진다.

#### 14.5.3 `AI CPU 사용`의 값 **[Phase 11-C, 제안]**

숫자가 아니라 **모드**(`auto`/`half`/`max`)를 저장한다. 폴더째 다른 PC로 옮기는 것이 이 제품의
전제인데(TECH 9.1), 저장된 "8스레드"가 4코어 PC에서는 전혀 다른 의미가 된다. 실제 숫자는 그
PC의 코어 수에서 그때그때 뽑는다(`config.settings.resolve_n_threads`).

`auto`는 숫자를 계산하지 않고 `None`을 넘겨 llama.cpp 자신의 판정에 맡긴다 — Phase 6~7의
실측치가 전부 인자를 안 넘긴 상태에서 나온 값이라 기본값이 그 조건과 같아야 한다.

### 14.6 색상 토큰 추가·변경 **[Phase 11]**

§10.1 표에 아래를 추가·교체한다.

| 용도 | 값 | 비고 |
|---|---|---|
| **본문 영역 배경** | **`#F3F6FA`** | **사용자 확정** — 기존 `#F7F8F9`를 대체한다 |
| 사이드바 배경 | `#1E293B` 계열 다크 네이비 | 목업 관측값 **[제안]** |
| 사이드바 본문 텍스트 | `#FFFFFF` / 비활성 `#94A3B8` | **[제안]** |
| 강조(버튼·토글) | `#2563EB` | **변경 없음** — 목업과 기존 토큰이 이미 같다 |
| 카드 배경 | `#FFFFFF` | 변경 없음 |

> 강조색이 그대로라는 점이 중요하다 — Phase 7.7에서 목업의 `#1E90FF` 대신 기존 `#2563EB`를
> 유지하기로 한 결정이 여전히 유효하고, 검색바 포커스·버튼·요약 카드 테두리가 공유하는
> 토큰이라 바꿨다면 전 화면을 다시 시각 검증해야 했다.

### 14.7 UI에서 제거하되 기능은 유지 **[확정]**

`대/소문자 구분`, `일치되는 단어` 두 토글을 화면에서 없앤다. **검색 기능 자체는 남긴다.**

- `hybrid_search()`·`SearchWorker`의 `case_sensitive`/`exact_word` 인자는 그대로 둔다 —
  Phase 2에서 FTS5 접두 매칭/완전 토큰 매칭 전환까지 만들어 넣은 경로다.
- `MainWindow`는 위젯 대신 **`AppState`에서 읽는다.** 기본값은 둘 다 꺼짐(현재 기본과 동일).
  `data/app_state.json`을 직접 고치면 켤 수 있어, 나중에 UI를 되살릴 때 배선이 남아 있다.
- **기능 회귀는 계속 지켜진다** — `tests/test_indexer_search.py`에 UI 없이 검증하는 테스트가
  3건 있다(대소문자 1건, 완전 토큰 매칭 2건). 제거 대상은 위젯을 조작하는 UI 통합 테스트
  2건(`test_case_sensitive_toggle_changes_results`, `test_exact_word_excludes_mid_token_match`)뿐이다.

### 14.8 흡수·제거되는 기존 UI

| 기존 요소 | 처리 |
|---|---|
| 사이드바 `폴더 관리` 버튼 | **문서 관리 페이지의 `폴더 선택` 버튼으로 흡수** |
| 사이드바 `모델 관리` 버튼 | 설정 페이지로 이동 |
| 사이드바 `PC 성능 선택` 콤보 | 설정 페이지로 이동 |
| 사이드바 `대/소문자 구분`·`일치되는 단어` | 제거(기능은 14.7대로 유지) |
| 사이드바 `문서 형식`·`AI 챗봇 사용` | 확장 영역으로 이동 |
| 사이드바 `최근 검색` | **위치 유지** |

### 14.9 미확정 항목

| 항목 | 필요한 결정 |
|---|---|
| ~~설정값 적용 시점~~ | ✅ **확정** [Phase 11-C] — §14.5.2 표 참고(상주·유휴는 즉시, CPU는 서버를 내림) |
| 사이드바 색상 실제 값 | `#1E293B` 계열 **[제안]** — 목업에서 추정한 값이라 확정 필요 |
| ~~`재시도` 버튼의 정확한 의미~~ | ✅ **확정** [Phase 11-B, 사용자 확정] — "실패한 파일만 강제 재파싱"(§14.4.2) |

### 14.10 확장 영역의 표시 범위 **[Phase 11-B 확정, 사용자 확정]**

확장 영역(문서 형식 필터 · AI 챗봇 토글)은 **검색/대화 페이지에서만 보인다.** 11-A에서는
네비게이션 항목에 붙어 있다는 이유만으로 문서 관리·설정 화면에서도 남아 있었는데, 안에 든
것이 전부 검색 옵션이라 그 화면에서는 조작할 이유가 없다.

🔴 **펼침 상태 자체(`AppState.search_expanded`)는 건드리지 않는다.** 페이지를 옮길 때 접어
버리면 돌아왔을 때 사용자가 펼쳐 둔 상태가 사라지고, 그 값이 저장까지 되면 다음 실행에도
접힌 채로 시작한다 — 보이는 것만 감추고 상태는 그대로 둔다.
